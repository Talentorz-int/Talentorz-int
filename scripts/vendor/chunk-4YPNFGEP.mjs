import {
  U as n7,
  V as i7,
  X as s7,
  aa as Ve,
  ba as k7,
  ca as l7,
  fa as $7,
  j as Je,
  u as j7,
  v as wj,
  y as Jj,
} from "chunk-424ANBSC.mjs";
import {
  c as ct,
  d as $t,
  e as yj,
  g as dt,
  i as Et,
  l as u7,
  m as G7,
  n as R7,
  o as Xe,
  p as HA,
  q as F7,
  r as Qe,
  s as f7,
  x as I7,
  y as ze,
} from "chunk-47VSOHEQ.mjs";
import { b as d7, c as E7, g as B7, h as p7 } from "chunk-7JCGGICB.mjs";
import {
  $c as Kr,
  B as xj,
  Be as wr,
  C as hr,
  Ca as qj,
  Ee as Jr,
  Eg as e7,
  Ff as Xr,
  Ge as Hr,
  Hf as Qr,
  Nf as zr,
  Ng as Ue,
  Pc as MA,
  Qb as Pr,
  Qc as Dr,
  Qf as Wr,
  Rc as Nr,
  Rf as Yr,
  Yc as xr,
  _c as qr,
  _f as Lj,
  ad as Lr,
  af as Kj,
  bf as Vr,
  cf as Ur,
  da as gr,
  ea as _r,
  eb as Sr,
  fb as yr,
  j as Pt,
  ka as br,
  tc as Mr,
  ub as Tr,
  vb as vr,
  wh as Hj,
} from "chunk-CLNV2WTB.mjs";
import { B as c7, l as A7 } from "chunk-243G3IP7.mjs";
import { a as vt } from "chunk-BLTSRKBK.mjs";
import { a as m7 } from "chunk-V3C5CSFI.mjs";
import { c as Vj } from "chunk-TF2UUO3R.mjs";
import { b as L, c as Or } from "chunk-SJT3LDOF.mjs";
import { a as a7 } from "chunk-IAA62EI4.mjs";
import { a as o7 } from "chunk-AJZYH3BG.mjs";
import { c as JA, e as r7 } from "chunk-IY77CULL.mjs";
import { B as He } from "chunk-E4V4QQI3.mjs";
import {
  Ce as cr,
  Hh as mr,
  Ie as $r,
  Jh as ur,
  P as vA,
  Pd as kr,
  Ph as Gr,
  Uc as sr,
  Uh as Rr,
  Ve as dr,
  Ye as Er,
  Yh as Fr,
  Ze as Br,
  _e as pr,
  ei as fr,
  gi as Ir,
  ha as qt,
  rc as ar,
  ze as lr,
} from "chunk-KIWT3OS5.mjs";
import { f as q } from "chunk-WMNAJO4L.mjs";
import { a as Le } from "chunk-YNCW2456.mjs";
import { a as Zr } from "chunk-Z4IZU2LA.mjs";
import { f as Ke } from "chunk-QNKC3NEE.mjs";
import { Qf as Fe } from "chunk-LNLOS7MO.mjs";
import {
  $f as Ct,
  Bj as Mj,
  C as KA,
  Cj as Ne,
  Ei as FA,
  Fi as sj,
  Gi as Lt,
  Je as nj,
  Kc as ft,
  Le as Ot,
  Nf as Dt,
  Pi as wt,
  Qe as Tt,
  Qk as Ht,
  Ro as ir,
  Tg as oA,
  We as vj,
  Ya as oj,
  Yf as sA,
  Yg as aj,
  Za as Ft,
  Ze as Mt,
  Zf as Pj,
  ao as kj,
  b as be,
  bo as tr,
  co as rr,
  ed as ht,
  ep as PA,
  hd as gt,
  jf as Pe,
  jo as or,
  ko as nr,
  l as pt,
  nd as LA,
  nk as Jt,
  og as ij,
  pg as Me,
  qg as De,
  ri as Kt,
  sd as wA,
  ug as Ce,
  vg as Nt,
} from "chunk-UVMW6WIE.mjs";
import { i as Bt } from "chunk-SEYZCGEY.mjs";
import { i as lt } from "chunk-44TE32RJ.mjs";
import { a as Cr } from "chunk-TW4P4OFD.mjs";
import { d as Oe } from "chunk-URITSZHW.mjs";
import { c as DA } from "chunk-HLFUGSYU.mjs";
import { g as t7 } from "chunk-LZDNIQ2U.mjs";
import { a as we } from "chunk-ZWBHDWJC.mjs";
import {
  $t as Zt,
  Ae as _e,
  Bd as hj,
  Eq as Sj,
  Gt as Dj,
  Hq as ye,
  J as C,
  Jt as Cj,
  Kk as jt,
  Kt as xe,
  Li as W6,
  Nr as mt,
  Nt as qe,
  Od as L6,
  Or as ut,
  Pd as w6,
  Pg as EA,
  Pt as Qt,
  Qd as J6,
  Qt as zt,
  Rc as N6,
  Rd as H6,
  Rt as Wt,
  Sc as fe,
  Sd as he,
  Se as z6,
  St as Yt,
  Tc as x6,
  Tj as RA,
  Tp as it,
  Ud as ge,
  Uj as Y6,
  Vd as V6,
  Vp as at,
  Vq as kt,
  Vs as ve,
  Wd as U6,
  Wj as _j,
  Xd as X6,
  Xj as Z6,
  Yc as tA,
  Yr as rj,
  Zb as fj,
  Zp as st,
  _r as Gt,
  as as Rt,
  at as It,
  au as Ar,
  bd as q6,
  bk as At,
  c as Ge,
  cc as P6,
  cd as OA,
  dd as rA,
  dk as qA,
  du as Nj,
  ed as ej,
  et as _t,
  fd as tj,
  fe as Q6,
  ft as Tj,
  fu as jr,
  g as S6,
  gc as M6,
  gd as Ie,
  gn as et,
  gt as bt,
  hc as NA,
  ho as rt,
  ht as St,
  id as K6,
  it as yt,
  jd as W,
  kd as xA,
  ku as er,
  mb as Aj,
  md as Y,
  me as gj,
  ne as U,
  nn as bj,
  sd as TA,
  td as b,
  tn as tt,
  ud as Ij,
  ut as xt,
  vo as ot,
  wc as D6,
  xd as $A,
  xo as nt,
  yc as C6,
  yd as N,
  zc as jj,
  zt as Vt,
} from "chunk-C45BQYIA.mjs";
import { b as Xt } from "chunk-KSPVA2M3.mjs";
import { i as Se } from "chunk-A4TNDN6S.mjs";
import { sa as Oj, ua as dA, va as Te, ya as Ut } from "chunk-HSCVLSVX.mjs";
import { a as O6 } from "chunk-OETKI2VS.mjs";
import { b as b6 } from "chunk-AUNF3KWQ.mjs";
import { jg as v6 } from "chunk-IGDTGOWI.mjs";
import { a as y6 } from "chunk-LEYNIZPU.mjs";
import { o as jA } from "chunk-RJAQ7ZXH.mjs";
import { b as v, e as eA, f as yA, j as ZA, l as T6, m as Re } from "chunk-DYNCKUFC.mjs";
import { a as nn } from "chunk-SJONTHBI.mjs";
import { j as SA } from "chunk-WKFLJETF.mjs";
import { k as ue } from "chunk-WJBO7C5H.mjs";
import {
  Aa as h6,
  Ba as _,
  Da as g6,
  Ga as pe,
  La as _6,
  X as H,
  Ya as me,
  Z as F6,
  ia as cA,
  k as YA,
  ra as f6,
  sa as V,
  ta as Q,
  ua as uA,
  wa as GA,
  xa as Fj,
  za as I6,
} from "chunk-3GX5YKYT.mjs";
import { a as d, b as aA } from "chunk-6PNPO5DW.mjs";
import { e as Be, j as E, k as w, l as bA, m as Rj } from "chunk-XELMBOBL.mjs";
var an = { track: !0 };
function h7(t) {
  let j = { ...t, event: "external_component_insert" };
  b6(j, an);
}
function _7(t, j, A, e, { shouldOpenPage: r = !0, renamedIds: o = new MA() } = {}) {
  let n = qr(t, j, A, "duplicate", {
    enterIsolation: !1,
    preferredName: e,
    insertionIndex: t.tree.root.children.findIndex((i) => i.id === j.id) + 1,
    renamedIds: o,
  });
  return (
    t.stores.persistedUserDefaults.newContentAsDraft &&
      n.set({ isDraft: !0, duplicatedFrom: gt(n.duplicatedFrom, j.id) }),
    r && t.stores.scopeStore.select(n.id, { keepHistory: !1 }),
    n
  );
}
function sn(t, j, A) {
  let e = new MA(),
    r = new Set(
      t.stores.scopeStore
        .getDesignPageNodes()
        .map((s) => s.resolveValue("name"))
        .filter(v)
    ),
    o = kr(A ?? j.resolveValue("name") ?? "Design", r),
    n = j.clone({ name: o, children: new bj() });
  e.set(j.id, n.id);
  let i = [];
  for (let s of j.children) {
    let a = Nr(s, new Map(), new Map(), e, !1);
    (i.push(a), e.set(s.id, a.id), n.addChild(a));
  }
  t.tree.insertNode(n, void 0, t.tree.root.children.findIndex((s) => s.id === j.id) + 1);
  for (let s of i) Dr(t.tree, t.componentLoader, s, e, !1);
  return (q("design_page_create", { pageId: n.id, source: "duplicate" }), n);
}
async function Ma(t, j, A) {
  let e = j.isLoaded() ? j : await j.load();
  if (e) return sn(t, e, A);
}
var BA = Be(y6(), 1),
  b7 = "dismissedInstantNPMToast",
  S7 = "showedInstantNPMToast",
  y7 = "instant-npm-toast",
  xa = () => window.localStorage.getItem(S7) === "true",
  O7 = () => window.localStorage.getItem(b7) === "true";
function kn() {
  return (0, BA.jsxs)(BA.Fragment, {
    children: [
      (0, BA.jsx)(Fe, { children: "Installed NPM library." }),
      (0, BA.jsxs)("a", {
        href: "https://www.framer.com/developers/#faq",
        target: "_blank",
        children: [" ", (0, BA.jsx)(Fe, { children: "Learn more." })],
      }),
    ],
  });
}
function T7() {
  (window.localStorage.setItem(S7, "true"),
    L({
      type: "add",
      variant: "info",
      text: (0, BA.jsx)(kn, {}),
      key: y7,
      duration: 1 / 0,
      showCloseButton: "never",
      action: { title: "Dismiss", onClick: () => window.localStorage.setItem(b7, "true") },
    }));
}
function qa() {
  L({ type: "remove", key: y7 });
}
var v7 = "https://app.framerstatic.com/framer_compiler_bg-UGK2KYHD.wasm";
var pA = cA("compiler");
async function Za() {
  await lj();
}
function P7(t) {
  return {
    code: `const err = new Error(${JSON.stringify(String(t))}); err.name = "CompilationError"; throw err;`,
    sourceMap: void 0,
    annotations: {},
    exportedNames: [],
    reExportedModules: [],
    imports: { absolute: [], relative: [], bare: [] },
  };
}
function M7(t, j, A) {
  let e = { ...j.exports };
  A &&
    ((e.__FramerMetadata__ = { type: "variable" }),
    (t = t.concat(
      `
export const __FramerMetadata__ = `,
      JSON.stringify({ exports: e })
    )));
  let r = Object.entries(e)
      .filter(([, n]) => n.type !== "tsType")
      .map(([n]) => n),
    o = {};
  for (let [n, i] of Object.entries(j.exports)) i.annotations && (o[n] = i.annotations);
  return { moduleCode: t, annotations: o, exportedNames: r };
}
async function D7({ localId: t, name: j, source: A, includeSourceMap: e = !0 }) {
  let r = await lj();
  try {
    let {
        code: o,
        metadata: n,
        map: i,
      } = await r.transformSync(A, {
        fileName: DA(j).source,
        framerContractVersion: 1,
        mediaType: "tsx",
        minify: !1,
        compress: !1,
        mangle: !1,
        sourceMap: e,
        useReactRefresh: !0,
      }),
      s = `
if (!window.$RefreshReg$) throw new Error("React refresh preamble was not loaded. Something is wrong.");
const prevRefreshReg = window.$RefreshReg$;
const prevRefreshSig = window.$RefreshSig$;
window.$RefreshReg$ = window.reactRefreshRuntime.getRefreshReg("${t}");
window.$RefreshSig$ = window.reactRefreshRuntime.createSignatureFunctionForTransform;

${o}

window.$RefreshReg$ = prevRefreshReg;
window.$RefreshSig$ = prevRefreshSig;`,
      { moduleCode: a, annotations: k, exportedNames: l } = M7(s, n, !0);
    return {
      code: a,
      sourceMap: i ?? void 0,
      imports: n.requestedModules,
      annotations: k,
      exportedNames: l,
      reExportedModules: n.reExportedModules,
    };
  } catch (o) {
    let n = `Failed to compile development module ${j}: ${o}`;
    return (
      pA.debug(n, "(enable trace logging to see full source)"),
      pA.trace(A),
      t.startsWith("codeFile/") ||
        (pA.reportCriticalError(new Error(n, { cause: o }), { moduleName: j, moduleSource: A }),
        q("application_error", { message: n, area: "compiler" })),
      P7(o)
    );
  }
}
async function As(t, j) {
  await (
    await lj()
  ).transformSync(j, {
    fileName: DA(t).source,
    framerContractVersion: 1,
    mediaType: "tsx",
    minify: !0,
    compress: !1,
    mangle: !1,
    sourceMap: !1,
    useReactRefresh: !1,
  });
}
async function C7({
  name: t,
  source: j,
  type: A,
  includeSourceMap: e = !0,
  addFramerMetadata: r = !0,
  telemetrySession: o,
}) {
  pA.debug("Compiling module", t, "; with source map:", e);
  let n = performance.now(),
    i = await lj(),
    s = o?.start("transform"),
    a = A === "collection";
  try {
    let {
        code: k,
        map: l,
        metadata: c,
      } = await i.transformSync(j, {
        fileName: DA(t).source,
        framerContractVersion: 1,
        mediaType: "tsx",
        minify: !0,
        compress: a,
        mangle: a,
        sourceMap: e,
        useReactRefresh: !1,
      }),
      { moduleCode: $, annotations: p, exportedNames: B } = M7(k, c, r);
    return {
      code: $,
      sourceMap: l ?? void 0,
      imports: c.requestedModules,
      annotations: p,
      exportedNames: B,
      reExportedModules: c.reExportedModules,
    };
  } catch (k) {
    let l = `Failed to compile module ${t}: ${k}`;
    return (
      pA.debug(l, "(enable trace logging to see full source)"),
      pA.trace(j),
      t.startsWith("codeFile/") ||
        (pA.reportCriticalError(new Error(l, { cause: k }), { moduleName: t, moduleSource: j }),
        q("application_error", { message: l, area: "compiler" })),
      P7(k)
    );
  } finally {
    (s?.end(), we(`\u{1F4DD} Compile ${t}`, n, void 0, "vekter"));
  }
}
async function N7(t) {
  let j = performance.now();
  try {
    return { ok: !0, value: await (await lj()).analyzeImports(t) };
  } catch (A) {
    return (
      pA.warn("Failed to analyze imports:", A),
      A instanceof Error ? { ok: !1, error: A } : { ok: !1, error: new Error(`${A}`, { cause: A }) }
    );
  } finally {
    let A = performance.now();
    we("\u{1F575}\u{1F3FB} Analyze Imports", j, A, "vekter");
    let e = A - j;
    pA.debug("\u{1F575}\u{1F3FB} Analyze Imports took", e.toFixed(2), "ms");
  }
}
var Uj;
async function lj() {
  if (!Uj) {
    let t = new Worker(Xt("./modulesCompilerWorker.js")),
      j = Vt(t);
    ((Uj = j
      .initWithPathToWasm(v7)
      .then(() => ({ transformSync: j.transformSync, analyzeImports: j.analyzeImports }))),
      Uj.catch(() => {
        if (Cr() || !F6.isProduction)
          throw new Error(`\u274C Your dev setup is struggling to run the compiler\u2019s WASM binary.
To fix it, run "make clean" followed by "make dev".`);
      }));
  }
  return Uj;
}
function q7(t, j, A) {
  if (!b(j)) return;
  let e = j.getSupportedLayout(t),
    r = j.getCurrentVariantSize(t);
  (Ge(e.width) &&
    r?.widthType !== 0 &&
    ((A.widthType = 2), A.left !== null && A.right !== null && (A.right = null)),
    Ge(e.height) &&
      r?.heightType !== 0 &&
      ((A.heightType = 2), A.top !== null && A.bottom !== null && (A.bottom = null)));
}
function K7(t, j) {
  let [A, ...e] = j.stores.selectionStore.ids;
  if (!A || e.length) return null;
  let r = j.tree,
    o = r.getNode(A),
    n = !1;
  for (; !n && o && ((n = vA(r, o, t, j.stores.scopeStore.active.id, j.componentLoader)), !n); )
    o = r.getParent(o.id);
  return o;
}
function L7(t, j) {
  let A = new Set(),
    e = new Set();
  for (let r of j) {
    if (e.has(r)) continue;
    let o = t.get(r);
    if (o) {
      if (oA(o)) {
        A.add(r);
        continue;
      }
      for (let n of o.ancestors())
        if (!e.has(n.id) && (e.add(n.id), n.cache.reset(), !!oA(n))) {
          A.add(n.id);
          break;
        }
    }
  }
  return A;
}
function cn(t, j, A) {
  let e = [],
    r = [],
    o = (i, s) => {
      if (!ye(i)) return;
      let a = i.getProvidedVariableMap(t, s, j, j.activeBundleHash);
      if (!a) return;
      let k = i.getPrimaryId();
      (r.push(k), e.push(a));
    };
  rj(A) && o(A, void 0);
  let n = A;
  for (let i of A.ancestors()) {
    let s = n;
    if (((n = i), o(i, s), De(i))) {
      let a = Ce(t);
      a && e.push(a);
    }
  }
  return { variableMaps: e, providerIds: r };
}
var w7 = (() => {
  let t = [],
    j = [],
    A;
  return (e, r, o, n, i) => {
    if (!o || !i || !n) return null;
    let { variableMaps: s, providerIds: a } = cn(e, r, i);
    if (s.length === 0) return null;
    if (!A || !dA(t, s) || !dA(j, a)) {
      let g = function (G, S) {
        if (!ye(G)) return;
        let x = G.getProvidedVariableMap(e, S, r, r.activeBundleHash);
        if (!x) return;
        let f = G.getPrimaryId(),
          O = G.getVariableSourceIdentifier();
        O && (R[f] = O);
        for (let [J, h] of x)
          ($.add(J),
            p.add(h.type),
            h.type === "enum" &&
              (d(O, "Variable source identifier should exist for enum variables"),
              u.push({ variable: h, providerId: f, sourceIdentifier: O })));
        (nt(G) && (B = f), W(G) && (m = f), l.set(f, x));
        let M = G.getProvidedControlMap(e, S, r, r.activeBundleHash);
        M && c.set(f, M);
      };
      var k = g;
      ((t = s), (j = a));
      let l = new Map(),
        c = new Map(),
        $ = new Set(),
        p = new Set(),
        B = null,
        m = null,
        R = {},
        u = [],
        F = null;
      rj(i) && g(i, void 0);
      let y = i;
      for (let G of i.ancestors()) {
        let S = y;
        ((y = G), De(G) && (F = G), g(G, S));
      }
      if (F) {
        let G = Ce(e);
        if (G) {
          R[ij] = Me;
          for (let [x, f] of G)
            ($.add(x),
              p.add(f.type),
              d(f.type === "enum"),
              f.type === "enum" && u.push({ variable: f, providerId: ij, sourceIdentifier: Me }));
          l.set(ij, G);
          let S = Nt(e);
          (d(S, "Control map should be defined because we received locale variables"),
            c.set(ij, S));
        }
      }
      A = {
        combined: l,
        combinedControls: c,
        ids: $,
        types: p,
        providerIds: a,
        idOfMutableVariableProvider: B,
        fallbackProvider: m,
        variableSourceIdentifiers: R,
        enums: u,
      };
    }
    return A;
  };
})();
var Xj = class {
  constructor(j, A) {
    this.componentLoader = j;
    this.nodeIgnoredInNodeIdIndex = A;
    E(this, "smartComponentNodes", new Set());
    E(this, "layoutTemplateNodes", new Set());
    E(this, "codeComponentNodes", new Set());
    E(this, "shaderNodes", new Set());
    E(this, "richTextNodes", new Set());
    E(this, "masterNodes", new Set());
    E(this, "navigationActionNodes", new Set());
    E(this, "setVariantActionNodes", new Set());
    E(this, "shapeContainerNodes", new Set());
    E(this, "formContainerNodes", new Set());
    E(this, "withDataIdentifierNodes", new Set());
  }
  reset() {
    (this.smartComponentNodes.clear(),
      this.layoutTemplateNodes.clear(),
      this.codeComponentNodes.clear(),
      this.shaderNodes.clear(),
      this.richTextNodes.clear(),
      this.masterNodes.clear(),
      this.navigationActionNodes.clear(),
      this.setVariantActionNodes.clear(),
      this.shapeContainerNodes.clear(),
      this.formContainerNodes.clear(),
      this.withDataIdentifierNodes.clear());
  }
  verifyNode(j, A, e) {
    d(j.size === A.size, "VerificationIndex node " + e + " is out of sync:", j.size, "!==", A.size);
    for (let r of j) d(A.has(r), "Tree index for " + e + " does not exists");
  }
  update(j) {
    (this.reset(),
      j.forEachNode((A) => {
        if (!this.nodeIgnoredInNodeIdIndex(A)) {
          if (
            (hj(A) && this.shapeContainerNodes.add(A.id),
            b(A) && this.codeComponentNodes.add(A.id),
            Ij(A) && this.shaderNodes.add(A.id),
            TA(A) && this.richTextNodes.add(A.id),
            W(A) && this.smartComponentNodes.add(A.id),
            xA(A) && this.layoutTemplateNodes.add(A.id),
            Mj(A, this.componentLoader) && this.navigationActionNodes.add(A.id),
            xj(A, this.componentLoader))
          ) {
            let e = LA.getReplicaForTemplateNode(j, A);
            (!e || (e && _j(e))) && this.setVariantActionNodes.add(A.id);
          }
          (wA(A) && this.formContainerNodes.add(A.id),
            NA(A) && this.withDataIdentifierNodes.add(A.id));
        }
      }));
  }
  verify({
    smartComponentNodeIds: j,
    layoutTemplateNodeIds: A,
    codeComponentNodeIds: e,
    shaderNodeIds: r,
    richTextNodeIds: o,
    navigationActionNodeIds: n,
    setVariantActionNodeIds: i,
    shapeContainerNodeIds: s,
    formContainerNodeIds: a,
    withDataIdentifierNodeIds: k,
  }) {
    (this.verifyNode(this.smartComponentNodes, j, "SmartComponents"),
      this.verifyNode(this.layoutTemplateNodes, A, "LayoutTemplates"),
      this.verifyNode(this.codeComponentNodes, e, "CodeComponents"),
      this.verifyNode(this.shaderNodes, r, "ShaderNodes"),
      this.verifyNode(this.richTextNodes, o, "RichTextNodes"),
      this.verifyNode(this.shapeContainerNodes, s, "shapeContainerNodes"),
      this.verifyNode(this.navigationActionNodes, n, "NavigationActionNodes"),
      this.verifyNode(this.setVariantActionNodes, i, "setVariantActionNodes"),
      this.verifyNode(this.formContainerNodes, a, "formContainerNodes"),
      this.verifyNode(this.withDataIdentifierNodes, k, "withDataIdentifierNodes"));
  }
};
var dn = 25,
  En = new (class extends Set {
    has(t) {
      return !0;
    }
  })();
function Qj(t) {
  return t ? nr(t) || or(t) || U(t) || gj(t) || J6(t) || H6(t) : !1;
}
var UA = class {
  constructor(j, A) {
    this.componentLoader = j;
    E(this, "tree");
    E(this, "reader");
    E(this, "verificationIndex");
    E(this, "onEngineChange");
    E(this, "layoutTemplateNodeIds", new Set());
    E(this, "smartComponentNodeIds", new Set());
    E(this, "codeComponentNodeIds", new Set());
    E(this, "shaderNodeIds", new Set());
    E(this, "richTextNodeIds", new Set());
    E(this, "navigationActionNodeIds", new Set());
    E(this, "setVariantActionNodeIds", new Set());
    E(this, "shapeContainerNodeIds", new Set());
    E(this, "searchComponentNodeIds", new Set());
    E(this, "formContainerNodeIds", new Set());
    E(this, "withDataIdentifierNodeIds", new Set());
    E(this, "deprecatedCodeComponentNodeIds", new Set());
    E(this, "foundExternalModuleIdentifiers", new Set());
    E(this, "externalModuleIdentifiersToLoad", new Set());
    E(this, "nodeIdIndex", new Map());
    E(this, "activeScopeLinkedNavigationNodes", []);
    E(this, "activeScopeLinkedVariantNodes", []);
    E(this, "activeScopeLinkedGestureNodes", []);
    E(this, "activeScopeCodeComponentArrows", new Set());
    E(this, "activeScopeId", "");
    E(this, "activeScopeGroundNodeIds", new Set());
    E(this, "_shouldChangeSmartComponents", !1);
    E(this, "_shouldChangeLayoutTemplates", !1);
    E(this, "shouldChangeCodeComponents", !1);
    E(this, "shouldChangeShaderNodes", !1);
    E(this, "shouldChangeRichTextNodes", !1);
    E(this, "shouldChangeNavigationActions", !1);
    E(this, "shouldChangeShapeContainerNodes", !1);
    E(this, "shouldChangeVariantActions", !1);
    E(this, "shouldChangeActiveScopeGroundNodes", !1);
    E(this, "shouldChangeWithDataIdentifierNodes", !1);
    E(this, "prototypeScreenLinksTree", null);
    E(this, "prototypeScreenLinks", {});
    ((this.verificationIndex = new Xj(j, Qj)),
      (this.reader = A.openNodeChangesReader()),
      this.reset(A.tree));
  }
  get shouldChangeSmartComponents() {
    return this._shouldChangeSmartComponents;
  }
  set shouldChangeSmartComponents(j) {
    ((this._shouldChangeSmartComponents = j),
      j && this.onEngineChange && this.onEngineChange("LocalSmartComponents"));
  }
  get shouldChangeLayoutTemplates() {
    return this._shouldChangeLayoutTemplates;
  }
  set shouldChangeLayoutTemplates(j) {
    ((this._shouldChangeLayoutTemplates = j),
      j && this.onEngineChange && this.onEngineChange("LocalLayoutTemplates"));
  }
  setOnEngineChange(j) {
    this.onEngineChange = j;
  }
  getScopeIdFor(j) {
    if (j) return this.nodeIdIndex.get(j)?.scope;
  }
  getGroundNodeIdFor(j) {
    if (j) return this.nodeIdIndex.get(j)?.ground;
  }
  getNodeIdToCurrentGroundNode(j, A) {
    if (!A) return;
    let e = this.nodeIdIndex.get(A);
    if (e && e.scope === this.activeScopeId) return j.get(e.ground) || void 0;
  }
  addNode(j, A) {
    if (
      (hj(A) && ((this.shouldChangeShapeContainerNodes = !0), this.shapeContainerNodeIds.add(A.id)),
      b(A) &&
        ((this.shouldChangeCodeComponents = !0),
        this.codeComponentNodeIds.add(A.id),
        this.handleSearchComponents(A),
        this.handleDeprecatedCodeComponent(A)),
      Ij(A) && ((this.shouldChangeShaderNodes = !0), this.shaderNodeIds.add(A.id)),
      wA(A) && this.formContainerNodeIds.add(A.id),
      TA(A) && ((this.shouldChangeRichTextNodes = !0), this.richTextNodeIds.add(A.id)),
      W(A) && ((this.shouldChangeSmartComponents = !0), this.smartComponentNodeIds.add(A.id)),
      xA(A) && ((this.shouldChangeLayoutTemplates = !0), this.layoutTemplateNodeIds.add(A.id)),
      Mj(A, this.componentLoader) &&
        ((this.shouldChangeNavigationActions = !0), this.navigationActionNodeIds.add(A.id)),
      xj(A, this.componentLoader))
    ) {
      let e = LA.getReplicaForTemplateNode(j, A);
      (!e || (e && _j(e))) &&
        ((this.shouldChangeVariantActions = !0), this.setVariantActionNodeIds.add(A.id));
    }
    (NA(A) &&
      ((this.shouldChangeWithDataIdentifierNodes = !0), this.withDataIdentifierNodeIds.add(A.id)),
      this.handleExternalDataSource(A));
  }
  handleSearchComponents(j) {
    let A = _(j.codeComponentIdentifier);
    A && V(A) && A.moduleId === c7 && this.searchComponentNodeIds.add(j.id);
  }
  handleDeprecatedCodeComponent(j) {
    W6(j) && (j.originalid || this.deprecatedCodeComponentNodeIds.add(j.id));
  }
  handleExternalDataSource(j) {
    let A;
    (M6(j) && (A = j.dataIdentifier),
      V6(j) && (A = j.componentIdentifier),
      A &&
        V(A) &&
        (this.foundExternalModuleIdentifiers.has(A) ||
          (this.foundExternalModuleIdentifiers.add(A),
          this.externalModuleIdentifiersToLoad.add(A))));
  }
  getAndClearExternalDataSourcesToLoad() {
    if (this.externalModuleIdentifiersToLoad.size === 0) return null;
    let j = Array.from(this.externalModuleIdentifiersToLoad);
    return (this.externalModuleIdentifiersToLoad.clear(), j);
  }
  deleteNode(j) {
    (this.smartComponentNodeIds.delete(j) && (this.shouldChangeSmartComponents = !0),
      this.layoutTemplateNodeIds.delete(j) && (this.shouldChangeLayoutTemplates = !0),
      this.codeComponentNodeIds.delete(j) && (this.shouldChangeCodeComponents = !0),
      this.shaderNodeIds.delete(j) && (this.shouldChangeShaderNodes = !0),
      this.richTextNodeIds.delete(j) && (this.shouldChangeRichTextNodes = !0),
      this.navigationActionNodeIds.delete(j) && (this.shouldChangeNavigationActions = !0),
      this.setVariantActionNodeIds.delete(j) && (this.shouldChangeVariantActions = !0),
      this.shapeContainerNodeIds.delete(j) && (this.shouldChangeShapeContainerNodes = !0),
      this.withDataIdentifierNodeIds.delete(j) && (this.shouldChangeWithDataIdentifierNodes = !0),
      this.activeScopeGroundNodeIds.delete(j) && (this.shouldChangeActiveScopeGroundNodes = !0),
      this.formContainerNodeIds.delete(j),
      this.nodeIdIndex.delete(j),
      this.searchComponentNodeIds.delete(j),
      this.deprecatedCodeComponentNodeIds.delete(j));
  }
  reset(j) {
    (this.smartComponentNodeIds.clear(),
      this.layoutTemplateNodeIds.clear(),
      this.codeComponentNodeIds.clear(),
      this.shaderNodeIds.clear(),
      this.richTextNodeIds.clear(),
      this.navigationActionNodeIds.clear(),
      this.setVariantActionNodeIds.clear(),
      this.shapeContainerNodeIds.clear(),
      this.activeScopeGroundNodeIds.clear(),
      this.nodeIdIndex.clear(),
      this.searchComponentNodeIds.clear(),
      this.deprecatedCodeComponentNodeIds.clear(),
      this.formContainerNodeIds.clear(),
      this.withDataIdentifierNodeIds.clear(),
      (this.tree = j),
      j.beginAllowPartialScopeAccess(),
      j.forEachNode((A) => {
        Qj(A) || (this.addNode(j, A), this.updateNodeIdIndex(j, A));
      }),
      j.endAllowPartialScopeAccess(),
      this.reader.read(),
      this.setAllChangeFlags());
  }
  setAllChangeFlags() {
    ((this.shouldChangeSmartComponents = !0),
      (this.shouldChangeLayoutTemplates = !0),
      (this.shouldChangeRichTextNodes = !0),
      (this.shouldChangeCodeComponents = !0),
      (this.shouldChangeShaderNodes = !0),
      (this.shouldChangeNavigationActions = !0),
      (this.shouldChangeShapeContainerNodes = !0),
      (this.shouldChangeVariantActions = !0),
      (this.shouldChangeActiveScopeGroundNodes = !0),
      (this.shouldChangeWithDataIdentifierNodes = !0));
  }
  getNodes(j) {
    let A = [];
    for (let e of j) {
      let r = this.tree.get(e);
      r && A.push(r);
    }
    return A;
  }
  getNodesForActiveScope(j) {
    let A = [];
    for (let e of j) {
      if (this.getScopeIdFor(e) !== this.activeScopeId) continue;
      let r = this.tree.get(e);
      r && A.push(r);
    }
    return A;
  }
  processActiveScope(j, A) {
    if (this.activeScopeId === A || A.length === 0) return;
    ((this.activeScopeId = A),
      this.activeScopeGroundNodeIds.clear(),
      (this.shouldChangeActiveScopeGroundNodes = !0),
      (this.shouldChangeShapeContainerNodes = !0));
    let e = j.get(this.activeScopeId);
    if (!(!oA(e) || !e.isLoaded()))
      for (let r of e.loaded.children) this.activeScopeGroundNodeIds.add(r.id);
  }
  processReset(j) {
    (this.processActiveScope(j, ""), this.reset(j), this.verify());
  }
  process(j, A) {
    if ((this.processActiveScope(j, A), this.tree.lineage !== j.lineage)) {
      (this.reset(j), this.verify());
      return;
    }
    this.tree = j;
    let e = [],
      r = [];
    for (let [o, n] of this.reader.read()) {
      let i = j.get(o);
      if (!Qj(i) && (this.deleteNode(o), i && (this.addNode(j, i), e.push(i), !oA(i)))) {
        for (let s of n)
          if (!s.added && s.to.parentid) {
            r.push(i);
            break;
          }
      }
    }
    for (let o of e) this.updateNodeIdIndex(j, o);
    for (let o of r) this.updateNodeIdIndexForMovedNode(o);
    this.verify();
  }
  updateNodeIdIndexForMovedNode(j) {
    if (!j.children) return;
    let A = this.nodeIdIndex.get(j.id);
    for (let e of j.children) for (let r of e.walk()) this.nodeIdIndex.set(r.id, A);
  }
  updateNodeIdIndex(j, A) {
    let e = this.nodeIdIndex.get(A.id);
    if (e) return e;
    if (oA(A)) {
      let n = { scope: A.id, ground: A.id };
      (this.nodeIdIndex.set(A.id, n),
        A.id === this.activeScopeId && (this.shouldChangeActiveScopeGroundNodes = !0));
      return;
    }
    let r = j.get(A.parentid);
    if (!r) return;
    if (oA(r) || q6(r)) {
      let n = { scope: r.id, ground: A.id };
      return (
        this.nodeIdIndex.set(A.id, n),
        fe(A) ||
          (r.id === this.activeScopeId &&
            ((this.shouldChangeActiveScopeGroundNodes = !0),
            this.activeScopeGroundNodeIds.add(A.id))),
        n
      );
    }
    let o = this.updateNodeIdIndex(j, r);
    if (o) return (this.nodeIdIndex.set(A.id, o), o);
  }
  updateCodeComponents() {
    this.tree.beginAllowPartialScopeAccess();
    for (let j of this.codeComponentNodeIds) {
      (this.navigationActionNodeIds.delete(j) && (this.shouldChangeNavigationActions = !0),
        this.setVariantActionNodeIds.delete(j) && (this.shouldChangeVariantActions = !0));
      let A = this.tree.get(j);
      A && this.addNode(this.tree, A);
    }
    this.tree.endAllowPartialScopeAccess();
  }
  verify() {}
  verifyScope(j, A) {
    let e = 0;
    for (let r of A.walk()) {
      if (Qj(r)) continue;
      e += 1;
      let o = this.nodeIdIndex.get(r.id);
      (d(o, "id", r.id, "does not exist in index"),
        d(o.scope === j.id, r.id, "scope is:", o.scope, "should be:", j.id),
        d(o.ground === A.id, r.id, "ground is:", o.ground, "should be:", A.id));
    }
    return e;
  }
  updateConnectedScreens(j, A, e) {
    let r = new Set();
    if (e) r = En;
    else {
      for (let a of A) {
        let k = this.getNodeIdToCurrentGroundNode(j, a);
        k && r.add(k);
      }
      r.size > dn && r.clear();
    }
    let o = this.getLinkedNavigationNodes(j, r);
    dA(this.activeScopeLinkedNavigationNodes, o) || (this.activeScopeLinkedNavigationNodes = o);
    let n = this.getLinkedVariantNodes(j, A, r, e);
    dA(this.activeScopeLinkedVariantNodes, n) || (this.activeScopeLinkedVariantNodes = n);
    let i = this.getLinkedGestureNodes(j, r);
    dA(this.activeScopeLinkedGestureNodes, i) || (this.activeScopeLinkedGestureNodes = i);
    let s = this.getCodeComponentArrows(j, r);
    Oj(this.activeScopeCodeComponentArrows, s) || (this.activeScopeCodeComponentArrows = s);
  }
  getLinkedVariantNodes(j, A, e, r) {
    let o = [],
      n = new Set(),
      i = A[0];
    if (!r && (A.length !== 1 || !i)) return o;
    for (let s of this.setVariantActionNodeIds) {
      if (this.nodeIdIndex.get(s)?.scope !== this.activeScopeId) continue;
      let a = this.getNodeIdToCurrentGroundNode(j, s);
      if (!a || (!r && i !== a.id && i !== s)) continue;
      let k = j.get(s);
      if (!k) continue;
      (n.clear(),
        hr(j, k, this.componentLoader).forEach((c) => {
          if (n.has(c)) return;
          let $ = j.getNode(c);
          if (!$ || !RA($)) return;
          let p = this.getNodeIdToCurrentGroundNode(j, $.id);
          p === $ &&
            p !== a &&
            p.parentid === a.parentid &&
            (e.has(a) || e.has(p)) &&
            (n.add($.id), o.push({ fromNode: k, toNode: $ }));
        }));
    }
    return o;
  }
  getLinkedNavigationNodes(j, A) {
    let e = [],
      r = new Set();
    for (let o of this.navigationActionNodeIds) {
      if (this.nodeIdIndex.get(o)?.scope !== this.activeScopeId) continue;
      let n = this.getNodeIdToCurrentGroundNode(j, o);
      if (!n) continue;
      r.clear();
      let i = j.get(o);
      Ne(i, this.componentLoader).forEach((a) => {
        if (
          !a.controls ||
          !a.controls.target ||
          !a.controls.target.value ||
          !a.controls.type ||
          a.controls.type.value !== "next"
        )
          return;
        let k = j.getNode(a.controls.target.value);
        if (!k || r.has(k.id)) return;
        let l = this.getNodeIdToCurrentGroundNode(j, k.id);
        l &&
          l === k &&
          l !== n &&
          l.parentid === n.parentid &&
          ((A.has(n) || A.has(l)) && e.push({ fromNode: i, toNode: k }), r.add(k.id));
      });
    }
    return e;
  }
  getLinkedGestureNodes(j, A) {
    let e = [],
      r = j.get(this.activeScopeId);
    for (let o of r?.children ?? []) {
      if (!Z6(o) || !o.replicaInfo.inheritsFrom) continue;
      let i = j.getNode(o.replicaInfo.inheritsFrom);
      if (!i || !RA(i)) continue;
      let s = this.getNodeIdToCurrentGroundNode(j, o.id),
        a = this.getNodeIdToCurrentGroundNode(j, i.id);
      (A.has(a) || A.has(s)) && e.push({ fromNode: o, toNode: i });
    }
    return e;
  }
  getCodeComponentArrows(j, A) {
    let e = new Set(),
      r = j.get(this.activeScopeId);
    if (!oA(r)) return e;
    for (let o of this.codeComponentNodeIds) {
      if (this.nodeIdIndex.get(o)?.scope !== this.activeScopeId) continue;
      let n = this.getNodeIdToCurrentGroundNode(j, o);
      if (!n) continue;
      let i = j.get(o);
      if (!i || (d(b(i), "bug"), i.slotsAreChildNodes)) continue;
      if (A.has(n)) {
        e.add(i);
        continue;
      }
      let a = this.componentLoader.reactComponentForIdentifier(i.codeComponentIdentifier);
      if (!a) continue;
      let k = i.getControlProps(a.properties);
      Object.values(k).some((c) => {
        if (c.type !== "slot") return !1;
        for (let $ of c.value) {
          let p = j.get($.reference);
          if (!Fr(r.id, p)) continue;
          let B = this.getNodeIdToCurrentGroundNode(j, p.id);
          if (B && B !== n && B.parentid === n.parentid && A.has(B)) return !0;
        }
        return !1;
      }) && e.add(i);
    }
    return e;
  }
  getPrototypeScreenLinks(j, A) {
    let e = this.getScopeIdFor(A);
    if (!e) return [];
    this.prototypeScreenLinksTree !== j &&
      ((this.prototypeScreenLinksTree = j), (this.prototypeScreenLinks = {}));
    let r = this.prototypeScreenLinks[e];
    if (r) return r;
    let o = [],
      n = new Set();
    for (let i of this.navigationActionNodeIds) {
      let s = this.nodeIdIndex.get(i);
      if (!s || s.scope !== e) continue;
      let a = j.get(s.ground);
      if (!a) continue;
      let k = j.get(i),
        l = Ne(k, this.componentLoader);
      (n.clear(),
        l.forEach((c) => {
          if (
            !c.controls ||
            !c.controls.target ||
            !c.controls.target.value ||
            !c.controls.type ||
            c.controls.type.value !== "next"
          )
            return;
          let $ = j.getNode(c.controls.target.value);
          if (!$ || n.has($.id)) return;
          let p = j.get(this.getGroundNodeIdFor($.id));
          p &&
            p === $ &&
            p !== a &&
            p.parentid === a.parentid &&
            (n.add($.id), o.push({ fromNode: a.id, toNode: $.id }));
        }));
    }
    for (let i of this.codeComponentNodeIds) {
      let s = this.nodeIdIndex.get(i);
      if (!s || s.scope !== e) continue;
      let a = j.get(s.ground);
      if (!a) continue;
      let k = j.get(i);
      if (!k || (d(b(k), "bug"), rA(k))) continue;
      let l = Rr(k, this.componentLoader);
      if (l.length !== 0)
        for (let c of l) {
          let p = k.getRawControlProp(c)?.value;
          !p ||
            !Array.isArray(p) ||
            p.forEach((B) => {
              if (typeof B.value != "string") return;
              let m = j.get(B.value);
              m && (RA(m) || o.push({ fromNode: a.id, toNode: m.id }));
            });
        }
    }
    return ((this.prototypeScreenLinks[e] = o), o);
  }
  getShapeContainerNodes(j) {
    let A = [];
    for (let e of this.shapeContainerNodeIds) {
      if (this.getScopeIdFor(e) !== j.id) continue;
      let r = this.tree.get(e);
      r && A.push(r);
    }
    return A;
  }
};
var Ye = cA("treeStore");
function pn(t) {
  return yA(t) && typeof t.then == "function";
}
function mn(t) {
  return yA(t) && typeof t.then == "function";
}
function un() {
  if (!("treeInitData" in window) || !yA(window.treeInitData)) return;
  let t = window.treeInitData,
    {
      mode: j,
      version: A,
      file: e,
      prefetchPromise: r,
      prefetchSyncPromise: o,
      syncURL: n,
      socketURL: i,
      isMPSSocket: s,
    } = t;
  delete window.treeInitData;
  let a = s === !0 ? { isMPSSocket: !0 } : {},
    k = v(n) ? { syncURL: n } : {},
    l = mt(j) ? { mode: j } : {};
  if (r && mn(r)) {
    if (!v(i) || !eA(A) || !v(e)) {
      let c = Object.keys(t);
      Ye.error("Expected socketURL, version, and file in window.treeInitData but got:", c);
      return;
    }
    return { version: A, file: e, prefetchPromise: r, socketURL: i, ...l, ...k, ...a };
  } else if (o && pn(o)) {
    if (!v(i)) {
      let c = Object.keys(t);
      Ye.error("Expected socketURL in window.treeInitData but got:", c);
      return;
    }
    return { prefetchSyncPromise: o, socketURL: i, ...l, ...k, ...a };
  }
  if ((Ye.warn("Not using prefetch."), v(i))) return { socketURL: i, ...l, ...k, ...a };
}
var cj = class {
  constructor(j, A, e, r, o, n, i, s, a) {
    this.addChanges = j;
    this.componentLoader = A;
    this.persistedUserDefaults = e;
    this.chromeStore = r;
    this.guidesStore = o;
    this.undoRedoManager = n;
    this.layoutCache = i;
    this.getEffectCacheUpdate = s;
    E(this, "oldSelection", []);
    E(this, "oldSelectedGradientStopId");
    E(this, "oldScopeId", null);
    E(this, "projectId");
    E(this, "pendingCacheUpdateNodeIds", new Set());
    E(this, "initData", un());
    E(this, "tree");
    E(this, "timeline");
    E(this, "highlight");
    E(this, "collapseOverrides", new Map());
    E(this, "showAllArrows", !1);
    E(this, "isDocumentEmpty", !0);
    E(this, "visibleGroundNodesInActiveScope", []);
    E(this, "groundNodesInViewport", new Set());
    E(this, "_codeComponents", null);
    E(this, "_richTextNodes", null);
    E(this, "_shaderNodes", null);
    E(this, "_localSmartComponentNameByIdentifier", null);
    E(this, "_localLayoutTemplatesNameByIdentifier", null);
    E(this, "activeScopeShapeContainerNodes", []);
    E(this, "activeScopeCodeComponentArrows", new Set());
    E(this, "activeScopeLinkedNavigationNodes", []);
    E(this, "activeScopeLinkedVariantNodes", []);
    E(this, "activeScopeLinkedGestureNodes", []);
    E(this, "allNodesShareVariableContext", !1);
    E(this, "variableProviderInfo", null);
    E(this, "crashSoon", !1);
    E(this, "sandboxRepeaterData", new r7("editor"));
    E(this, "sandboxVisibility", new o7("editor"));
    E(this, "domLayoutUpdate", 0);
    E(this, "treeIndex");
    E(this, "initialTree");
    let k = window.bootstrap?.project?.id;
    if (k) this.projectId = k;
    else if (!(SA.isAutomation || SA.isTest)) throw Error("Can't find project id");
    let l = ur(a?.mode ?? this.initData?.mode);
    ((this.tree = Ir.createWithDefaultWebPage({ ...a, mode: l })),
      (this.initialTree = this.tree),
      this.tree.setService("treeQueryContext", this),
      (this.timeline = this.createTimeline(l)),
      (this.treeIndex = new UA(A, this.timeline)),
      this.treeIndex.setOnEngineChange((c) => this.addChanges(c)),
      this.assertTimelineMatchesMode(),
      this.setTree(this.tree),
      this.setShowAllArrows(e.displayAllLinks, []));
  }
  get codeComponents() {
    return (
      this.treeIndex.shouldChangeCodeComponents &&
        ((this.treeIndex.shouldChangeCodeComponents = !1),
        (this._codeComponents = this.treeIndex.getNodes(this.treeIndex.codeComponentNodeIds))),
      this._codeComponents ?? []
    );
  }
  get shaderNodes() {
    return (
      this.treeIndex.shouldChangeShaderNodes &&
        ((this.treeIndex.shouldChangeShaderNodes = !1),
        (this._shaderNodes = this.treeIndex.getNodes(this.treeIndex.shaderNodeIds))),
      this._shaderNodes ?? []
    );
  }
  get richTextNodes() {
    return (
      this.treeIndex.shouldChangeRichTextNodes &&
        ((this.treeIndex.shouldChangeRichTextNodes = !1),
        (this._richTextNodes = this.treeIndex.getNodes(this.treeIndex.richTextNodeIds))),
      this._richTextNodes ?? []
    );
  }
  get formContainerNodeIds() {
    return this.treeIndex.formContainerNodeIds;
  }
  get withDataIdentifierNodeIds() {
    return this.treeIndex.withDataIdentifierNodeIds;
  }
  get localSmartComponentNameByIdentifier() {
    if (this.treeIndex.shouldChangeSmartComponents) {
      this.treeIndex.shouldChangeSmartComponents = !1;
      let j = {};
      for (let e of this.treeIndex.smartComponentNodeIds) {
        let r = this.tree.get(e);
        r && (j[r.instanceIdentifier] = r.resolveValue("name"));
      }
      let A = this._localSmartComponentNameByIdentifier ?? {};
      Te(A, j) || (this._localSmartComponentNameByIdentifier = j);
    }
    return this._localSmartComponentNameByIdentifier ?? {};
  }
  get localLayoutTemplatesNameByIdentifier() {
    if (this.treeIndex.shouldChangeLayoutTemplates) {
      this.treeIndex.shouldChangeLayoutTemplates = !1;
      let j = {};
      for (let e of this.treeIndex.layoutTemplateNodeIds) {
        let r = this.tree.get(e);
        r && (j[r.instanceIdentifier] = r.resolveValue("name"));
      }
      let A = this._localLayoutTemplatesNameByIdentifier ?? {};
      Te(A, j) || (this._localLayoutTemplatesNameByIdentifier = j);
    }
    return this._localLayoutTemplatesNameByIdentifier ?? {};
  }
  get mode() {
    return n7(this);
  }
  set mode(j) {
    i7(this, j, {
      initialTree: this.initialTree,
      createTimeline: (A) => this.createTimeline(A),
      resetTimeline: !0,
    }) &&
      ((this.treeIndex = new UA(this.componentLoader, this.timeline)),
      this.treeIndex.setOnEngineChange((A) => this.addChanges(A)),
      this.assertTimelineMatchesMode(),
      this.addChanges("TreeStore"));
  }
  createTimeline(j) {
    let { projectId: A } = this,
      e = A ? (r, o) => a7().setSeqWatermark(A, r, o) : void 0;
    return j === "crdt"
      ? new k7(this.tree, this.componentLoader, {
          afterLocalWrite: e,
          shouldAutoBranch: () => this.persistedUserDefaults.autoBranching,
        })
      : new s7(this.tree, this.componentLoader);
  }
  assertCrdtTimeline() {
    (d(this.mode === "crdt", "CRDT timeline requested for non-CRDT tree mode"),
      d(Ve(this.timeline), "CRDT tree mode requires a CRDT timeline"));
  }
  get crdtTimeline() {
    return (this.assertCrdtTimeline(), this.timeline);
  }
  assertTimelineMatchesMode() {
    let j = Ve(this.timeline);
    d(this.mode === "crdt" ? j : !j, "Tree mode and timeline type are out of sync");
  }
  prepareDetachedRootReplacement(j) {
    (this.timeline.reset(j),
      this.mode === "crdt" && this.resetCrdtStoreToTree(j),
      (this.treeIndex = new UA(this.componentLoader, this.timeline)),
      this.treeIndex.setOnEngineChange((A) => this.addChanges(A)));
  }
  resetCrdtStoreToTree(j) {
    let { store: A } = this.crdtTimeline;
    (A.reset(),
      A.setObject(j.root.id, j.root.toJS()),
      A.setRoot(j.root.id),
      A.setVersion(j.serializationVersion));
  }
  ensureMainCrdtStoreInitialized() {
    this.mode === "crdt" &&
      this.isOnMainBranch() &&
      (this.crdtTimeline.store.length > 0 || this.resetCrdtStoreToTree(this.tree));
  }
  getLoadedIndexes() {
    let j = this.tree.getService("loader");
    return (
      d(!j, "Some scopes are still loading:", j?.numberOfScopesToLoad(), "scopes left to load"),
      this.getPartialIndexes()
    );
  }
  getPartialIndexes() {
    return this;
  }
  getDataTree() {
    return this.timeline.getOrCreateDataTree(this);
  }
  resetDataTree() {
    this.timeline.resetDataTree();
  }
  getDataTreeOrLoadedTree() {
    let j = this.getDataTree();
    return j
      ? j.getCanvasTreeProxy()
      : (d(!this.tree.getService("loader"), "Tree must be fully loaded"), this.tree);
  }
  getDataTreeOrPartialTree() {
    let j = this.getDataTree();
    return j ? j.getCanvasTreeProxy() : this.tree;
  }
  query() {
    return new mr(this);
  }
  updateActiveScopeShapeContainerNodes() {
    if (!this.treeIndex.shouldChangeShapeContainerNodes) return;
    this.treeIndex.shouldChangeShapeContainerNodes = !1;
    let j = this.treeIndex.getNodesForActiveScope(this.treeIndex.shapeContainerNodeIds);
    dA(this.activeScopeShapeContainerNodes, j) ||
      ((this.activeScopeShapeContainerNodes = j), this.addChanges("ShapeContainerNodes"));
  }
  updateGroundNodesList() {
    if (!this.treeIndex.shouldChangeActiveScopeGroundNodes) return;
    this.treeIndex.shouldChangeActiveScopeGroundNodes = !1;
    let j = this.treeIndex.getNodes(this.treeIndex.activeScopeGroundNodeIds).filter(Pe);
    if (
      (dA(this.visibleGroundNodesInActiveScope, j) || (this.visibleGroundNodesInActiveScope = j),
      j.length > 0)
    ) {
      this.isDocumentEmpty = !1;
      return;
    } else
      for (let A of this.tree.root.children)
        if (!(oA(A) && !A.isValid()) && A.children) {
          for (let e of A.children)
            if (Pe(e)) {
              this.isDocumentEmpty = !1;
              return;
            }
        }
    this.isDocumentEmpty = !0;
  }
  get initialSnapshotVersion() {
    return this.initData?.version;
  }
  get isNewDocument() {
    let j = this.initialSnapshotVersion;
    return eA(j) && j <= 2;
  }
  get initialFileURL() {
    return this.initData?.file;
  }
  get initialSocketURL() {
    return this.initData?.socketURL;
  }
  get isMPSSocket() {
    return this.initData?.isMPSSocket === !0;
  }
  get serverHistoryURL() {
    let j = `/projects/${this.projectId}/history`;
    return this.isMPSSocket ? l7(j) : `/internal${j}`;
  }
  getSocketURL(j) {
    return this.initialSocketURL ? this.initialSocketURL : j;
  }
  getDocumentURL(j) {
    return j || this.initialFileURL;
  }
  setTree(j, A) {
    ((this.tree = j),
      this.tree.setService("treeQueryContext", this),
      ut(this.tree, this.mode),
      (j.inEditor = !0),
      this.assertTimelineMatchesMode(),
      A?.isBranchSwitch ? this.crdtTimeline.replaceTree(j) : this.timeline.reset(j),
      (this.highlight = Se),
      (this.collapseOverrides = new Map()),
      this.tree.resetDraftNodesAndCache(),
      A?.isBranchSwitch || this.undoRedoManager.clearUndoStack(),
      this.addChanges("Tree"),
      this.addChanges("ColorStyleTokens"),
      this.addChanges("TextStylePresets"),
      d(j.root.children.at(0), "Tree must not be empty"),
      this.treeIndex.processReset(j),
      this.updateGroundNodesList());
  }
  hasUncommittedChanges() {
    return this.tree.hasUncommittedChanges() || !!this.getDataTree()?.hasUncommittedChanges();
  }
  commit() {
    this.ensureMainCrdtStoreInitialized();
    let j = this.timeline.commitLocalTree();
    return (
      this.pendingCacheUpdateNodeIds.size > 0 &&
        (this.updateTreeCacheForNodes(
          this.pendingCacheUpdateNodeIds,
          this.componentLoader.activeBundleHash,
          j.tree
        ),
        this.pendingCacheUpdateNodeIds.clear()),
      j
    );
  }
  syncNodeCacheWithDOMLayout(j) {
    Hr(this.tree, this.layoutCache, j).size > 0 &&
      (this.domLayoutUpdate++, this.addChanges("TreeDOMLayoutSync"));
  }
  clearStaleDOMLayoutCacheForOutOfScopeNodes(j) {
    if (j.size !== 0)
      for (let A of j) {
        let e = this.tree.get(A);
        !e || !at(e) || e.invalidateDOMRect();
      }
  }
  updateTreeCacheForNodes(j, A, e = this.tree) {
    if (j.size === 0) return;
    if (e.hasUncommittedChanges()) {
      for (let o of j) this.pendingCacheUpdateNodeIds.add(o);
      return;
    }
    let r = L7(e, j);
    (this.addChanges("TreeStore"),
      Jr(
        e,
        this.layoutCache,
        this.sandboxRepeaterData,
        this.sandboxVisibility,
        this.componentLoader,
        A,
        this.chromeStore.canvasLocaleId,
        this.getEffectCacheUpdate(),
        r
      ));
  }
  updateConnectedScreens(j) {
    (this.treeIndex.updateConnectedScreens(this.tree, j, this.showAllArrows),
      (this.activeScopeLinkedNavigationNodes !== this.treeIndex.activeScopeLinkedNavigationNodes ||
        this.activeScopeLinkedVariantNodes !== this.treeIndex.activeScopeLinkedVariantNodes ||
        this.activeScopeLinkedGestureNodes !== this.treeIndex.activeScopeLinkedGestureNodes ||
        this.activeScopeCodeComponentArrows !== this.treeIndex.activeScopeCodeComponentArrows) &&
        ((this.activeScopeLinkedNavigationNodes = this.treeIndex.activeScopeLinkedNavigationNodes),
        (this.activeScopeLinkedVariantNodes = this.treeIndex.activeScopeLinkedVariantNodes),
        (this.activeScopeLinkedGestureNodes = this.treeIndex.activeScopeLinkedGestureNodes),
        (this.activeScopeCodeComponentArrows = this.treeIndex.activeScopeCodeComponentArrows),
        this.addChanges("Arrows")));
  }
  updateVariableProviderInfo(j) {
    let A = this.tree.getNodes(j),
      e = A.map((s) =>
        rj(s)
          ? s.cache.getUnsafeCombinedVariableValueMap()
          : s.cache.getUnsafeInheritedCombinedVariableValueMap()
      ),
      r = A.every((s) => s.supportsDynamicValues()),
      o = new Set(e).size === 1,
      n = A[0];
    this.allNodesShareVariableContext = o;
    let i = this.variableProviderInfo;
    ((this.variableProviderInfo = w7(this.tree, this.componentLoader, o, r, n)),
      i !== this.variableProviderInfo && this.addChanges("VariableProviderInfo"));
  }
  updateCodeComponents() {
    this.treeIndex.updateCodeComponents();
  }
  updateGroundNodesInViewport(j) {
    let A = new Set(),
      e = this.groundNodesInViewport,
      r = this.visibleGroundNodesInActiveScope,
      o = !0;
    for (let n of r) {
      let i = n.id;
      C.intersects(wt(n), j) && (A.add(i), o && !e.has(i) && (o = !1));
    }
    (!o || A.size !== e.size) && (this.groundNodesInViewport = A);
  }
  selectionDidChange(j) {
    let A = this.tree;
    for (let r in this.collapseOverrides) A.has(r) || this.deleteCollapseOverride(r);
    if (
      (A.has(this.highlight) || (this.highlight = Se),
      j.length > 0 && this.collapseOverrides.size > 0)
    ) {
      let r = br(j, this.oldSelection),
        o = [];
      (r.forEach((n) => {
        o.push(...A.getAncestors(n));
      }),
        o.forEach((n) => {
          this.deleteCollapseOverride(n);
        }));
    }
    let e = this.oldSelection.length !== j.length;
    for (let r of j) {
      let o = this.tree.getNode(r);
      o && (o.cache.selected || ((e = !0), (o.cache.selected = !0)));
    }
    if (e)
      for (let r of this.oldSelection) {
        if (j.includes(r)) continue;
        let o = A.getNode(r);
        o && (o.cache.selected = !1);
      }
  }
  processLatestChangesForUndo() {
    let j = this.timeline.getLatestChangesForUndo();
    if (!j) return [];
    let A = [];
    for (let e of j) {
      e.ignoreInUndo || A.push(e);
      let r = this.tree.get(e.id);
      ((L6(r) || w6(r)) && this.addChanges("ColorStyleTokens"),
        (X6(r) || U6(r)) && this.addChanges("TextStylePresets"));
      let o = this.persistedUserDefaults.frameBackground;
      e.removed === "ColorStyleTokenNode" &&
        o &&
        o.includes(e.id) &&
        (this.persistedUserDefaults.frameBackground = null);
    }
    return A;
  }
  discardUndoRedoEntriesForNode(j) {
    this.undoRedoManager.removeEntriesForNode(j);
  }
  postProcess(j, A, e, r, o, n) {
    (d(!j.hasUncommittedChanges(), "tree must be clean"),
      d(j.isLatest(), "tree must be latest"),
      j.lineage.setActiveChangeset(void 0),
      this.resetDataTree());
    let i = this.tree !== j,
      s = !1;
    if (
      i &&
      ((s = j.get(r) !== this.tree.get(r)),
      (this.tree = j),
      this.addChanges("Tree"),
      this.crashSoon)
    )
      throw ((this.crashSoon = !1), Error("TreeStore CrashTest"));
    this.tree.beginAllowPartialScopeAccess();
    let a = this.oldSelection !== A;
    if ((a && this.selectionDidChange(A), i)) {
      let k = this.processLatestChangesForUndo();
      if (
        (e &&
          k.length > 0 &&
          this.undoRedoManager.addUndoEntry({
            kind: "treeEdit",
            changes: k,
            selection: this.oldSelection,
            scopeId: this.oldScopeId ?? r,
            localeId: o,
            selectedGradientStopId: this.oldSelectedGradientStopId,
          }),
        this.addChanges("TreeStore"),
        k.length > 0)
      ) {
        let l = this.layoutCache.pruneAfterTreeChanges(k, this.tree, r);
        this.clearStaleDOMLayoutCacheForOutOfScopeNodes(l);
      }
      this.treeIndex.process(j, r);
    } else this.treeIndex.processActiveScope(j, r);
    (this.updateActiveScopeShapeContainerNodes(),
      this.updateGroundNodesList(),
      (s || a) && this.updateConnectedScreens(A),
      (i || a) && this.updateVariableProviderInfo(A),
      (this.oldSelection = A),
      (this.oldScopeId = r),
      (this.oldSelectedGradientStopId = n),
      this.undoRedoManager.processScheduledEndUndoGroup({
        kind: "treeEdit",
        selection: A,
        scopeId: r,
        localeId: o,
        selectedGradientStopId: n,
      }),
      this.tree.endAllowPartialScopeAccess());
  }
  setHighlight(j) {
    this.highlight !== j &&
      ((this.highlight = j),
      j && (this.guidesStore.highlighted = void 0),
      this.addChanges("TreeStore", "Highlight"));
  }
  setCollapseOverride(j, A) {
    let e = new Map(this.collapseOverrides);
    (e.set(j, A), (this.collapseOverrides = e));
  }
  deleteCollapseOverride(j) {
    if (!this.collapseOverrides.has(j)) return;
    let A = new Map(this.collapseOverrides);
    (A.delete(j), (this.collapseOverrides = A));
  }
  setCollapseMode(j, A, e) {
    if (e) this.setCollapseOverride(j, A);
    else if (this.collapseOverrides.has(j)) this.deleteCollapseOverride(j);
    else return;
    this.addChanges("TreeStore");
  }
  collapseLayers(j) {
    let A = new Map();
    for (let e of j) {
      let r = this.tree.get(e);
      if (r && r.children) {
        for (let o of r.children) A.set(o.id, !0);
        A.set(e, !0);
      }
    }
    ((this.collapseOverrides = A), this.addChanges("TreeStore"));
  }
  resetCollapseOverrides() {
    ((this.collapseOverrides = new Map()), this.addChanges("TreeStore"));
  }
  setShowAllArrows(j, A) {
    this.showAllArrows !== j &&
      ((this.showAllArrows = j), this.updateConnectedScreens(A), this.addChanges("TreeStore"));
  }
  resetTreeWithToken(j) {
    let A = this.undoRedoManager.resetTreeWithToken(this.timeline, j);
    if (!A) return;
    let { selection: e, changedGroundNodes: r } = A;
    for (let o of r) this.unsafeUpdateNodeCache(o, !0, !1);
    return e;
  }
  unsafeUpdateNodeCache(j, A, e) {
    j = j.draftOrCurrent();
    let r = nj(j) && !j.cache.overlayVisible;
    A &&= jA.isOn("visibilityFromSandbox")
      ? this.sandboxVisibility.getVisible(j.id)
      : j.isVisible();
    let o = A && !r;
    ((j.cache.visible = o),
      (e ||= RA(j)),
      (j.cache.insideVariant = e),
      j.children?.forEach((n) => this.unsafeUpdateNodeCache(n, o, e)));
  }
  get remoteTreeVersion() {
    return this.timeline.remoteTreeVersion;
  }
  debugComputeLazyIndexes() {
    let j = {
      codeComponents: this.codeComponents,
      richTextNodes: this.richTextNodes,
      shaderNodes: this.shaderNodes,
      formContainerNodeIds: this.formContainerNodeIds,
      withDataIdentifierNodeIds: this.withDataIdentifierNodeIds,
      localSmartComponentNameByIdentifier: this.localSmartComponentNameByIdentifier,
      localLayoutTemplatesNameByIdentifier: this.localLayoutTemplatesNameByIdentifier,
      activeScopeShapeContainerNodes: this.activeScopeShapeContainerNodes,
      activeScopeCodeComponentArrows: this.activeScopeCodeComponentArrows,
      activeScopeLinkedNavigationNodes: this.activeScopeLinkedNavigationNodes,
      activeScopeLinkedVariantNodes: this.activeScopeLinkedVariantNodes,
      activeScopeLinkedGestureNodes: this.activeScopeLinkedGestureNodes,
    };
  }
  get branchId() {
    return this.mode === "crdt" ? this.crdtTimeline.branches.activeBranchId : Le;
  }
  isOnMainBranch() {
    return this.branchId === Le;
  }
};
function J7(t, j) {
  if (!t || t.length === 0) return j?.placeholder ?? "";
  if (t.length === 1) return t[0] && t[0] !== "" ? t[0] : (j?.placeholder ?? "");
  let A = [...t],
    e = "";
  if (j?.max && j.max < t.length) {
    let r = t.length - j.max;
    ((A = A.slice(0, j.max)), (e = `${r} ${r > 1 ? "others" : "other"}`));
  } else e = A.pop() ?? "";
  return A.length > 1 ? `${A.join(", ")}, and ${e}` : `${A.join(", ")} and ${e}`;
}
function $j(t) {
  let j = _(t);
  if (!(!j || !uA(j) || !V(j))) return `${j.moduleId}:${j.exportSpecifier}`;
}
function H7(t) {
  let j = new Map();
  for (let A of t) {
    let e = $j(A);
    e && j.set(e, A);
  }
  return j;
}
function V7(t, j) {
  let A = t.getRawControlProps();
  for (let e in A) {
    let r = A[e]?.type,
      o = A[e]?.value;
    if (r !== "vectorsetitem" || !v(o) || !uA(o)) continue;
    let n = $j(o);
    if (!n || !j.has(n)) continue;
    let i = j.get(n);
    i && t.setControlProp(e, { type: "vectorsetitem", value: i });
  }
}
function U7(t, j) {
  for (let A of j) {
    V7(A, t);
    let e = $j(A.codeComponentIdentifier);
    if (!e || !t.has(e)) continue;
    let r = t.get(e);
    r && A.set({ codeComponentIdentifier: r });
  }
}
function X7(t, j) {
  let A = xt(j);
  if (A) {
    d(A.isLoaded(), "CMS Should always be loaded.");
    for (let { node: e, skipChildren: r } of A.walkWithSkipChildren()) {
      if ($A(e)) {
        if (!e.variables.some((i) => i.type === "vectorsetitem")) {
          r();
          continue;
        }
        Q7(e, t);
      }
      if (!N(e)) continue;
      let o = e.getControlProps();
      for (let n in o) {
        let i = o[n]?.value;
        if (o[n]?.type !== "vectorsetitem" || !v(i) || !uA(i)) continue;
        let a = $j(i);
        if (!a || !t.has(a)) continue;
        let k = t.get(a);
        k && e.setControlProp(n, { ...o[n], value: k });
      }
    }
  }
}
function Q7(t, j) {
  let A = !1,
    e = t.variables.map((r) => {
      if (r.type !== "vectorsetitem") return r;
      let { initialValue: o } = r,
        n = o.identifier;
      if (!n) return r;
      let i = $j(n);
      if (!i || !j.has(i)) return r;
      let s = j.get(i);
      return s ? ((A = !0), { ...r, initialValue: { ...o, identifier: s } }) : r;
    });
  A && t.set({ variables: e });
}
function z7(t, j) {
  for (let A of j.root.children) {
    if (Y(A)) {
      V7(A, t);
      continue;
    }
    (W(A) || xA(A)) && Q7(A, t);
  }
}
var T = cA("modules-store"),
  W7 = new Set(["codeFile", "canvasComponent", "screen", "layoutTemplate", "vector"]),
  Rn = 500;
function Fn(t) {
  return "sourceContent" in t && v(t.sourceContent);
}
function zj(t) {
  let { module: j, exportSpecifier: A } = t,
    { id: e, saveId: r, files: o } = j;
  return (d(o.module, "module file must exist for a saved module"), GA(e, r, o.module, A));
}
var XA,
  dj,
  Ej,
  IA,
  CA,
  hA,
  Y7 = class extends Ut {
    constructor(A, e, r, o, n, i, s, a, k) {
      super();
      this.componentLoader = A;
      this.treeStore = e;
      this.loadedExternalModulesStore = r;
      this.chromeStore = o;
      this.modulesAPIServicePromise = n;
      this.makeDocumentReadOnly = i;
      this.insertTemporaryImportMap = s;
      this.loadAllLocalModules = a;
      this.abortSignal = k;
      E(this, "dependenciesModule");
      E(this, "localModules", new Map());
      E(this, "fastRefreshModules", new Map());
      E(this, "externalModulesDynamicInfo", new Map());
      E(this, "revision", 0);
      bA(this, XA, new Map());
      E(this, "moduleDepsGraph", {});
      E(this, "modulesStorage");
      E(this, "moduleUpdatesEmitter", new ue());
      E(this, "fastRefreshModulesEmitter", new ue());
      bA(this, dj);
      E(this, "preInit", () => {
        this.modulesStorage.preInit();
      });
      bA(this, Ej, new Map());
      bA(this, IA, new Map());
      bA(this, CA, !1);
      bA(this, hA, []);
      E(
        this,
        "debugModuleStoreAndComponentLoaderRevisionsMatch",
        () => this.revision === this.componentLoader.modulesRevision
      );
      E(this, "checkForExternalModuleUpdates", async () => {
        if (this.treeStore.tree.isViewOnly) return;
        let A = kj.get(this.treeStore.tree);
        if (!A?.children?.length) return;
        let e = [],
          r = new Map();
        A.children.forEach((s) => {
          let a = _(s.codeComponentIdentifier);
          if (
            (d(
              a?.kind === "externalModuleExport",
              "expected ExternalModuleNode identifier to be of kind externalModuleExport"
            ),
            s.type === "vector")
          ) {
            let k = s.annotation("framerVector");
            if (!FA(k)) return;
            let l = k.set.moduleId,
              c = r.get(l) ?? [];
            (c.push(a), r.set(l, c));
          } else e.push(a);
        });
        for (let [s, a] of r) {
          let k = this.treeStore.tree.get(s);
          if (!U(k)) continue;
          let l = k.annotation("framerVectorSet");
          sj(l) && (Lt(l) || e.push(...a));
        }
        let o = e.map(({ moduleId: s, saveId: a }) => ({ moduleId: s, saveId: a }));
        if (!o.length) return;
        let n = [],
          i = [];
        for (let s = 0; s < o.length; s++) {
          let a = o[s];
          if (
            (d(a, "query is expected to be defined."), n.push(a), n.length < Rn && s < o.length - 1)
          )
            continue;
          let { modules: k } = await this.lookUpModules(n, { includeStatus: !0 });
          (i.push(...k), (n = []));
        }
        this.treeStore.tree.isViewOnly ||
          this.updateExternalModuleNodes(
            i.map((s, a) => {
              let k = e[a];
              return (
                d(k, "External module missing a matching identifier"),
                { module: s, exportSpecifier: k.exportSpecifier }
              );
            }),
            { onTreeUpdate() {} }
          ).catch(H);
      });
      E(this, "updatesPollId");
      E(
        this,
        "compileFastRefreshModule",
        async ({ localId: A, name: e, source: r, includeSourceMap: o = !0 }) => {
          let { type: n, name: i } = I6(A);
          if (!W7.has(n)) return;
          let s = n === "codeFile" ? DA(e).source : i,
            {
              code: a,
              imports: k,
              sourceMap: l,
            } = await D7({ localId: A, source: r, name: s, includeSourceMap: o }),
            c = {
              kind: "fast-refresh",
              localId: A,
              type: n,
              name: s,
              sourceContent: r,
              moduleContent: a,
              relativeImports: k.relative,
              sourceMapContent: l,
              binaryAssetContents: void 0,
              submoduleContents: {},
              update: 0,
            },
            [$, p] = t7(this.fastRefreshModules, (B) => {
              B.set(e, c);
            });
          ((this.fastRefreshModules = $),
            this.fastRefreshModulesEmitter.emit({
              patches: p,
              dependentModules: [],
              prioritizedModules: [],
              revision: ++this.revision,
              initialized: !0,
            }));
        }
      );
      E(
        this,
        "compile",
        ({
          localId: A,
          name: e,
          source: r,
          includeSourceMap: o = !0,
          type: n,
          telemetrySession: i,
        }) => {
          let s = C7({ name: e, source: r, includeSourceMap: o, type: n, telemetrySession: i });
          return (
            this.fastRefreshEnabled &&
              A &&
              this.compileFastRefreshModule({
                localId: A,
                name: e,
                source: r,
                includeSourceMap: o,
              }).catch(H),
            s
          );
        }
      );
      ((this.modulesStorage = new p7(
        () =>
          this.modulesAPIServicePromise.catch((l) => {
            throw (T.error(l, { context: "Failed to discover ModulesAPI service:" }), l);
          }),
        this.compile,
        (l) => this.scheduler.process(l),
        (l) => this.scheduler.processWhenReady(l),
        (l) => this.scheduler.runWhenIdle(l),
        e,
        () => this.getActiveScope(),
        () => this.makeDocumentReadOnly(),
        this.abortSignal
      )),
        (this.fastRefreshModulesEmitter.onNewStream = (l) => (
          T.debug("onNewStream - fast refresh", l),
          { latest: this.getAllModulesAsUpdateEvent(!0) }
        )),
        (this.moduleUpdatesEmitter.onNewStream = (l) => (
          T.debug("onNewStream - module updates", l),
          { latest: this.getAllModulesAsUpdateEvent(!1) }
        )),
        $7(
          {
            check: () =>
              SA.isTest || this.treeStore.tree.isViewOnly || !jA.isOn("importMapPruning")
                ? !1
                : w(this, CA) || w(this, hA).length > 0,
            effect: () => {
              T.debug("Showing Unsaved Changes dialog (import map queue processing)");
            },
          },
          this.abortSignal
        ),
        this.modulesStorage.subscribe((l) => {
          let c = l.dependenciesModule?.importMapContent,
            $ = this.dependenciesModule?.importMapContent,
            p = zt($, c),
            B = !0;
          jA.isOn("importMapPruning") && (B = p);
          let m = !1;
          if (c && c !== $) {
            let f = Qt(c);
            for (let O in f.imports ?? {}) w(this, XA).set(O, "success");
            m = l.metadata.multiplayerChange || p;
          }
          (c !== $ &&
            (p
              ? T.info("Import map resolutions changed, re-evaluating all modules")
              : B
                ? T.info("Import map updated, re-evaluating all modules")
                : T.info(
                    "Import map updated without existing resolution changes, skipping blanket re-eval"
                  )),
            m && l.metadata.multiplayerChange,
            (this.dependenciesModule = l.dependenciesModule),
            (this.localModules = l.modules));
          let R = this.moduleDepsGraph;
          this.moduleDepsGraph = l.depsGraph;
          let u = l.metadata.patches,
            F = new Set(),
            g = [],
            y = [],
            G;
          for (let f of u) {
            let O = f.path[0];
            if ((d(typeof O == "string"), O === _6)) {
              if (((G = f), B)) for (let M of l.modules.keys()) F.add(M);
              break;
            }
            f.op === "remove" && f.path.length === 1 ? y.push(O) : g.push(O);
          }
          let S = new Set();
          if (!G) {
            let f = this.getActiveScope(),
              O = d7(f);
            T.debug("Calculated visible code component module typeSlashNames", O);
            for (let M of g) Ue(l.depsGraph, M, F, O, S);
            for (let M of y) Ue(R, M, F, O, S);
            (g.forEach((M) => {
              F.delete(M);
            }),
              y.forEach((M) => {
                (F.delete(M), S.delete(M));
              }));
          }
          T.debug(
            "prioritized evaluation sending to sandbox",
            { dependentModules: F, visibleDependentModules: S },
            "highprio size",
            S.size,
            "dep size",
            F.size
          );
          let x = {
            patches: u,
            dependentModules: Array.from(F),
            prioritizedModules: Array.from(S),
            revision: ++this.revision,
            initialized: l.initialized,
          };
          (this.moduleUpdatesEmitter.emit(x),
            this.fastRefreshEnabled &&
              G &&
              this.fastRefreshModulesEmitter.emit({
                patches: [G],
                dependentModules: [],
                prioritizedModules: [],
                revision: x.revision,
                initialized: l.initialized,
              }));
        }));
    }
    getActiveScope() {
      if (this.chromeStore.mainView === 5) return;
      let A = this.treeStore.tree.getNode(w(this, dj));
      if (A) return (d(A?.loaded, "Active scope node should be loaded"), A.loaded);
    }
    setActiveScope(A) {
      Rj(this, dj, A);
    }
    getLatestDependencyGraph() {
      return this.moduleDepsGraph;
    }
    initialize() {
      return this.initialized
        ? this.modulesStorage.refresh()
        : (this.preInit(),
          this.setupPollingExternalModulesUpdates(),
          this.modulesStorage.initialize());
    }
    get initialized() {
      return this.modulesStorage.initialized;
    }
    whenInitialized() {
      return this.modulesStorage.whenInitialized();
    }
    whenIdle() {
      return this.modulesStorage.whenIdle();
    }
    startDivergenceReporter() {
      this.modulesStorage.startDivergenceReporter();
    }
    sampleDivergencesNow() {
      this.modulesStorage.sampleDivergencesNow();
    }
    refresh() {
      return this.modulesStorage.refresh();
    }
    deleteModules(A) {
      return this.modulesStorage.delete(A);
    }
    restoreModule(A, e) {
      return this.modulesStorage.restore(A, e);
    }
    upsertBatch(A) {
      return this.modulesStorage.upsertBatch(A);
    }
    forType(A) {
      return new B7(this.modulesStorage, A, this.debugModuleStoreAndComponentLoaderRevisionsMatch);
    }
    getNpmDependencyInstallStatus(A) {
      return w(this, XA).get(Dj(A)) ?? "unknown";
    }
    async addNpmDependencies(A) {
      let r = typeof this.dependenciesModule > "u" && !O7(),
        o = await Hj(this.modulesStorage.addNpmDependencies(A)),
        n = Array.isArray(A) ? A : [A],
        i = n.filter(({ target: k }) => !xe(k));
      for (let k of i) w(this, XA).set(Dj(k), o.ok ? "success" : "failure");
      if (!o.ok) {
        let k = o.error instanceof Error ? o.error.message : String(o.error);
        for (let l of i) {
          let c = Dj(l);
          q("npm_dependency_add_fail", { name: c, reason: k });
        }
        throw (
          i.length > 0 &&
            T.reportError(o.error, {
              context: "Failed to add npm dependencies",
              descriptors: i.map(({ target: l }) => l),
            }),
          o.error
        );
      }
      r && T7();
      let s = Object.keys(o.value.dependenciesMap.dependencies),
        a = n.map(({ target: k }) => k).filter((k) => !s.includes(k) && xe(k));
      if (a.length > 0) {
        let k = J7(a, { max: 5, placeholder: "package" });
        L({
          type: "add",
          variant: "warning",
          primaryText: k,
          secondaryText: `${a.length === 1 ? "is" : "are"} incompatible.`,
          duration: 5e3,
        });
      }
      return o.value;
    }
    changeScope(A) {
      (this.setActiveScope(A.id), this.modulesStorage.changeScope(A));
    }
    async reinstallNpmDependencies() {
      return { importMap: await this.modulesStorage.reinstallNpmDependencies() };
    }
    async uninstallBlockedNpmDependencies() {
      return { importMap: await this.modulesStorage.uninstallBlockedNpmDependencies() };
    }
    async unsafeUpgradeDependency(A, e, r) {
      return { importMap: await this.modulesStorage.unsafeUpgradeDependency(A, e, r) };
    }
    async clearNpmDependencies() {
      return this.modulesStorage.clearNpmDependencies();
    }
    async removeBlockedNpmDependencies() {
      return { dependenciesMap: await this.modulesStorage.removeBlockedNpmDependencies() };
    }
    async rebuildImportMapFromDependencies() {
      return { importMap: await this.modulesStorage.rebuildImportMapFromDependencies() };
    }
    async pruneProjectImportMapFromEntryPoints(A = !1) {
      let e = this.getEntryPointModuleNames();
      return {
        importMap: await this.modulesStorage.pruneProjectImportMapFromEntryPoints(e, { dryRun: A }),
      };
    }
    getEntryPointModuleNames() {
      let A = [];
      for (let e of this.localModules.values()) {
        if (
          e.kind === "fast-refresh" ||
          (e.type !== "codeFile" && e.type !== "canvasComponent" && e.type !== "screen")
        )
          continue;
        let r = e.files?.module;
        if (!r) continue;
        let o = pe(e.localId, r);
        A.push(o);
      }
      for (let e of kj.getModuleNodes(this.treeStore.tree)) {
        if (e.type !== "codeFile" && e.type !== "canvasComponent") continue;
        let r = _(e.codeComponentIdentifier);
        V(r) && A.push(r.importSpecifier);
      }
      return [...new Set(A)];
    }
    async updateSources(A) {
      return this.modulesStorage.updateSources(A);
    }
    async updateKit(...A) {
      return (await this.modulesStorage.whenInitialized(), this.modulesStorage.upsertKit(...A));
    }
    async listKits() {
      let A = await this.modulesAPIServicePromise;
      return (d(A, "Modules API service not initialized correctly"), await A.kits());
    }
    async enqueueKitScreenshots(...A) {
      return this.modulesStorage.enqueueKitScreenshots(...A);
    }
    async updateDependenciesSource(A, e) {
      await this.modulesStorage.updateDependenciesSource(A, e);
    }
    getDependentsOfModule(A) {
      return this.modulesStorage.getDependentsOfModule(A);
    }
    subscribeToModulesStorage(A) {
      return this.modulesStorage.subscribe(A);
    }
    async getModuleDependencies(A) {
      let e = await this.modulesAPIServicePromise;
      return (
        d(e, "Modules API service not initialized correctly"),
        await e.getModuleDependencies(A)
      );
    }
    getModuleEntryByLocalId(A) {
      for (let e of this.localModules.values()) if (e.localId === A) return e;
    }
    getModuleEntryByUniqueName(A) {
      for (let e of this.localModules.values()) if (e.name === A) return e;
    }
    getPersistedDependenciesModule() {
      return this.modulesStorage.getPersistedDependenciesModule();
    }
    getPersistedModuleByGlobalId(A) {
      return this.modulesStorage.getPersistedModuleByGlobalId(A);
    }
    getPersistedModuleByLocalId(A) {
      return this.modulesStorage.getPersistedModuleByLocalId(A);
    }
    getPersistedModuleByLocalIdentifier(A) {
      let e = _(A);
      if (Q(e)) return this.getPersistedModuleByLocalId(e.localId);
    }
    tryConvertExternalCodeComponentIdentifierToLocal(A) {
      let e = _(A);
      if (e?.kind !== "externalModuleExport") return;
      let r = this.getPersistedModuleByGlobalId(e.moduleId);
      if (r) return Fj(r.localId, e.exportSpecifier).value;
    }
    async listNamespaces() {
      let A = await this.modulesAPIServicePromise;
      return (d(A, "Modules API service not initialized correctly"), await A.listNamespaces());
    }
    async createNamespace(A, e) {
      let r = await this.modulesAPIServicePromise;
      return (
        d(r, "Modules API service not initialized correctly"),
        await r.createNamespace({ ownerId: A, ownerType: e })
      );
    }
    async lookUpModule(A, e) {
      let { modules: r, dependencies: o } = await this.lookUpModules([A], e),
        n = r[0];
      d(n, "Module does not exist, looking up:", A);
      let i = { module: n };
      return (o && (i.dependencies = o[n.ownerType]?.[n.ownerId]), i);
    }
    async getSourceContentForModuleSave(A) {
      let { module: e } = await this.lookUpModule(A);
      if (Fn(e)) return e.sourceContent;
      let r = e.baseURL + e.files.source,
        o = await Hj(fetch(r));
      if (!o.ok)
        throw (
          T.reportError(o.error, {
            context: "Failed to fetch source content for module save",
            moduleId: A.moduleId,
            saveId: A.saveId,
            sourceURL: r,
          }),
          o.error
        );
      if (!o.value.ok) {
        let i = new Error(
          `Failed to fetch source content for module save ${A.moduleId}@${A.saveId}: ${o.value.status} ${o.value.statusText}`
        );
        throw (
          T.reportError(i, {
            context: "Failed to fetch source content for module save",
            moduleId: A.moduleId,
            saveId: A.saveId,
            sourceURL: r,
            httpStatus: o.value.status,
          }),
          i
        );
      }
      let n = await Hj(o.value.text());
      if (!n.ok)
        throw (
          T.reportError(n.error, {
            context: "Failed to read source content for module save",
            moduleId: A.moduleId,
            saveId: A.saveId,
            sourceURL: r,
          }),
          n.error
        );
      return n.value;
    }
    async lookUpModules(A, e) {
      let r = await this.modulesAPIServicePromise;
      if (!r)
        return (
          T.error("Modules API service not initialized correctly"),
          { modules: [], dependencies: {} }
        );
      let { data: o, dependencies: n } = await r.lookUpModules({ queries: A, ...e }),
        i = new Map(this.externalModulesDynamicInfo);
      for (let a of o) {
        let k = a.id,
          { localId: l, title: c, description: $, lastPublish: p, status: B } = a;
        i.set(k, {
          localId: l,
          title: c,
          description: $,
          lastPublish: p,
          status: B ?? i.get(k)?.status,
        });
      }
      this.externalModulesDynamicInfo = i;
      let s = { modules: o };
      return (n && (s.dependencies = n), s);
    }
    trackExternalComponentInsert(A, e) {
      let r = v6();
      for (let o of A) h7({ insertId: r, insertType: e, codeComponentId: o });
    }
    async installExternalModulesDependencies(A) {
      let e = [],
        r = [];
      for (let o of A) {
        let n = w(this, Ej).get(o);
        n ? r.push(n) : e.push(o);
      }
      if (e.length > 0) {
        let o = this.installDependenciesForModuleURLs(e);
        for (let n of e) w(this, Ej).set(n, o);
        r.push(o);
      }
      return Promise.allSettled(r).then((o) => {
        for (let n of o)
          n.status !== "fulfilled" && T.error("Failed to install external module", n.reason);
      });
    }
    isProcessingModules() {
      return this.modulesStorage.isProcessing();
    }
    hasLocalCodeFileChanges() {
      return this.modulesStorage.hasLocalCodeFileChanges();
    }
    isReadOnly() {
      return this.modulesStorage.isReadOnly();
    }
    resetModuleStateAndPermissions(A) {
      this.modulesStorage.resetModuleStateAndPermissions(A);
    }
    getTransientSave(A) {
      return this.modulesStorage.getTransientSave(A);
    }
    createFastRefreshModuleFromModule(A) {
      if (W7.has(A.type)) {
        if (A.kind === "server") {
          let e = this.getPersistedModuleByLocalId(A.localId);
          (d(e?.kind === "server", "Only server modules should have no sourceContent"),
            this.modulesStorage.createLocalModuleFromModule(e).catch(H));
          return;
        }
        this.compileFastRefreshModule({
          name: jr(A),
          localId: A.localId,
          source: A.sourceContent,
        }).catch(H);
      }
    }
    getModuleWithTypeSlashName(A) {
      return this.modulesStorage.getModuleWithTypeSlashName(A);
    }
    waitForModulesToSave() {
      return this.modulesStorage.waitForModulesToSave();
    }
    async insertTemporaryImportMapForExternalModules(A) {
      let e = { imports: {} };
      for (let { dependencies: r, module: o } of A) {
        let n = r?.importMap;
        if (!(!n || Cj(n))) {
          if ((d(o.files.module, "Module must have a module file"), n.scopes)) {
            e.scopes ??= {};
            for (let [i, s] of Object.entries(n.scopes))
              e.scopes[i] = Object.assign({}, s, e.scopes[i]);
          }
          Object.keys(n.imports).length > 0 &&
            ((e.scopes ??= {}),
            (e.scopes[o.baseURL] = Object.assign({}, n.imports, e.scopes[o.baseURL])));
        }
      }
      Cj(e) || (await this.insertTemporaryImportMap(e));
    }
    async installDependenciesForExternalModules(A) {
      let e,
        r,
        o = [],
        n = new Set();
      for (let { dependencies: i, module: s } of A)
        if (i?.importMap) {
          if (jA.isOn("importMapPruning")) {
            d(s.files.module, "Module must have a module file");
            let a = `${s.baseURL}${s.files.module}`;
            if ((o.push(a), n.has(s.ownerId))) continue;
            n.add(s.ownerId);
          }
          if (!jA.isOn("importMapPruning")) {
            let a = i.dependencies ?? Yt(i?.importMap);
            r = Wt(r ?? { dependencies: {} }, a);
          }
          e = qe(e ?? { imports: {} }, i.importMap);
        }
      if (!(!e || Cj(e))) {
        if (jA.isOn("importMapPruning") && o.length > 0) {
          this.queueImportMapUpdate({ importMap: e, entryPoints: o }).catch(H);
          return;
        }
        !jA.isOn("importMapPruning") &&
          r &&
          (await this.modulesStorage.extendCurrentImportMap(e, r));
      }
    }
    installDependenciesForModuleURLs(A) {
      return (
        T.info(
          `\u{1F986} Installing external modules:
	`,
          A.join(`
	`)
        ),
        this.lookUpModules(
          A.map((e) => ({ url: e })),
          { includeDependencies: !0 }
        ).then(async ({ modules: e, dependencies: r = {} }) => {
          let o = e.map((n) => ({ module: n, dependencies: r[n.ownerType]?.[n.ownerId] }));
          (await this.insertTemporaryImportMapForExternalModules(o),
            await this.installDependenciesForExternalModules(o));
        })
      );
    }
    async addOrUpdateExternalVectors(A) {
      await this.modulesStorage.whenInitialized();
      let { modules: e } = await this.lookUpModules(
          A.filter((n) => {
            let i = this.treeStore.tree.getNodeWithTrait(n.moduleId, U);
            return i ? _(i.codeComponentIdentifier).saveId !== n.saveId : !0;
          }),
          { includeDependencies: !1, includeStatus: !0 }
        ),
        r = new Map(),
        o = "default";
      for (let n of e) {
        let i = zj({ module: n, exportSpecifier: o }),
          s = this.treeStore.tree.getNodeWithTrait(n.id, U);
        (uA(s?.codeComponentIdentifier) && _(s.codeComponentIdentifier).saveId === n.saveId) ||
          r.set(i.value, { module: n, exportSpecifier: o });
      }
      (await Promise.all(
        Array.from(r.keys(), (n) => {
          let i = _(n);
          return (
            d(i, "Identifier must be a valid module identifier"),
            this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(i).catch((s) => {
              T.error(`Failed to evaluate external module "${n}"`, s);
            })
          );
        })
      ),
        this.treeStore.getDataTree() ||
          (await this.scheduler.runWithFullyLoadedTreeAsync({
            name: "update_external_module_version",
            runInBackground: !0,
          })),
        await this.updateExternalModuleNodes(Array.from(r.values()), {
          onTreeUpdate: () => {
            let n = H7(r.keys()),
              i = this.treeStore.getDataTreeOrPartialTree(),
              s = i.query().whereClass(PA).iterate();
            (U7(n, s), X7(n, i), z7(n, i));
          },
          updateSave: !0,
        }));
    }
    async addOrUpdateVectorSetAndVectors(A) {
      await this.addOrUpdateExternalVectors([A]);
      let r = this.treeStore.tree.getNodeWithTrait(A.moduleId, U)?.annotation("framerVectorSet");
      sj(r) &&
        (await this.addOrUpdateExternalVectors(
          r.items.map((o) => ({ moduleId: o.moduleId, saveId: o.saveId ?? "" }))
        ));
    }
    async addExternalModulesToProject(A, e) {
      let r = A.map((n) => (typeof n == "string" ? _(n) : n)),
        o = await this.preloadExternalModules(r);
      return this.updateExternalModuleNodes(o, e);
    }
    usesNoopExternalModuleEvaluation() {
      return this.loadedExternalModulesStore.usesNoopExternalModuleEvaluation?.() === !0;
    }
    async preloadExternalModules(A) {
      if (A.length === 0) return [];
      await this.modulesStorage.whenInitialized();
      let e = new Map();
      for (let a of A) e.set(a.bareValue, a);
      if (this.loadedExternalModulesStore.usesNoopExternalModuleEvaluation?.() === !0)
        return (
          await Promise.all(
            Array.from(e.values(), (a) =>
              this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(a)
            )
          ),
          []
        );
      let r = [],
        o = [],
        n = [];
      for (let [a, k] of e) {
        let { moduleId: l, saveId: c } = k;
        if (!!this.modulesStorage.getPersistedModuleByGlobalId(l)) {
          T.reportError("Attempted to treat local module as external module", { identifier: k });
          continue;
        }
        let p = this.treeStore.tree.getNode(l);
        if (
          p &&
          _(p.codeComponentIdentifier).saveId === c &&
          !this.loadedExternalModulesStore.hasModuleForIdentifier(k)
        ) {
          o.push(k);
          continue;
        }
        let B = w(this, IA).get(a);
        if (B) {
          n.push({ identifier: k, preloadPromise: B });
          continue;
        }
        (w(this, IA).set(a, new me()), r.push(k));
      }
      let i = new Map();
      for (let { identifier: a, preloadPromise: k } of n)
        try {
          let l = await k;
          i.set(a.value, l);
        } catch {
          (w(this, IA).set(a.bareValue, new me()), r.push(a));
        }
      if (r.length)
        try {
          let a = Array.from(r.values(), ({ moduleId: $, saveId: p }) => ({
              moduleId: $,
              saveId: p,
            })),
            { modules: k, dependencies: l } = await this.lookUpModules(a, {
              includeDependencies: !0,
              includeStatus: !0,
            }),
            c = 0;
          for (let $ of r) {
            let p = k[c++];
            (d(p, "Module must be defined"),
              i.set($.value, {
                module: p,
                dependencies: l?.[p.ownerType]?.[p.ownerId],
                exportSpecifier: $.exportSpecifier,
              }),
              o.push($));
          }
        } catch (a) {
          T.reportError(a);
          for (let k of r) w(this, IA).get(k.bareValue)?.reject(a);
          throw Error(`Failed to preload modules: ${a}`);
        }
      let s = [];
      for (let a of o) {
        let k = i.get(a.value);
        k && s.push(k);
      }
      (await this.insertTemporaryImportMapForExternalModules(s),
        await Promise.all(
          Array.from(o.values(), (a) =>
            this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(a).catch((k) => {
              T.error(`Failed to evaluate external module "${a.value}"`, k);
            })
          )
        ));
      for (let a of r) {
        let k = i.get(a.value);
        d(k, "Expected to have module information for:", a.value);
        let l = w(this, IA).get(a.bareValue);
        (d(l, "Expected to find a preload promise for:", a.bareValue), l.resolve(k));
      }
      return [...i.values()];
    }
    queueImportMapUpdate(A) {
      return new Promise((e, r) => {
        (w(this, hA).push({ externalDeps: A, resolve: e, reject: r }),
          this.processImportMapUpdateQueue());
      });
    }
    async processImportMapUpdateQueue() {
      if (w(this, CA)) return;
      Rj(this, CA, !0);
      let A = "import-map-prune",
        e = !1;
      L({
        type: "add",
        key: A,
        text: "Optimizing dependencies\u2026",
        variant: "progress",
        duration: 1 / 0,
        showCloseButton: "never",
      });
      try {
        for (; w(this, hA).length > 0; ) {
          let r = w(this, hA).splice(0, w(this, hA).length);
          try {
            let o = [],
              n = { imports: {} };
            for (let { externalDeps: a } of r) {
              let k = await this.modulesStorage.scopeExternalImportMap(a.entryPoints, a.importMap);
              ((n = qe(n, k)), o.push(...a.entryPoints));
            }
            let i = [...new Set([...this.getEntryPointModuleNames(), ...o])],
              s = await this.modulesStorage.pruneProjectImportMapFromEntryPoints(i, {
                additionalImportMap: n,
              });
            for (let { resolve: a } of r) a(s);
          } catch (o) {
            e = !0;
            for (let { reject: n } of r) n(o);
          }
        }
      } finally {
        (e
          ? L({
              type: "add",
              key: A,
              text: "Failed to optimize dependencies.",
              variant: "error",
              duration: 3e3,
            })
          : L({ type: "remove", key: A }),
          Rj(this, CA, !1));
      }
    }
    async updateExternalModuleNodes(
      A,
      { onTreeUpdate: e, updateSave: r = !1, scheduleTreeUpdate: o }
    ) {
      (await this.insertTemporaryImportMapForExternalModules(A),
        await Promise.all(
          A.map((s) => this.loadedExternalModulesStore.ensureModuleEvaluatedInSandbox(zj(s)))
        ));
      let i = await (o ?? ((s) => this.scheduler.processWhenReadyAsync(s)))(() => {
        if (this.treeStore.tree.isViewOnly) throw Error("unable to edit");
        for (let s of A) {
          let { id: a } = s.module,
            k = this.treeStore.tree.getNode(a);
          if (k && !r) {
            let l = zj(s),
              c = this.externalModulesDynamicInfo.get(l.moduleId)?.status ?? s.module.status;
            k.set({ group: c?.group, updateSaveId: c?.updateSaveId });
          } else {
            let { type: l, metadata: c, ownerId: $, ownerType: p } = s.module,
              B = zj(s),
              { title: m, status: R } = this.externalModulesDynamicInfo.get(B.moduleId) ?? s.module,
              u = kj.get(this.treeStore.tree);
            (u || ((u = new kj()), this.treeStore.tree.insertNode(u, this.treeStore.tree.root.id)),
              d(gj(u)));
            let F = c.pluginId,
              g = {
                codeComponentIdentifier: B.value,
                type: l,
                scopeNodeId: Ar(s.module),
                intrinsicWidth: KA(c.intrinsicWidth, !0) ?? 200,
                intrinsicHeight: KA(c.intrinsicHeight, !0) ?? 200,
                ownerId: $,
                ownerType: p,
                group: R?.group,
                updateSaveId: R?.updateSaveId,
                namespaceId: s.module.lastPublish?.namespaceId,
                pluginId: v(F) ? F : void 0,
              },
              y = this.componentLoader.componentForIdentifier(B.value);
            d(y, "Component not found in the component loader", B.value);
            let G = y.annotations ?? void 0;
            g.annotations = tr(l) ? er({ default: G }).default : G;
            let S = y.name ?? m;
            if (((g.title = S), k)) k.set(g);
            else {
              let x = new rr({ id: a, ...g });
              this.treeStore.tree.insertNode(x, u.id);
            }
          }
        }
        return e();
      });
      return (await this.installDependenciesForExternalModules(A), i);
    }
    setupPollingExternalModulesUpdates() {
      let A = m7(() => {
          this.checkForExternalModuleUpdates().catch(T.reportError);
        }, 1e4),
        e = !1,
        r = () => {
          e ||
            (A(),
            this.updatesPollId === void 0 &&
              (this.updatesPollId = window.setInterval(this.checkForExternalModuleUpdates, 6e4)));
        },
        o = () => {
          (window.clearInterval(this.updatesPollId), (this.updatesPollId = void 0));
        };
      function n() {
        return !document.hidden && navigator.onLine;
      }
      n() && r();
      function i() {
        setTimeout(() => {
          n() ? r() : o();
        }, 500);
      }
      (document.addEventListener("visibilitychange", i, { signal: this.abortSignal }),
        window.addEventListener("offline", i, { signal: this.abortSignal }),
        window.addEventListener("online", i, { signal: this.abortSignal }),
        this.abortSignal?.addEventListener("abort", () => {
          ((e = !0), o());
        }),
        (this.abortSignal = void 0));
    }
    getCodeComponentNodesForExternalModuleExport(A) {
      return (
        this.treeStore.getDataTree() ||
          d(
            !this.treeStore.tree.getService("loader"),
            "tree must be complete to query code components"
          ),
        (this.treeStore instanceof cj && this.treeStore.getDataTree()
          ? this.treeStore.query().whereClass(PA).asArray()
          : this.treeStore.getLoadedIndexes().codeComponents
        ).filter((r) => {
          if (!b(r) || (tA(r) && !r.isVectorInstance)) return !1;
          let o = _(r.codeComponentIdentifier);
          return o?.kind !== "externalModuleExport"
            ? !1
            : o.moduleId === A.moduleId && o.exportSpecifier === A.exportSpecifier;
        })
      );
    }
    getShaderNodesForExternalModuleExport(A) {
      return (
        this.treeStore.getDataTree() ||
          d(!this.treeStore.tree.getService("loader"), "tree must be complete to query shaders"),
        (this.treeStore instanceof cj && this.treeStore.getDataTree()
          ? this.treeStore.query().whereClass(Er).asArray()
          : this.treeStore.getLoadedIndexes().shaderNodes
        ).filter((r) => {
          if (tA(r)) return !1;
          let o = _(r.shaderModuleIdentifier);
          return o?.kind !== "externalModuleExport"
            ? !1
            : o.moduleId === A.moduleId && o.exportSpecifier === A.exportSpecifier;
        })
      );
    }
    get fastRefreshEnabled() {
      return this.chromeStore.codeEditorPreviewVisible;
    }
    async updateExternalModuleComponentInstances(A) {
      let e = _(A);
      d(
        e?.kind === "externalModuleExport",
        "parsed 'ExternalModuleNodes.id' should be of kind externalModuleExport"
      );
      let { moduleId: r, exportSpecifier: o } = e,
        n = this.treeStore.tree.getNodeWithTrait(r, U),
        i = this.externalModulesDynamicInfo.get(r)?.status?.updateSaveId ?? n?.updateSaveId;
      if (!i) {
        T.warn("Could not update module", r, "latest status missing");
        return;
      }
      if (i === e.saveId) return;
      let { module: s, dependencies: a } = await this.lookUpModule(
        { moduleId: r, saveId: i },
        { includeDependencies: !0, includeStatus: !0 }
      );
      d(s.files.module, "Module file must be defined");
      let k = GA(r, i, s.files.module, o);
      try {
        (await this.loadedExternalModulesStore
          .ensureModuleEvaluatedInSandbox(k)
          .catch(T.reportError),
          this.treeStore.getDataTree() ||
            (await this.scheduler.runWithFullyLoadedTreeAsync({
              name: "update_external_module_version",
              modalTitle: "Updating the module version",
            })),
          await this.updateExternalModuleNodes(
            [{ module: s, dependencies: a, exportSpecifier: o }],
            {
              onTreeUpdate: () => {
                let l = this.treeStore.getDataTreeOrPartialTree();
                (s.type === "shader"
                  ? this.getShaderNodesForExternalModuleExport(e).forEach(($) =>
                      $.set({ shaderModuleIdentifier: k.value })
                    )
                  : this.getCodeComponentNodesForExternalModuleExport(e).forEach(($) =>
                      $.set({ codeComponentIdentifier: k.value })
                    ),
                  Mr(l, e.value, k.value),
                  wr(l, e.value, k.value));
              },
              updateSave: !0,
            }
          ),
          this.trackExternalComponentInsert([k.value], "update"));
      } catch (l) {
        T.reportError(l);
      }
    }
    async replaceRelativeImportsWithImportMapSpecifiers(A, e, r) {
      let o = {};
      for (let i of r) {
        let s = Nj(i, A);
        if (!s) continue;
        let a;
        if (
          ((a = this.fastRefreshModules.get(s)),
          a || (a = this.modulesStorage.getModuleWithTypeSlashName(s)),
          !a)
        ) {
          T.error("Cannot resolve", i, "from", A);
          continue;
        }
        let k,
          { name: l, localId: c } = a;
        if (a.kind === "fast-refresh") k = DA(l).module;
        else {
          let { files: $ } = a;
          (d(
            v($.module),
            "Must have a module file name to build a local module import map specifier."
          ),
            (k = $.module));
        }
        o[i] = pe(c, k);
      }
      let n = await E7(e, o);
      return (d(n.ok), n.value);
    }
    getModules(A = !1) {
      let e = {};
      for (let [r, o] of this.localModules) e[r] = o;
      if (A) for (let [r, o] of this.fastRefreshModules) e[r] = o;
      return {
        dependenciesModule: this.dependenciesModule,
        modules: e,
        revision: this.revision,
        initialized: this.modulesStorage.initialized,
      };
    }
    getAllModulesAsUpdateEvent(A = !1) {
      let { dependenciesModule: e, modules: r, initialized: o, revision: n } = this.getModules(A),
        i = [];
      e && i.push({ op: "add", path: [`${e.type}/${e.name}`], value: e });
      for (let s of Object.values(r))
        i.push({ op: "add", path: [`${s.type}/${s.name}`], value: s });
      return {
        patches: i,
        dependentModules: [],
        prioritizedModules: [],
        initialized: o,
        revision: n,
      };
    }
    getModulesUpdatesService(A = !1) {
      return {
        moduleUpdatesStream: () =>
          A
            ? this.fastRefreshModulesEmitter.newStream({ replay: "latest" })
            : this.moduleUpdatesEmitter.newStream({ replay: "latest" }),
      };
    }
    hasScreenModuleLoaded(A) {
      let e = h6("screen", A, "default").value;
      return this.componentLoader.componentForIdentifier(e) !== null;
    }
    hasPendingTreeData() {
      return this.modulesStorage.hasPendingTreeData();
    }
    writeTreeData() {
      this.modulesStorage.writeTreeData();
    }
    postProcess(A) {
      this.modulesStorage.processTreeUpdates(A);
    }
    takeSnapshot() {
      let A = this.modulesStorage.takeSnapshot();
      return new Ze(this, A);
    }
  };
((XA = new WeakMap()),
  (dj = new WeakMap()),
  (Ej = new WeakMap()),
  (IA = new WeakMap()),
  (CA = new WeakMap()),
  (hA = new WeakMap()));
var Ze = class {
  constructor(j, A) {
    this.modulesStorageSnapshot = A;
    E(this, "dependenciesModule");
    E(this, "localModuleKeys");
    ((this.dependenciesModule = j.dependenciesModule),
      (this.localModuleKeys = Array.from(j.localModules.keys())));
  }
  getPersistedModuleByLocalId(j) {
    return this.modulesStorageSnapshot.getPersistedModuleByLocalId(j);
  }
  getPersistedModuleByLocalIdentifier(j) {
    let A = _(j);
    if (Q(A)) return this.getPersistedModuleByLocalId(A.localId);
  }
  getModuleWithTypeSlashName(j) {
    return this.modulesStorageSnapshot.getModuleWithTypeSlashName(j);
  }
};
var In = {
    defaultComponent: { insert: "insert-default", drag: "insert-default-drag" },
    libraryModule: { insert: "insert-library", drag: "insert-library-drag" },
    externalModuleComponent: { insert: "insert", drag: "insert-drag" },
  },
  j6 = "default_component_",
  Wj = "external_",
  e6 = "local_",
  jo = "legacy_design_component",
  eo = "legacy_code_component",
  to = `${e6}${eo}`,
  hn = `${Wj}${eo}`,
  ro = `${e6}${jo}`,
  oo = `${Wj}${jo}`;
var gn = new Set(["form", "frame", "image-frame", "stack", "repeater"]),
  _n = (t) => t.startsWith(j6),
  no = (t) => t === ro || t === oo,
  bn = (t) => gn.has(t);
function io({ canvasNode: t, source: j, isDrag: A, engine: e }) {
  let r = Sn(t, e);
  switch ((q("component_instance", { componentType: r.type, source: j }), j)) {
    case "insert_menu": {
      let o = yn(r);
      q("insert_menu_add", { type_added: o });
      break;
    }
  }
  if (
    (r.type === "local_module_canvasComponent_component" &&
      q("component_canvas_instance", { componentId: t.id }),
    no(r.type) && q("component_design_instance", {}),
    b(t) && V(t.codeComponentIdentifier))
  ) {
    let o = r.type.startsWith(j6)
        ? "defaultComponent"
        : j === "team_library"
          ? "libraryModule"
          : "externalModuleComponent",
      n = In[o][A ? "drag" : "insert"];
    e.stores.modulesStore.trackExternalComponentInsert([t.codeComponentIdentifier], n);
  }
}
function Sn(t, j) {
  if (b(t)) {
    let r = t.codeComponentIdentifier,
      o = _(r);
    if (!o)
      return {
        type: t.codeComponentIdentifier.startsWith(".") ? to : hn,
        codeComponentIdentifier: r,
      };
    if (Q(o)) return { type: `${e6}module_${o.type}_component`, codeComponentIdentifier: r };
    if (V(o)) {
      let n = j.stores.treeStore.tree.getNode(o.moduleId);
      return JA(j.stores.treeStore.tree, r)
        ? { type: `${j6}${n?.title}`, codeComponentIdentifier: r }
        : { type: `${Wj}module_${n?.type ?? "unknown"}_component`, codeComponentIdentifier: r };
    }
  }
  let e = (t.replicaInfo?.master && j.tree.getNode(t.replicaInfo.master)) || t;
  return N6(e)
    ? x6(e)
      ? { type: oo }
      : { type: ro }
    : K6(t)
      ? { type: "repeater" }
      : wA(t)
        ? { type: "form" }
        : rA(t)
          ? { type: "stack" }
          : OA(t)
            ? t.fillType === "image"
              ? { type: "image-frame" }
              : { type: "frame" }
            : { type: "unknown" };
}
function yn({ type: t }) {
  return t.startsWith("local_module_") || t === to
    ? "code_component"
    : no(t)
      ? "design_component"
      : t.startsWith(Wj)
        ? "package"
        : _n(t) || bn(t)
          ? t
          : "unknown";
}
function t6(t) {
  let j = new $r({ widthType: 2, heightType: 2, html: "<p>No items</p>" });
  return (
    j.setTextColor("rgb(153, 153, 153)", !1),
    new ar({
      name: "Empty State",
      widthType: 2,
      minWidth: "100%",
      heightType: 0,
      height: 100,
      minHeight: "100%",
      layout: "stack",
      stackDirection: "vertical",
      stackDistribution: "center",
      stackAlignment: "center",
      gap: 10,
      padding: 10,
      fillColor: "rgba(204, 204, 204, 0.2)",
      borderEnabled: !0,
      borderWidth: 1,
      borderPerSide: !1,
      borderTop: 1,
      borderRight: 1,
      borderBottom: 1,
      borderLeft: 1,
      borderColor: "rgba(136, 136, 136, 0.2)",
      borderStyle: "dashed",
      radius: 20,
      children: new bj([j]),
      visible: P6("boolean", { type: "variableReference", id: ir, providerId: t.getPrimaryId() }, [
        { ...kt({ name: "equals", input: "number", output: "boolean" }), value: 0 },
      ]),
    })
  );
}
function Jl(t, j) {
  let A = t6(j);
  (q("repeater_empty_state_create", {}),
    t.scheduler.process(() => {
      let e = Gt(t.tree, j),
        r = e ? t.tree.getIndex(e) : -1,
        o = r >= 0 ? r + 1 : void 0;
      (t.tree.insertNode(A, j.getPrimaryId(), o), t.stores.selectionStore.set(A.id));
    }));
}
function ao(t, j) {
  for (let A of Object.values(t.variables))
    if (A?.type === "collectionreference" && A.dataIdentifier === j) return A;
}
function so(t, j) {
  return Object.values(t.variables).find(
    (A) => A?.type === "multicollectionreference" && A.dataIdentifier === j
  );
}
function On(t, j) {
  let A = Rt(j);
  if (Ie(A)) return { dataIdentifier: A.dataIdentifier, providerId: A.id };
  let e = t.tree.getScopeNodeFor(j);
  if (NA(e)) return { dataIdentifier: e.dataIdentifier, providerId: e.id };
}
function Tn(t, j, A) {
  let e = On(t, j);
  if (!e) return;
  let { dataIdentifier: r, providerId: o } = e,
    n = ve(t.tree, A.dataIdentifier),
    i = ve(t.tree, r);
  if (!n || !i) return;
  let s = ao(n, r),
    a = so(n, r),
    k = ao(i, A.dataIdentifier),
    l = so(i, A.dataIdentifier);
  if (l)
    return {
      id: EA(),
      itemKey: "id",
      transforms: [{ id: EA(), type: "valueTransform", name: "isIncludedIn", value: Aj(l.id, o) }],
    };
  if (k)
    return {
      id: EA(),
      itemKey: "id",
      transforms: [{ id: EA(), type: "valueTransform", name: "equals", value: Aj(k.id, o) }],
    };
  if (a)
    return {
      id: EA(),
      itemKey: a.id,
      transforms: [{ id: EA(), type: "valueTransform", name: "contains", value: Aj(Ke, o) }],
    };
  if (s)
    return {
      id: EA(),
      itemKey: s.id,
      transforms: [{ id: EA(), type: "valueTransform", name: "equals", value: Aj(Ke, o) }],
    };
}
function ko(t, j, A) {
  if (!Ie(A)) return;
  let e = Tn(t, j, A);
  if (e) {
    A.set({ collectionFilters: { filters: [e] } });
    return;
  }
  t.tree.insertNode(t6(A), A.id);
}
function lo(t, j) {
  let A = t.children;
  return D6(t) ? (!A || A.length === 0 ? [] : (j.getChildrenRects(t.id) ?? [])) : null;
}
function Eo(t, j, A) {
  if ((t >= j && t <= A) || (t <= j && t >= A)) return 0;
  let e = Math.abs(j - t),
    r = Math.abs(A - t);
  return Math.min(e, r);
}
function vn(t, j, A) {
  let e = t ? "y" : "x",
    r = t ? "height" : "width",
    o = j[e],
    n = 0,
    i = 1 / 0;
  return (
    A.forEach((s, a) => {
      if (i === 0) return;
      let k = s[e],
        l = k + s[r],
        c = Eo(o, k, l);
      c < i && ((n = a), (i = c));
    }),
    n
  );
}
function Pn(t, j, A, e) {
  let r = Math.max(t, A),
    o = Math.min(j, e);
  return r < o;
}
function Mn(t, j, A) {
  let e = t ? "y" : "x",
    r = t ? "height" : "width",
    o = A[j];
  d(o, "Out of bound access to childRects:", j, A);
  let n = o[e],
    i = n + o[r],
    s = new Set([j]);
  return (
    A.forEach((a, k) => {
      if (k === j) return;
      let l = a[e],
        c = l + a[r];
      Pn(n, i, l, c) && s.add(k);
    }),
    { startIndex: Math.min(...s), endIndex: Math.max(...s) }
  );
}
function Dn(t, j, A, e, r, o) {
  if (r) return e.startIndex;
  if (o) return e.endIndex;
  let n = j ? "x" : "y",
    i = j ? "width" : "height",
    s = t[n],
    a = 1 / 0,
    k = e.startIndex;
  for (let l = e.startIndex; l <= e.endIndex; l++) {
    let c = A[l];
    d(c, "Ouf of bound access to childRects:", l, e, A);
    let $ = c[n],
      p = $ + c[i],
      B = Eo(s, $, p);
    B < a && ((a = B), (k = l));
  }
  return k;
}
function Cn(t, j) {
  let A = [];
  for (let e = j.startIndex; e <= j.endIndex; e++) {
    let r = t[e];
    (d(r, "Ouf of bound access to childRects:", e, j, t), A.push(r));
  }
  return C.merge(...A);
}
function Nn(t, j, A, e) {
  let r = ej(t) || t.resolveValue("stackDirection") === "horizontal",
    o = C.merge(...j),
    n = r ? "x" : "y",
    i = r ? "y" : "x",
    s = r ? "width" : "height",
    a = r ? "height" : "width",
    k = A[n],
    l = A[i],
    c = o[i],
    $ = c + o[a],
    p = l < c,
    B = !p && l > $,
    m = vn(r, A, j),
    R = Mn(r, m, j),
    u = Dn(A, r, j, R, p, B),
    F = j[u];
  d(F, "Closest rect is not defined:", u);
  let g = F[n] + F[s] / 2,
    y = p || B ? p : k < g,
    G = y ? u : u + 1;
  (p && (G = 0), B && (G = j.length));
  let S = F[n],
    x = S + F[s],
    f = y ? S : x,
    O = null,
    M = y ? u - 1 : u + 1;
  if (M >= R.startIndex && M <= R.endIndex) {
    let X = j[M];
    if (X) {
      let z = X[n],
        lA = z + X[s];
      O = y ? lA : z;
    }
  }
  let J = f;
  eA(O) && (J = Math.round((J + O) / 2));
  let h = Cn(j, R),
    I = Math.round(h[i] + h[a] / 2),
    D = { [n]: J, [i]: I },
    Z = { ...D },
    nA = { ...D },
    iA = e[a],
    AA = Math.round(iA / 2);
  return (
    r ? ((Z.y -= AA), (nA.y += AA)) : ((Z.x -= AA), (nA.x += AA)),
    { insertionIndex: G, localInsertionLine: { a: Z, b: nA } }
  );
}
function xn(t, j) {
  let A = { insertionIndex: 0, localInsertionLine: { a: { ...j }, b: { ...j } } };
  if (t.length === 0) return A;
  let e = new Map(),
    r = 1e3;
  t.forEach((c, $) => {
    let p = Math.round(c.x * r),
      B = e.get(p);
    B
      ? (B.indices.push($), c.width > B.width && (B.width = c.width))
      : e.set(p, { x: c.x, width: c.width, indices: [$] });
  });
  let o = Array.from(e.values()).sort((c, $) => c.x - $.x),
    n = o.find((c) => j.x >= c.x && j.x <= c.x + c.width);
  if (
    (n ||
      (n = o.reduce((c, $) => {
        if (!c) return $;
        let p = $.x + $.width / 2,
          B = c.x + c.width / 2;
        return Math.abs(j.x - p) < Math.abs(j.x - B) ? $ : c;
      }, o[0])),
    !n)
  )
    return A;
  let i = [...n.indices].sort((c, $) => {
      let p = t[c],
        B = t[$];
      return !p || !B ? 0 : p.y - B.y;
    }),
    s = i.findIndex((c) => {
      let $ = t[c];
      return $ ? j.y < $.y + $.height / 2 : !1;
    });
  s === -1 && (s = i.length);
  let a;
  s < i.length ? (a = i[s] ?? 0) : (a = (i[i.length - 1] ?? 0) + 1);
  let k;
  if (s <= 0) {
    if (!i[0]) return A;
    k = t[i[0]]?.y ?? 0;
  } else if (s >= i.length) {
    let c = i[i.length - 1];
    if (!c) return A;
    let $ = t[c];
    if (!$) return A;
    k = $.y + $.height;
  } else {
    let c = i[s];
    if (!c) return A;
    let $ = t[c];
    if (!$) return A;
    k = $.y;
  }
  let l = { a: { x: n.x, y: k }, b: { x: n.x + n.width, y: k } };
  return { insertionIndex: a, localInsertionLine: l };
}
var co = 4,
  $o = 20;
function Bo(t, j, A, e, r) {
  if (
    (d(j.length, "childInsertion only works correctly if there are more than 0 child rects"),
    jA.isOn("betterStackGridItemMoving"))
  ) {
    let { rect: l, index: c } = C.closestRect(j, e),
      $ = jj(t),
      p = C6(t) ? !$ : t.resolveValue("stackDirection") === "horizontal",
      B = p ? "x" : "y",
      m = p ? "y" : "x",
      R = p ? "width" : "height",
      u = p ? "height" : "width",
      F = e[B] < l[B] + l[R] / 2,
      g = $ && eA(t.gridColumnCount) ? Math.max(Math.floor(t.gridColumnCount), 1) : 1,
      y = F ? c - g : c + g,
      G = j[y],
      S = !G || (F ? G[B] >= l[B] : l[B] >= G[B]),
      x = F ? c : Math.min(c + g, j.length);
    if (S) {
      let f = F ? l[B] + co : l[B] + l[R] - co;
      return {
        insertionIndex: x,
        localInsertionLine: { a: Yj(B, f, l[m]), b: Yj(B, f, l[m] + Math.max(l[u], $o)) },
      };
    } else {
      let f = F ? (G[B] + G[R] + l[B]) / 2 : (l[B] + l[R] + G[B]) / 2;
      return {
        insertionIndex: x,
        localInsertionLine: {
          a: Yj(B, f, Math.min(G[m], l[m])),
          b: Yj(B, f, Math.max(G[m] + G[u], l[m] + Math.max(l[u], $o))),
        },
      };
    }
  }
  if (ej(t) || t.stackWrapEnabled) return jj(t) ? xn(j, e) : Nn(t, j, e, r);
  let o = 1 / 0,
    n = null,
    i = t.stackDirection === "vertical";
  j.forEach((l, c) => {
    let $ = Math.abs(i ? e.y - l.y : e.x - l.x);
    $ > o || ((o = $), (n = c));
  });
  let s = 0;
  if (n !== null) {
    let l = j[n];
    d(l, "Closest child rect is not defined:", n);
    let c = C.center(l);
    ((i ? e.y > c.y : e.x > c.x) && n++, (s = Math.min(Math.max(0, n), j.length)));
  }
  let a = Kn(t),
    k = Ln(t.resolveValue("stackDirection"), t.resolveValue("stackAlignment"), a, A, j, s, r);
  return { insertionIndex: qn(t, s), localInsertionLine: k };
}
function Yj(t, j, A) {
  return t === "x" ? { x: j, y: A } : { x: A, y: j };
}
function qn(t, j) {
  d(aj(t), "A node must support children to consider an insertion index.");
  let A = j;
  for (let e = 0; e < t.children.length && e !== j; e++) {
    let r = t.children.at(e);
    r && (Dt(r) || A++);
  }
  return A;
}
function Kn(t) {
  let j = t.resolveValue("stackDirection"),
    A = t.resolveValue("stackAlignment");
  if (A === "center") return 0;
  let e = qt(t);
  if (!e.perSide) return e.top;
  let r = A === "start";
  return j === "vertical" ? (r ? e.left : e.right) : r ? e.top : e.bottom;
}
function Ln(t, j, A, e, r, o, n) {
  let i = r[o - 1],
    s = r[o],
    a = t === "vertical",
    k = { x: e.width / 2, y: e.height / 2 };
  if (!(!i && !s)) {
    if (r.length === 1) {
      let l = i || s;
      (d(l, "Child rect must be defined"),
        o === 0
          ? a
            ? (k.y = l.y - 10)
            : (k.x = l.x - 10)
          : a
            ? (k.y = l.y + l.height + 10)
            : (k.x = l.x + l.width + 10));
    } else if (i && s) {
      let l = Math.abs(a ? i.y + i.height - s.y : i.x + i.width - s.x);
      a ? (k.y = s.y - l / 2) : (k.x = s.x - l / 2);
    } else if (i) {
      let l = r[o - 2];
      d(l, "Child rect must be defined");
      let c = Math.abs(a ? l.y + l.height - i.y : l.x + l.width - i.x);
      a ? (k.y = i.y + i.height + c / 2) : (k.x = i.x + i.width + c / 2);
    } else if (s) {
      let l = r[o + 1];
      d(l, "Child rect must be defined");
      let c = Math.abs(a ? s.y + s.height - l.y : s.x + s.width - l.x);
      a ? (k.y = s.y - c / 2) : (k.x = s.x - c / 2);
    }
  }
  return (
    (k.x = Math.min(Math.max(4, k.x), e.width - 4)),
    (k.y = Math.min(Math.max(4, k.y), e.height - 4)),
    wn(t, j, A, k, e, n)
  );
}
function wn(t, j, A, e, r, o) {
  let n = t === "vertical",
    i,
    s,
    a = A ?? 0;
  switch (j) {
    case "center":
      ((i = { x: n ? e.x - o.width / 2 : e.x, y: n ? e.y : e.y - o.height / 2 }),
        (s = { x: n ? e.x + o.width / 2 : e.x, y: n ? e.y : e.y + o.height / 2 }));
      break;
    case "start":
      ((i = { x: n ? a : e.x, y: n ? e.y : a }),
        (s = { x: n ? o.width + a : e.x, y: n ? e.y : o.height + a }));
      break;
    case "end":
    default:
      ((i = { x: n ? r.width - o.width - a : e.x, y: n ? e.y : r.height - o.height - a }),
        (s = { x: n ? r.width - a : e.x, y: n ? e.y : r.height - a }));
      break;
  }
  return { a: i, b: s };
}
var po = 10;
function Jn(t, j) {
  for (let A of t) if (j.includes(A)) return !0;
  return !1;
}
function Hn(t, j, A, e, r) {
  if (r.length === 0 || t.stackWrapEnabled || RA(t) || tA(t)) return null;
  let { tree: o } = j,
    { zoom: n } = j.stores.canvasStore,
    i = o.getParent(t.id);
  if (i && rA(i) && (i.stackDirection !== t.stackDirection || i.stackWrapEnabled)) return null;
  let s = o.convertPointToNode(t, A),
    a = o.getRect(t),
    k = t.stackDirection === "vertical",
    l = k ? "x" : "y",
    c = k ? "y" : "x",
    $ = k ? "width" : "height",
    p = k ? "height" : "width",
    B = a[$],
    m = a[p],
    R = B * n,
    u = m * n;
  if (R < 40 || u < 40) return null;
  let F = s[l],
    g = 10 / n,
    y = C.merge(...r),
    G = y[l],
    S = G + y[$],
    x = Math.max(g, G / 2),
    f = Math.max(g, (B - S) / 2),
    O = F <= x,
    M = F >= B - f;
  if (!O && !M) return null;
  let J = O ? 0 : 1,
    h = e[p],
    I = 4 / n,
    D = O ? I : B - I,
    Z = m / 2,
    nA = h / 2,
    iA = m / 3,
    AA = s[c],
    kA = "end";
  h > m ? (kA = "center") : AA < iA ? (kA = "start") : AA < 2 * iA && (kA = "center");
  let X, z;
  switch (kA) {
    case "start": {
      ((X = 0), (z = h));
      break;
    }
    case "end": {
      ((z = m), (X = z - h));
      break;
    }
    case "center":
    default: {
      ((X = Z - nA), (z = Z + nA));
      break;
    }
  }
  let lA = { [l]: D, [c]: X },
    gA = { [l]: D, [c]: z },
    zA = o.convertPointToCanvas(t, lA),
    _A = o.convertPointToCanvas(t, gA);
  return { insertionIndex: J, insertionLine: { a: zA, b: _A }, wrap: { wrapAlignment: kA } };
}
function r6(t, j, A, e, { allowWrapping: r = !0 } = {}) {
  if (!aj(t)) return { insertionIndex: null, insertionLine: null };
  let o = lo(t, j.stores.layoutCache);
  if (!o) return { insertionIndex: null, insertionLine: null };
  if (rA(t) && r) {
    let c = Hn(t, j, A, e, o);
    if (c) return c;
  }
  let n = j.tree,
    i = n.getRect(t),
    s = n.convertPointToNode(t, A);
  if (o.length === 0) return { insertionIndex: 0, insertionLine: null };
  let { insertionIndex: a, localInsertionLine: k } = Bo(t, o, i, s, e),
    l = { a: n.convertPointToCanvas(t, k.a), b: n.convertPointToCanvas(t, k.b) };
  return { insertionIndex: a, insertionLine: l };
}
var Vn = { width: 10, height: 10 };
function mo(t, j, A, e, r = !1) {
  d(A.length, "getStackOrGridInRange(): expects at least a single selected node");
  let o = t.stores.scopeStore.active;
  if (A.some(bt)) return null;
  let i = t.tree
    .getNodesAtPoint(o, j)
    .filter(
      (k) =>
        !(
          (Ht(k) && k.locked) ||
          !aj(k) ||
          Xr(k) ||
          Ot(t.tree, t.stores.overlayStore.activeOverlays, k)
        )
    )
    .reverse();
  if (e) {
    let k = e.id,
      l = i.find((c) => c.id === k);
    if (l) return l;
  }
  let s = null,
    a = A.map((k) => k.id);
  for (let k of i) {
    if (!tt(k) || (r && !qA(k))) continue;
    let l = t.tree.getAncestors(k.id);
    if ((l.push(k.id), Jn(l, a) || (s || (s = k), !tj(k)))) continue;
    let { insertionLine: $ } = r6(k, t, j, Vn);
    if (!$) continue;
    if ($.a.x === $.b.x) {
      if (Math.abs(j.x - $.a.x) * t.stores.canvasStore.zoom > po) continue;
    } else if ($.a.y === $.b.y) {
      if (Math.abs(j.y - $.a.y) * t.stores.canvasStore.zoom > po) continue;
    } else continue;
    if (A.every((B) => vA(t.tree, k, B, o.id, t.componentLoader))) return k;
  }
  if (tj(s)) {
    let k = s;
    if (A.every((c) => vA(t.tree, k, c, o.id, t.componentLoader))) return s;
  }
  return null;
}
function uo(t, j, { wrapAlignment: A }) {
  let e = Lj.addStack(t, [j]);
  if (!e) return null;
  let r = t.tree.getNode(e);
  return !r || !rA(r)
    ? null
    : (r.set({
        stackDirection: j.stackDirection === "horizontal" ? "vertical" : "horizontal",
        stackAlignment: A,
      }),
      r);
}
function o6(t, j) {
  let { width: A, height: e } = t.tree.getRect(j);
  return (
    be(j) &&
      _e(j) &&
      (j.widthType === 2 && j.width > 0 && (A = j.width),
      j.heightType === 2 && j.height > 0 && (e = j.height)),
    { width: A, height: e }
  );
}
function Un(t, j, A) {
  let e = o6(t, j);
  return { ...e, x: A.x - e.width / 2, y: A.y - e.height / 2 };
}
function Xn(t, j) {
  let A = o6(t, j),
    e = t.stores.scopeStore.active,
    r = t.tree.getCommonGroundNode(t.stores.selectionStore.nodes),
    o,
    n;
  if (r) {
    let s = sA(t.tree, r);
    ((o = C.center(s).x - A.width / 2), (n = s.y));
  } else if (Y(e) || W(e)) {
    let s = e.getPrimaryVariant(),
      a = sA(t.tree, s);
    ((o = C.center(a).x - A.width / 2), (n = a.y));
  } else {
    let s = t.stores.canvasStore.getCanvasCenter();
    ((o = s.x - A.width / 2), (n = s.y - A.height / 2));
  }
  let i = { x: o, y: n };
  for (;;) {
    let s = { ...A, ...i },
      a = fr(t.tree, e, s, !0);
    if (a.length === 0) return s;
    let k = Pj(t.tree, a);
    i.x = k.x - A.width - 100;
  }
}
function Qn(t, j, A, e = !1) {
  let { tree: r } = t,
    o = r.getPotentialParents(
      t.stores.scopeStore.active,
      t.stores.overlayStore.activeOverlays,
      C.cornerPoints({ ...A, width: 1, height: 1 }),
      A,
      t.componentLoader,
      j
    );
  return (
    e && (o = o.filter(qA)),
    (o = r.sortVisually(o).reverse()),
    o.length > 0 && o[0] ? o[0] : null
  );
}
var Go = { width: 1, widthType: 1, heightType: 2, left: 0, right: null };
function Ro({
  engine: t,
  component: j,
  canvasPoint: A,
  source: e,
  type: r,
  shouldBecomeFullWidthForVariants: o = new Set(),
  isLayoutSection: n = !1,
  notDraggedOntoCanvas: i = !1,
  insertAsGroundNode: s = !1,
  insertAsOverlayType: a,
  imageToUpload: k,
  parentId: l,
}) {
  dr(j) ||
    io({
      canvasNode: j,
      source: r === "libraryModule" ? "team_library" : e,
      isDrag: !0,
      engine: t,
    });
  let { selectionStore: c, canvasStore: $, scopeStore: p } = t.stores,
    B = s ? Xn(t, j) : Un(t, j, A);
  a === 1 &&
    Tj(j) &&
    j.set({
      floatingPositionEnabled: !0,
      floatingPlacement: St,
      floatingAlignment: yt,
      floatingOffsetX: 0,
      floatingOffsetY: 10,
    });
  let m = Qr(t, j),
    R = mo(t, A, [m], null, n);
  if (i && n && !R) {
    let h = $.getCanvasVisibleRectTakingOverlaysIntoAccount(),
      I = c.ids.length === 1 && c.ids[0] ? t.tree.getNode(c.ids[0]) : null,
      D = I ? t.tree.getGroundNodeFor(I) : null;
    tj(D) && C.intersects(sA(t.tree, D), h) && (R = D);
  }
  let u = l ? t.tree.getNode(l) : (R ?? Qn(t, j, A, n)),
    F = c.ids.length === 0,
    g = p.active,
    y = it(g) ? g.getPrimaryVariant() : null;
  if ((i && n && y && F && (u = y), a === 1 && u && Tj(m) && !qA(u))) {
    d(u, "Parent should exist");
    let h = u.getPrimaryId(),
      I = e7(t.componentLoader, t.stores, "framer/useShowRelativeOverlay", h, m.draftOrCurrent());
    if (!I) return;
    t.tree.move(I.id, h);
    return;
  } else Tj(m) && m.set(_t);
  if (
    s &&
    ((u = null), (R = null), !C.containsRect($.getCanvasVisibleRectTakingOverlaysIntoAccount(), B))
  ) {
    let h = C.center(B),
      I = $.zoom,
      D = Math.min(I, 1);
    $.scrollToCenter(h, { animate: !0, zoom: D });
  }
  let G = -1,
    S = null;
  if (u) {
    (Tr(t, u, j, B), ko(t, u, j));
    let h = u.draftOrCurrent();
    rA(h) && (R = h);
  }
  if (R) {
    let h = o6(t, j);
    if (l) G = R.children.length;
    else {
      let { insertionIndex: I, wrap: D } = r6(R, t, A, h);
      (D && (S = D), eA(I) && (G = I));
    }
    if (i && n) {
      if (((G = R.children.length), c.ids.length === 1)) {
        let u6 = c.ids[0],
          de = u6 && t.tree.getNode(u6);
        if (de) {
          let G6 = new Set([de.id]);
          for (let Ee of de.ancestors()) G6.add(Ee.id);
          let R6 = R.children.findIndex((Ee) => G6.has(Ee.id));
          R6 >= 0 && (G = R6 + 1);
        }
      }
      let I,
        D = G > 0 ? R.children.at(G - 1) : void 0;
      (D ? ((I = sA(t.tree, D)), (I.y += I.height)) : (I = sA(t.tree, R)), (I.height = h.height));
      let Z = sA(t.tree, R),
        nA = Math.min(I.x, Z.x),
        iA = Math.max(C.maxX(I), C.maxX(Z));
      ((I.x = nA), (I.width = iA - nA));
      let { zoom: AA } = $,
        kA = $.getCanvasVisibleRectTakingOverlaysIntoAccount(),
        X = kA.width * AA,
        z = kA.height * AA,
        lA = 40,
        gA = Math.max(X - lA * 2, 100),
        zA = Math.max(z - lA * 2, 100),
        _A = gA / (I.width || 1),
        WA = zA / (I.height || 1),
        rn = Math.min(_A, WA, 0.5),
        on = C.center(I);
      $.scrollToCenter(on, { animate: !0, zoom: rn });
    }
    vr(t, R, j, B);
  }
  let x;
  if (u) {
    let { width: h, height: I } = B;
    x = { ...Ct(t.tree, u, B), width: h, height: I };
  } else x = B;
  let f = m.updateForRect({
    rect: x,
    parentSizeInfo: null,
    constraintsLocked: !1,
    shouldGuessPinToBottom: u ? Br(t.tree, m, u) : !0,
    shouldGuessPinToRight: u ? pr(t.tree, m, u) : !0,
  });
  (u && rA(u) && _e(m) && be(m) && (f = Wn(m, f)), q7(t.componentLoader, m, f), m.set(f, t.tree));
  let O = m.id,
    M = !1,
    J = null;
  if (u) {
    if (
      ((J = u),
      At(u) &&
        ((J = t.tree.getNode(u.originalid)),
        (M = !0),
        d(J, "Fail to insert node into variants: the original parent should exist")),
      S && !tA(J) && rA(J))
    ) {
      let Z = uo(t, J, S);
      if (!Z) return;
      J = Z;
    }
    (t.moveNode(m.id, J.id, G),
      M &&
        (d(tA(u), "Fail to insert node into variants: the new parent should be a replica node"),
        (O = Pt(t, m, u, J))));
    let h = t.tree.getGroundNodeFor(J),
      I = h.resolveValue("name"),
      D = !!(I && o.has(I.toLowerCase()));
    if (
      (OA(m) && D && m.set({ left: 0, width: 1, widthType: 1 }, t.tree), b(m) && Y6(h) && qA(h))
    ) {
      let nA = { width: m.width, widthType: m.widthType, left: m.left, right: m.right };
      D && m.set(Go);
      let iA = Sr(t.componentLoader, m);
      if (iA) {
        let AA = t.tree.getScopeNodeFor(h);
        Y(AA) &&
          AA.getTopLevelReplicaVariants().forEach((X) => {
            let z = X.resolveValue("name");
            if (!z) return;
            let lA = yr(z, iA);
            if (!lA || lA === iA.defaultVariant) return;
            let gA = { ...X.replicaInfo.overrides },
              zA = { ...X.replicaInfo, overrides: gA },
              _A = { ...gA[m.id] },
              WA = zn(D, o, z, nA);
            (WA && Object.assign(_A, WA),
              (_A[et(iA.key)] = { type: "enum", value: lA }),
              (gA[m.id] = _A),
              X.set({ replicaInfo: zA }));
          });
      }
    }
    ej(u) &&
      ft(m) &&
      (D || m.set({ gridItemFillCellWidth: !0 }), jj(u) || m.set({ gridItemFillCellHeight: !0 }));
  }
  (k && OA(j) && A7(k, j, t).catch(H),
    !l && c.set(O, { switchToLayers: !!l, switchToProperties: !0 }));
}
function zn(t, j, A, e) {
  let r = j.has(A.toLowerCase());
  if (t !== r) return r ? Go : e;
}
function Wn(t, j) {
  let A = { ...j };
  return (
    (t.widthType === 3 || t.widthType === 1) &&
      eA(j.width) &&
      j.widthType !== t.widthType &&
      (delete A.width, delete A.widthType),
    (t.heightType === 3 || t.heightType === 1) &&
      eA(j.height) &&
      j.heightType !== t.heightType &&
      (delete A.height, delete A.heightType),
    A
  );
}
function Zj({
  engine: t,
  module: j,
  identifier: A,
  position: e,
  centerInParent: r,
  insertAsGroundNode: o,
  update: n,
  parentId: i,
}) {
  let s = {
      width: KA(j.metadata.intrinsicWidth, !0) ?? 200,
      height: KA(j.metadata.intrinsicHeight, !0) ?? 200,
    },
    a = new PA({
      codeComponentIdentifier: A,
      slotsAreChildNodes: jA.isOn("componentSlotsAreChildNodes"),
      ...s,
    });
  if (i !== void 0) {
    let l = t.tree.getNode(i);
    (d(l, "Parent node not found", i),
      d(
        vA(t.tree, l, a, t.stores.scopeStore.active.id, t.componentLoader),
        "Parent does not accept children",
        i
      ));
  }
  n && n(a);
  let k = e;
  if (r && !o && !i) {
    let l = K7(a, t),
      c = l ? C.center(sA(t.tree, l)) : null,
      $ = t.stores.canvasStore.getCanvasVisibleRectTakingOverlaysIntoAccount();
    c && C.containsPoint($, c) && (k = c);
  }
  return (
    Ro({
      engine: t,
      component: a,
      canvasPoint: k,
      source: "context_menu",
      insertAsGroundNode: i ? !1 : o,
      parentId: i,
    }),
    a
  );
}
var Bj = cA("unlinking");
function Fo(t, j, A = !0) {
  t.scheduler.processWhenReady(() => {
    let {
      overlayStore: e,
      selectionStore: r,
      scopeStore: o,
      canvasStore: n,
      persistedUserDefaults: i,
    } = t.stores;
    o.selectByNode(j.id);
    let s = nj(j) ? j : t.tree.getNodeWithTrait(j.cache.overlayAncestorId, nj);
    if ((s ? e.showOverlay(r, s, s.parentid) : e.hideAll(), r.set(j.id), A)) {
      let a = Pj(t.tree, [j]);
      n.zoomToCenter(a, { animated: i.animateOnZoom, maxZoom: 1 });
    }
  });
}
async function Yn(t, j, A) {
  let { codeEditorStore: e, selectionStore: r, treeStore: o } = t.stores;
  e.closeEditor();
  let [n] = r.nodes;
  if (r.nodes.length === 1 && b(n) && n.codeComponentIdentifier === j) return !0;
  let i;
  if (o.getDataTree()) {
    for (let s of o.query().whereClass(PA).iterate())
      if (s.codeComponentIdentifier === j) {
        i = s;
        break;
      }
  } else
    i = await t.runWithFullyLoadedTreeAsync(
      () => {
        for (let s of o.treeIndex.codeComponentNodeIds) {
          let a = t.tree.getNodeWithTrait(s, b);
          if (a?.codeComponentIdentifier === j) return a;
        }
      },
      { runInBackground: !0, name: `findCodeComponentInstance: ${j}` }
    );
  return i
    ? (Fo(t, i), !0)
    : (L({
        type: "add",
        variant: "info",
        key: "add-component-instance-to-edit-in-plugin",
        primaryText: "Add component to Canvas",
        secondaryText: `to edit in ${A.name}.`,
      }),
      !1);
}
async function Ae(t, j, A) {
  return (await Yn(t, j, A)) ? (t.stores.pluginStore.openPlugin(A, t, { mode: "canvas" }), !0) : !1;
}
function je(t, j) {
  let A = t.componentForIdentifier(j);
  if (A) return A;
  let e = _(j);
  return !Q(e) || !uA(e) ? null : t.componentForIdentifier(f6(e));
}
var Zn = 0;
async function ee(t, j, A = Zn) {
  if (A === 0) {
    let r = _(j),
      o = Kj(t.stores.treeStore, t.stores.modulesStore, t.stores.pluginStore, r);
    if (o && (await Ae(t, j, o))) return;
  }
  let e = je(t.componentLoader, j);
  e && t.stores.codeEditorStore.editFile(e.file);
}
async function fo(
  t,
  {
    identifier: j,
    info: A,
    instance: e,
    enterIsolation: r,
    codeComponentIsolationMode: o,
    scheduleTreeUpdate: n = (i) => t.scheduler.processWhenReadyAsync(i),
  }
) {
  let i = A.module.baseURL + A.module.files.source,
    a = await (await fetch(i)).text(),
    [k, l] = await Promise.all([N7(a), t.stores.modulesStore.getModuleDependencies(j)]);
  if (k.ok) {
    let m = new Map();
    for (let u of k.value.relative) {
      let F = Nj(u.specifier, `${A.module.type}/`);
      if (!F) {
        Bj.warn("Failed to normalize relative import", u.specifier);
        continue;
      }
      let g = m.get(F) ?? [];
      (g.push(u), m.set(F, g));
    }
    let R;
    for (let u of l.imports) {
      if (u.type !== "local") continue;
      let F = `${u.moduleType}/${u.moduleName}`,
        g = m.get(F);
      if (!g) {
        Bj.warn("Unable to get source location of import", F, "when unlinking");
        continue;
      }
      for (let y of g) {
        R ||
          (R = a.split(`
`));
        let G = y.specifierLine;
        R[G] = R[G].replace(JSON.stringify(y.specifier), JSON.stringify(u.importURL));
      }
    }
    R &&
      (a = R.join(`
`));
  }
  let c = t.stores.modulesStore.forType("codeFile"),
    $ = c.getUniqueName(A.module.name),
    p = await c.create({ name: $, source: a, metadata: { pluginId: A.module.metadata.pluginId } }),
    B = g6(j, p).value;
  return (
    e &&
      (await n(() => {
        let m = t.tree.get(e.getPrimaryId());
        m && m.set({ codeComponentIdentifier: B });
      })),
    r && ((t.stores.chromeStore.contentPanelTab = "Assets"), ee(t, B, o)),
    B
  );
}
var Io = { canvasComponent: Kr, codeFile: fo };
function i6(t) {
  return v(t) ? t in Io : !1;
}
async function pj(
  t,
  {
    identifier: j,
    moduleType: A,
    instance: e,
    enterIsolation: r = !0,
    codeComponentIsolationMode: o,
    renamedIds: n = new MA(),
    isAutoDetach: i = !1,
    scheduleTreeUpdate: s,
  }
) {
  if (!i6(A)) throw Error(`Cannot import external module of type ${A}`);
  let [a] = await t.stores.modulesStore.preloadExternalModules([j]);
  d(a, "external module must exist on preload");
  let k = {
      identifier: j,
      info: a,
      instance: e,
      enterIsolation: r,
      codeComponentIsolationMode: o,
      renamedIds: n,
      isAutoDetach: i,
      scheduleTreeUpdate: s,
    },
    l = Io[A],
    c = await l(t, k);
  if (!T6(c))
    return !v(c) && W(c)
      ? { ...a, codeComponentIdentifier: c.instanceIdentifier, node: c }
      : { ...a, codeComponentIdentifier: c, node: void 0 };
}
var n6 = new Set();
async function a6(t, j) {
  let { instance: A, withUnlinkModal: e, ...r } = j,
    o = JA(t.stores.treeStore.tree, j.identifier),
    n = t.stores.insertSidebarStore.getEditBehavior(j.identifier.moduleId);
  if (e && n !== "autoUnlink" && (!o || Zr.isOn("openPrimaryForBuiltInModules"))) {
    t.stores.modalStore.set({
      type: "UnlinkComponent",
      source: "edit_action",
      ...r,
      nodeId: A?.id,
    });
    return;
  }
  if (n6.has(j.identifier)) return;
  n6.add(j.identifier);
  let i = j.skipUndoGroup !== !0;
  i &&
    t.scheduler.process(() => {
      t.beginUndoGroup();
    });
  try {
    return (await pj(t, { instance: A, enterIsolation: n !== "autoDetach", ...r }))
      ?.codeComponentIdentifier;
  } catch (s) {
    (Bj.reportError(s),
      L({
        type: "add",
        variant: "error",
        primaryText: "Failed to unlink",
        secondaryText: "component.",
        key: "external-component-unlink-error",
        icon: "error",
      }));
  } finally {
    (i &&
      t.scheduler.processWhenReady(() => {
        t.endUndoGroup();
      }),
      n6.delete(j.identifier));
  }
}
async function ho(t, j) {
  let { instance: A, moduleType: e, enterIsolation: r, codeComponentIsolationMode: o } = j,
    n = j.scheduleTreeUpdate ?? ((k) => t.scheduler.processWhenReadyAsync(k));
  if (!b(A)) return;
  let i = await a6(t, { ...j, enterIsolation: !1 });
  if (!i) return;
  await Pr(t.componentLoader, [i], t.stores.modulesStore);
  let s = t.componentLoader.componentForIdentifier(i);
  if (!s) return;
  let a = Yr(s);
  a &&
    (await n(() => {
      if (
        (t.tree
          .getNodes(Array.from(t.stores.treeStore.treeIndex.codeComponentNodeIds))
          .forEach((l) => {
            if (
              !a ||
              a.id === l.id ||
              !b(l) ||
              l.codeComponentIdentifier !== A.codeComponentIdentifier ||
              tA(l) ||
              !zr(l, { allowLockedNodes: !0 })
            )
              return;
            let c = t.cloneNode(a, !1, !1);
            Wr(t, [l], c);
          }),
        !!r)
      )
        if (e === "canvasComponent") {
          let l = vj(i);
          l && t.stores.scopeStore.select(l, { keepHistory: !1 });
        } else e === "codeFile" && ee(t, i, o);
    }));
}
function go(t, j, A) {
  if (qj(t, "canDesign")) return;
  let r = t.tree.getNodeWithTrait(j, U)?.annotation("framerVector");
  if (!FA(r)) return;
  let o = r.set.moduleId,
    i = t.tree.getNodeWithTrait(o, U)?.annotation("framerVectorSet");
  sj(i) &&
    t.stores.modalStore.set({
      type: "EditVector",
      source: "edit_action",
      vectorSetNodeId: o,
      moduleId: j,
      itemCount: i.items.length,
      instanceNodeId: A,
    });
}
var ei = cA("clipboard:modules");
async function _d(t, j, A) {
  if (!j || !Zt(j)) return !1;
  let e = "insert-external-component";
  try {
    (await ti({ engine: t, moduleURL: j, position: A, centerInParent: !0, toastKey: e }),
      L({ type: "remove", key: e }));
  } catch (r) {
    (console.error("Failed to insert external component", r),
      L({
        type: "add",
        variant: "error",
        key: e,
        primaryText: "Failed to insert",
        secondaryText: "external component.",
        icon: "error",
      }));
  }
  return !0;
}
function s6(t) {
  switch (t) {
    case "screen":
      return "web page.";
    case "vector":
      return "vector set.";
    default:
      return "external component.";
  }
}
async function k6(t, j) {
  let A = new URL(j),
    e = A.hash.substring(1).split(","),
    r = e[0] || "default",
    o = e.some((l, c) => c > 0 && l === "unlink"),
    { module: n } = await t.stores.modulesStore.lookUpModule({ url: A.href }),
    i = n.id,
    s = t.stores.modulesStore.getPersistedModuleByGlobalId(i);
  d(n.files.module, "Module file must be defined");
  let a = GA(i, n.saveId, n.files.module, r),
    k = s ? Fj(s.localId, r) : a;
  return { module: n, moduleIdentifier: k, externalIdentifier: a, insertUnlinked: o };
}
async function ti({
  engine: t,
  moduleURL: j,
  position: A,
  centerInParent: e,
  insertAsGroundNode: r,
  toastKey: o,
  update: n,
  parentId: i,
}) {
  let { module: s, moduleIdentifier: a, externalIdentifier: k, insertUnlinked: l } = await k6(t, j);
  if (s.type === "screen")
    if (Q(a)) {
      let c = vj(a.value);
      if (!c) return;
      let $ = t.tree.getNode(c);
      if (!Y($)) return;
      await t.loadScopesThenProcess([$], ([p]) => {
        p && _7(t, p, p.getPrimaryVariant());
      });
    } else
      (o &&
        L({
          type: "add",
          variant: "progress",
          key: o,
          primaryText: "Inserting",
          secondaryText: s6(s.type),
          icon: "reconnecting",
          duration: 1 / 0,
          showCloseButton: "never",
        }),
        await Lr(t, k));
  else if (l) {
    o &&
      L({
        type: "add",
        variant: "progress",
        key: o,
        primaryText: "Inserting",
        secondaryText: s6(s.type),
        icon: "reconnecting",
        duration: 1 / 0,
        showCloseButton: "never",
      });
    let c = await pj(t, { identifier: k, moduleType: s.type, enterIsolation: !1 });
    await t.scheduler.processWhenReadyAsync(() => {
      c &&
        Zj({
          engine: t,
          module: s,
          identifier: c.codeComponentIdentifier,
          position: A,
          centerInParent: e,
          insertAsGroundNode: r,
          update: n,
          parentId: i,
        });
    });
  } else {
    if (
      (o &&
        L({
          type: "add",
          variant: "progress",
          key: o,
          primaryText: "Inserting",
          secondaryText: s6(s.type),
          icon: "reconnecting",
          duration: 1 / 0,
          showCloseButton: "never",
        }),
      !V(a))
    ) {
      await t.scheduler.processWhenReadyAsync(() => {
        Zj({
          engine: t,
          module: s,
          identifier: a.value,
          position: A,
          centerInParent: e,
          insertAsGroundNode: r,
          update: n,
          parentId: i,
        });
      });
      return;
    }
    (await t.stores.modulesStore
      .addExternalModulesToProject([a], {
        onTreeUpdate: () => {
          if (
            (Zj({
              engine: t,
              module: s,
              identifier: a.value,
              position: A,
              centerInParent: e,
              insertAsGroundNode: r,
              update: n,
              parentId: i,
            }),
            s.type !== "vector")
          )
            return;
          let c = t.tree.getNodeWithTrait(s.id, U)?.annotation("framerVector");
          FA(c) &&
            t.stores.modulesStore
              .addOrUpdateVectorSetAndVectors({ moduleId: c.set.moduleId, saveId: "" })
              .catch(H);
        },
      })
      .catch(ei.reportError),
      t.stores.modulesStore.trackExternalComponentInsert([a.value], "paste-url"));
  }
  return s;
}
var _o = [oj.youtube, oj.vimeo, oj.codeblock, oj.twitter];
function vd(t, j) {
  let A = _(j);
  return Q(A)
    ? A.type === "canvasComponent"
    : V(A)
      ? t.tree.getNodeWithTrait(A.moduleId, U)?.type === "canvasComponent"
      : !1;
}
async function Pd(t, j) {
  let A = _(j);
  V(A) && (await t.stores.modulesStore.addExternalModulesToProject([A], { onTreeUpdate() {} }));
}
var bo = new WeakMap();
async function Md(t) {
  let { modules: j } = await t.stores.modulesStore.lookUpModules(_o.map(Ft)),
    A = [];
  for (let e = 0; e < j.length; e++) {
    let r = j[e];
    d(r, "Module must exist");
    let o = _o[e];
    (d(o, "Module component must exist"),
      bo.set(o, r),
      d(r.files.module, "Module must have a module file"),
      A.push(GA(r.id, r.saveId, r.files.module, o.exportSpecifier ?? "default")));
  }
  await t.stores.modulesStore.preloadExternalModules(A);
}
function Dd(t, j) {
  let A = bo.get(j);
  d(A, "Preloaded module must exist");
  let e = t.getNodeWithTrait(A.id, U);
  return e
    ? e.codeComponentIdentifier
    : (d(A.files.module, "Module must have a module file"),
      GA(A.id, A.saveId, A.files.module, j.exportSpecifier ?? "default").value);
}
async function Cd(t, j) {
  let { moduleIdentifier: A } = await k6(t, j);
  if (A.kind === "externalModuleExport") {
    let e = t.tree.getNodeWithTrait(A.moduleId, U);
    if (e) return e.codeComponentIdentifier;
  }
  return A.value;
}
function wd(t, j, A) {
  let e = j && t.getNode(j);
  if (!e || !tA(e)) return A(j, {}, !1);
  let r = Tt(t, e) ? {} : { visible: !1 },
    o = A(e.originalid, r, !0),
    n = LA.getReplicaForTemplateNode(t, e);
  return n ? (r.visible === !1 && vt(t, n, o, { visible: !0 }), ht(n.id, o)) : o;
}
var ri = /["&'<>`]/g,
  oi = /[\uD800-\uDBFF][\uDC00-\uDFFF]/g,
  ni = /[\x01-\t\v\f\x0E-\x1F\x7F\x81\x8D\x8F\x90\x9D\xA0-\uFFFF]/g,
  ii = /[|\\{}()[\]^$+*?.]/g,
  So = new WeakMap();
function yo(t, j) {
  if (((t = t.replace(j.subset ? ai(j.subset) : ri, e)), j.subset || j.escapeOnly)) return t;
  return t.replace(oi, A).replace(ni, e);
  function A(r, o, n) {
    return j.format(
      (r.charCodeAt(0) - 55296) * 1024 + r.charCodeAt(1) - 56320 + 65536,
      n.charCodeAt(o + 2),
      j
    );
  }
  function e(r, o, n) {
    return j.format(r.charCodeAt(0), n.charCodeAt(o + 1), j);
  }
}
function ai(t) {
  let j = So.get(t);
  return (j || ((j = si(t)), So.set(t, j)), j);
}
function si(t) {
  let j = [],
    A = -1;
  for (; ++A < t.length; ) j.push(t[A].replace(ii, "\\$&"));
  return new RegExp("(?:" + j.join("|") + ")", "g");
}
var ki = /[\dA-Fa-f]/;
function Oo(t, j, A) {
  let e = "&#x" + t.toString(16).toUpperCase();
  return A && j && !ki.test(String.fromCharCode(j)) ? e : e + ";";
}
var li = /\d/;
function To(t, j, A) {
  let e = "&#" + String(t);
  return A && j && !li.test(String.fromCharCode(j)) ? e : e + ";";
}
var vo = [
  "AElig",
  "AMP",
  "Aacute",
  "Acirc",
  "Agrave",
  "Aring",
  "Atilde",
  "Auml",
  "COPY",
  "Ccedil",
  "ETH",
  "Eacute",
  "Ecirc",
  "Egrave",
  "Euml",
  "GT",
  "Iacute",
  "Icirc",
  "Igrave",
  "Iuml",
  "LT",
  "Ntilde",
  "Oacute",
  "Ocirc",
  "Ograve",
  "Oslash",
  "Otilde",
  "Ouml",
  "QUOT",
  "REG",
  "THORN",
  "Uacute",
  "Ucirc",
  "Ugrave",
  "Uuml",
  "Yacute",
  "aacute",
  "acirc",
  "acute",
  "aelig",
  "agrave",
  "amp",
  "aring",
  "atilde",
  "auml",
  "brvbar",
  "ccedil",
  "cedil",
  "cent",
  "copy",
  "curren",
  "deg",
  "divide",
  "eacute",
  "ecirc",
  "egrave",
  "eth",
  "euml",
  "frac12",
  "frac14",
  "frac34",
  "gt",
  "iacute",
  "icirc",
  "iexcl",
  "igrave",
  "iquest",
  "iuml",
  "laquo",
  "lt",
  "macr",
  "micro",
  "middot",
  "nbsp",
  "not",
  "ntilde",
  "oacute",
  "ocirc",
  "ograve",
  "ordf",
  "ordm",
  "oslash",
  "otilde",
  "ouml",
  "para",
  "plusmn",
  "pound",
  "quot",
  "raquo",
  "reg",
  "sect",
  "shy",
  "sup1",
  "sup2",
  "sup3",
  "szlig",
  "thorn",
  "times",
  "uacute",
  "ucirc",
  "ugrave",
  "uml",
  "uuml",
  "yacute",
  "yen",
  "yuml",
];
var te = {
  nbsp: "\xA0",
  iexcl: "\xA1",
  cent: "\xA2",
  pound: "\xA3",
  curren: "\xA4",
  yen: "\xA5",
  brvbar: "\xA6",
  sect: "\xA7",
  uml: "\xA8",
  copy: "\xA9",
  ordf: "\xAA",
  laquo: "\xAB",
  not: "\xAC",
  shy: "\xAD",
  reg: "\xAE",
  macr: "\xAF",
  deg: "\xB0",
  plusmn: "\xB1",
  sup2: "\xB2",
  sup3: "\xB3",
  acute: "\xB4",
  micro: "\xB5",
  para: "\xB6",
  middot: "\xB7",
  cedil: "\xB8",
  sup1: "\xB9",
  ordm: "\xBA",
  raquo: "\xBB",
  frac14: "\xBC",
  frac12: "\xBD",
  frac34: "\xBE",
  iquest: "\xBF",
  Agrave: "\xC0",
  Aacute: "\xC1",
  Acirc: "\xC2",
  Atilde: "\xC3",
  Auml: "\xC4",
  Aring: "\xC5",
  AElig: "\xC6",
  Ccedil: "\xC7",
  Egrave: "\xC8",
  Eacute: "\xC9",
  Ecirc: "\xCA",
  Euml: "\xCB",
  Igrave: "\xCC",
  Iacute: "\xCD",
  Icirc: "\xCE",
  Iuml: "\xCF",
  ETH: "\xD0",
  Ntilde: "\xD1",
  Ograve: "\xD2",
  Oacute: "\xD3",
  Ocirc: "\xD4",
  Otilde: "\xD5",
  Ouml: "\xD6",
  times: "\xD7",
  Oslash: "\xD8",
  Ugrave: "\xD9",
  Uacute: "\xDA",
  Ucirc: "\xDB",
  Uuml: "\xDC",
  Yacute: "\xDD",
  THORN: "\xDE",
  szlig: "\xDF",
  agrave: "\xE0",
  aacute: "\xE1",
  acirc: "\xE2",
  atilde: "\xE3",
  auml: "\xE4",
  aring: "\xE5",
  aelig: "\xE6",
  ccedil: "\xE7",
  egrave: "\xE8",
  eacute: "\xE9",
  ecirc: "\xEA",
  euml: "\xEB",
  igrave: "\xEC",
  iacute: "\xED",
  icirc: "\xEE",
  iuml: "\xEF",
  eth: "\xF0",
  ntilde: "\xF1",
  ograve: "\xF2",
  oacute: "\xF3",
  ocirc: "\xF4",
  otilde: "\xF5",
  ouml: "\xF6",
  divide: "\xF7",
  oslash: "\xF8",
  ugrave: "\xF9",
  uacute: "\xFA",
  ucirc: "\xFB",
  uuml: "\xFC",
  yacute: "\xFD",
  thorn: "\xFE",
  yuml: "\xFF",
  fnof: "\u0192",
  Alpha: "\u0391",
  Beta: "\u0392",
  Gamma: "\u0393",
  Delta: "\u0394",
  Epsilon: "\u0395",
  Zeta: "\u0396",
  Eta: "\u0397",
  Theta: "\u0398",
  Iota: "\u0399",
  Kappa: "\u039A",
  Lambda: "\u039B",
  Mu: "\u039C",
  Nu: "\u039D",
  Xi: "\u039E",
  Omicron: "\u039F",
  Pi: "\u03A0",
  Rho: "\u03A1",
  Sigma: "\u03A3",
  Tau: "\u03A4",
  Upsilon: "\u03A5",
  Phi: "\u03A6",
  Chi: "\u03A7",
  Psi: "\u03A8",
  Omega: "\u03A9",
  alpha: "\u03B1",
  beta: "\u03B2",
  gamma: "\u03B3",
  delta: "\u03B4",
  epsilon: "\u03B5",
  zeta: "\u03B6",
  eta: "\u03B7",
  theta: "\u03B8",
  iota: "\u03B9",
  kappa: "\u03BA",
  lambda: "\u03BB",
  mu: "\u03BC",
  nu: "\u03BD",
  xi: "\u03BE",
  omicron: "\u03BF",
  pi: "\u03C0",
  rho: "\u03C1",
  sigmaf: "\u03C2",
  sigma: "\u03C3",
  tau: "\u03C4",
  upsilon: "\u03C5",
  phi: "\u03C6",
  chi: "\u03C7",
  psi: "\u03C8",
  omega: "\u03C9",
  thetasym: "\u03D1",
  upsih: "\u03D2",
  piv: "\u03D6",
  bull: "\u2022",
  hellip: "\u2026",
  prime: "\u2032",
  Prime: "\u2033",
  oline: "\u203E",
  frasl: "\u2044",
  weierp: "\u2118",
  image: "\u2111",
  real: "\u211C",
  trade: "\u2122",
  alefsym: "\u2135",
  larr: "\u2190",
  uarr: "\u2191",
  rarr: "\u2192",
  darr: "\u2193",
  harr: "\u2194",
  crarr: "\u21B5",
  lArr: "\u21D0",
  uArr: "\u21D1",
  rArr: "\u21D2",
  dArr: "\u21D3",
  hArr: "\u21D4",
  forall: "\u2200",
  part: "\u2202",
  exist: "\u2203",
  empty: "\u2205",
  nabla: "\u2207",
  isin: "\u2208",
  notin: "\u2209",
  ni: "\u220B",
  prod: "\u220F",
  sum: "\u2211",
  minus: "\u2212",
  lowast: "\u2217",
  radic: "\u221A",
  prop: "\u221D",
  infin: "\u221E",
  ang: "\u2220",
  and: "\u2227",
  or: "\u2228",
  cap: "\u2229",
  cup: "\u222A",
  int: "\u222B",
  there4: "\u2234",
  sim: "\u223C",
  cong: "\u2245",
  asymp: "\u2248",
  ne: "\u2260",
  equiv: "\u2261",
  le: "\u2264",
  ge: "\u2265",
  sub: "\u2282",
  sup: "\u2283",
  nsub: "\u2284",
  sube: "\u2286",
  supe: "\u2287",
  oplus: "\u2295",
  otimes: "\u2297",
  perp: "\u22A5",
  sdot: "\u22C5",
  lceil: "\u2308",
  rceil: "\u2309",
  lfloor: "\u230A",
  rfloor: "\u230B",
  lang: "\u2329",
  rang: "\u232A",
  loz: "\u25CA",
  spades: "\u2660",
  clubs: "\u2663",
  hearts: "\u2665",
  diams: "\u2666",
  quot: '"',
  amp: "&",
  lt: "<",
  gt: ">",
  OElig: "\u0152",
  oelig: "\u0153",
  Scaron: "\u0160",
  scaron: "\u0161",
  Yuml: "\u0178",
  circ: "\u02C6",
  tilde: "\u02DC",
  ensp: "\u2002",
  emsp: "\u2003",
  thinsp: "\u2009",
  zwnj: "\u200C",
  zwj: "\u200D",
  lrm: "\u200E",
  rlm: "\u200F",
  ndash: "\u2013",
  mdash: "\u2014",
  lsquo: "\u2018",
  rsquo: "\u2019",
  sbquo: "\u201A",
  ldquo: "\u201C",
  rdquo: "\u201D",
  bdquo: "\u201E",
  dagger: "\u2020",
  Dagger: "\u2021",
  permil: "\u2030",
  lsaquo: "\u2039",
  rsaquo: "\u203A",
  euro: "\u20AC",
};
var Po = ["cent", "copy", "divide", "gt", "lt", "not", "para", "times"];
var Mo = {}.hasOwnProperty,
  l6 = {},
  re;
for (re in te) Mo.call(te, re) && (l6[te[re]] = re);
var ci = /[^\dA-Za-z]/;
function Do(t, j, A, e) {
  let r = String.fromCharCode(t);
  if (Mo.call(l6, r)) {
    let o = l6[r],
      n = "&" + o;
    return A &&
      vo.includes(o) &&
      !Po.includes(o) &&
      (!e || (j && j !== 61 && ci.test(String.fromCharCode(j))))
      ? n
      : n + ";";
  }
  return "";
}
function Co(t, j, A) {
  let e = Oo(t, j, A.omitOptionalSemicolons),
    r;
  if (
    ((A.useNamedReferences || A.useShortestReferences) &&
      (r = Do(t, j, A.omitOptionalSemicolons, A.attribute)),
    (A.useShortestReferences || !r) && A.useShortestReferences)
  ) {
    let o = To(t, j, A.omitOptionalSemicolons);
    o.length < e.length && (e = o);
  }
  return r && (!A.useShortestReferences || r.length < e.length) ? r : e;
}
function mA(t, j) {
  return yo(t, Object.assign({ format: Co }, j));
}
var $i = /^>|^->|<!--|-->|--!>|<!-$/g,
  di = [">"],
  Ei = ["<", ">"];
function No(t, j, A, e) {
  return e.settings.bogusComments
    ? "<?" + mA(t.value, Object.assign({}, e.settings.characterReferences, { subset: di })) + ">"
    : "<!--" + t.value.replace($i, r) + "-->";
  function r(o) {
    return mA(o, Object.assign({}, e.settings.characterReferences, { subset: Ei }));
  }
}
function xo(t, j, A, e) {
  return (
    "<!" +
    (e.settings.upperDoctype ? "DOCTYPE" : "doctype") +
    (e.settings.tightDoctype ? "" : " ") +
    "html>"
  );
}
var K = qo(1),
  c6 = qo(-1),
  Bi = [];
function qo(t) {
  return j;
  function j(A, e, r) {
    let o = A ? A.children : Bi,
      n = (e || 0) + t,
      i = o[n];
    if (!r) for (; i && HA(i); ) ((n += t), (i = o[n]));
    return i;
  }
}
var pi = {}.hasOwnProperty;
function oe(t) {
  return j;
  function j(A, e, r) {
    return pi.call(t, A.tagName) && t[A.tagName](A, e, r);
  }
}
var mj = oe({
  body: ui,
  caption: $6,
  colgroup: $6,
  dd: fi,
  dt: Fi,
  head: $6,
  html: mi,
  li: Ri,
  optgroup: Ii,
  option: hi,
  p: Gi,
  rp: Ko,
  rt: Ko,
  tbody: _i,
  td: Lo,
  tfoot: bi,
  th: Lo,
  thead: gi,
  tr: Si,
});
function $6(t, j, A) {
  let e = K(A, j, !0);
  return !e || (e.type !== "comment" && !(e.type === "text" && HA(e.value.charAt(0))));
}
function mi(t, j, A) {
  let e = K(A, j);
  return !e || e.type !== "comment";
}
function ui(t, j, A) {
  let e = K(A, j);
  return !e || e.type !== "comment";
}
function Gi(t, j, A) {
  let e = K(A, j);
  return e
    ? e.type === "element" &&
        (e.tagName === "address" ||
          e.tagName === "article" ||
          e.tagName === "aside" ||
          e.tagName === "blockquote" ||
          e.tagName === "details" ||
          e.tagName === "div" ||
          e.tagName === "dl" ||
          e.tagName === "fieldset" ||
          e.tagName === "figcaption" ||
          e.tagName === "figure" ||
          e.tagName === "footer" ||
          e.tagName === "form" ||
          e.tagName === "h1" ||
          e.tagName === "h2" ||
          e.tagName === "h3" ||
          e.tagName === "h4" ||
          e.tagName === "h5" ||
          e.tagName === "h6" ||
          e.tagName === "header" ||
          e.tagName === "hgroup" ||
          e.tagName === "hr" ||
          e.tagName === "main" ||
          e.tagName === "menu" ||
          e.tagName === "nav" ||
          e.tagName === "ol" ||
          e.tagName === "p" ||
          e.tagName === "pre" ||
          e.tagName === "section" ||
          e.tagName === "table" ||
          e.tagName === "ul")
    : !A ||
        !(
          A.type === "element" &&
          (A.tagName === "a" ||
            A.tagName === "audio" ||
            A.tagName === "del" ||
            A.tagName === "ins" ||
            A.tagName === "map" ||
            A.tagName === "noscript" ||
            A.tagName === "video")
        );
}
function Ri(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && e.tagName === "li");
}
function Fi(t, j, A) {
  let e = K(A, j);
  return !!(e && e.type === "element" && (e.tagName === "dt" || e.tagName === "dd"));
}
function fi(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && (e.tagName === "dt" || e.tagName === "dd"));
}
function Ko(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && (e.tagName === "rp" || e.tagName === "rt"));
}
function Ii(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && e.tagName === "optgroup");
}
function hi(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && (e.tagName === "option" || e.tagName === "optgroup"));
}
function gi(t, j, A) {
  let e = K(A, j);
  return !!(e && e.type === "element" && (e.tagName === "tbody" || e.tagName === "tfoot"));
}
function _i(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && (e.tagName === "tbody" || e.tagName === "tfoot"));
}
function bi(t, j, A) {
  return !K(A, j);
}
function Si(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && e.tagName === "tr");
}
function Lo(t, j, A) {
  let e = K(A, j);
  return !e || (e.type === "element" && (e.tagName === "td" || e.tagName === "th"));
}
var wo = oe({ body: Ti, colgroup: vi, head: Oi, html: yi, tbody: Pi });
function yi(t) {
  let j = K(t, -1);
  return !j || j.type !== "comment";
}
function Oi(t) {
  let j = new Set();
  for (let e of t.children)
    if (e.type === "element" && (e.tagName === "base" || e.tagName === "title")) {
      if (j.has(e.tagName)) return !1;
      j.add(e.tagName);
    }
  let A = t.children[0];
  return !A || A.type === "element";
}
function Ti(t) {
  let j = K(t, -1, !0);
  return (
    !j ||
    (j.type !== "comment" &&
      !(j.type === "text" && HA(j.value.charAt(0))) &&
      !(
        j.type === "element" &&
        (j.tagName === "meta" ||
          j.tagName === "link" ||
          j.tagName === "script" ||
          j.tagName === "style" ||
          j.tagName === "template")
      ))
  );
}
function vi(t, j, A) {
  let e = c6(A, j),
    r = K(t, -1, !0);
  return A &&
    e &&
    e.type === "element" &&
    e.tagName === "colgroup" &&
    mj(e, A.children.indexOf(e), A)
    ? !1
    : !!(r && r.type === "element" && r.tagName === "col");
}
function Pi(t, j, A) {
  let e = c6(A, j),
    r = K(t, -1);
  return A &&
    e &&
    e.type === "element" &&
    (e.tagName === "thead" || e.tagName === "tbody") &&
    mj(e, A.children.indexOf(e), A)
    ? !1
    : !!(r && r.type === "element" && r.tagName === "tr");
}
var ne = {
  name: [
    [
      `	
\f\r &/=>`.split(""),
      `	
\f\r "&'/=>\``.split(""),
    ],
    [
      `\0	
\f\r "&'/<=>`.split(""),
      `\0	
\f\r "&'/<=>\``.split(""),
    ],
  ],
  unquoted: [
    [
      `	
\f\r &>`.split(""),
      `\0	
\f\r "&'<=>\``.split(""),
    ],
    [
      `\0	
\f\r "&'<=>\``.split(""),
      `\0	
\f\r "&'<=>\``.split(""),
    ],
  ],
  single: [
    ["&'".split(""), "\"&'`".split("")],
    ["\0&'".split(""), "\0\"&'`".split("")],
  ],
  double: [
    ['"&'.split(""), "\"&'`".split("")],
    ['\0"&'.split(""), "\0\"&'`".split("")],
  ],
};
function Jo(t, j, A, e) {
  let r = e.schema,
    o = r.space === "svg" ? !1 : e.settings.omitOptionalTags,
    n =
      r.space === "svg"
        ? e.settings.closeEmptyElements
        : e.settings.voids.includes(t.tagName.toLowerCase()),
    i = [],
    s;
  r.space === "html" && t.tagName === "svg" && (e.schema = yj);
  let a = Mi(e, t.properties),
    k = e.all(r.space === "html" && t.tagName === "template" ? t.content : t);
  return (
    (e.schema = r),
    k && (n = !1),
    (a || !o || !wo(t, j, A)) &&
      (i.push("<", t.tagName, a ? " " + a : ""),
      n &&
        (r.space === "svg" || e.settings.closeSelfClosing) &&
        ((s = a.charAt(a.length - 1)),
        (!e.settings.tightSelfClosing || s === "/" || (s && s !== '"' && s !== "'")) && i.push(" "),
        i.push("/")),
      i.push(">")),
    i.push(k),
    !n && (!o || !mj(t, j, A)) && i.push("</" + t.tagName + ">"),
    i.join("")
  );
}
function Mi(t, j) {
  let A = [],
    e = -1,
    r;
  if (j) {
    for (r in j)
      if (j[r] !== null && j[r] !== void 0) {
        let o = Di(t, r, j[r]);
        o && A.push(o);
      }
  }
  for (; ++e < A.length; ) {
    let o = t.settings.tightAttributes ? A[e].charAt(A[e].length - 1) : void 0;
    e !== A.length - 1 && o !== '"' && o !== "'" && (A[e] += " ");
  }
  return A.join("");
}
function Di(t, j, A) {
  let e = ct(t.schema, j),
    r = t.settings.allowParseErrors && t.schema.space === "html" ? 0 : 1,
    o = t.settings.allowDangerousCharacters ? 0 : 1,
    n = t.quote,
    i;
  if (
    (e.overloadedBoolean && (A === e.attribute || A === "")
      ? (A = !0)
      : (e.boolean || e.overloadedBoolean) &&
        (typeof A != "string" || A === e.attribute || A === "") &&
        (A = !!A),
    A == null || A === !1 || (typeof A == "number" && Number.isNaN(A)))
  )
    return "";
  let s = mA(
    e.attribute,
    Object.assign({}, t.settings.characterReferences, { subset: ne.name[r][o] })
  );
  return A === !0 ||
    ((A = Array.isArray(A)
      ? (e.commaSeparated ? dt : Et)(A, { padLeft: !t.settings.tightCommaSeparatedLists })
      : String(A)),
    t.settings.collapseEmptyAttributes && !A)
    ? s
    : (t.settings.preferUnquoted &&
        (i = mA(
          A,
          Object.assign({}, t.settings.characterReferences, {
            attribute: !0,
            subset: ne.unquoted[r][o],
          })
        )),
      i !== A &&
        (t.settings.quoteSmart && Xe(A, n) > Xe(A, t.alternative) && (n = t.alternative),
        (i =
          n +
          mA(
            A,
            Object.assign({}, t.settings.characterReferences, {
              subset: (n === "'" ? ne.single : ne.double)[r][o],
              attribute: !0,
            })
          ) +
          n)),
      s + (i && "=" + i));
}
var Ci = ["<", "&"];
function ie(t, j, A, e) {
  return A && A.type === "element" && (A.tagName === "script" || A.tagName === "style")
    ? t.value
    : mA(t.value, Object.assign({}, e.settings.characterReferences, { subset: Ci }));
}
function Ho(t, j, A, e) {
  return e.settings.allowDangerousHtml ? t.value : ie(t, j, A, e);
}
function Vo(t, j, A, e) {
  return e.all(t);
}
var Uo = Bt("type", {
  invalid: Ni,
  unknown: xi,
  handlers: { comment: No, doctype: xo, element: Jo, raw: Ho, root: Vo, text: ie },
});
function Ni(t) {
  throw new Error("Expected node, not `" + t + "`");
}
function xi(t) {
  let j = t;
  throw new Error("Cannot compile unknown node `" + j.type + "`");
}
var qi = {},
  Ki = {},
  Li = [];
function d6(t, j) {
  let A = j || qi,
    e = A.quote || '"',
    r = e === '"' ? "'" : '"';
  if (e !== '"' && e !== "'") throw new Error("Invalid quote `" + e + "`, expected `'` or `\"`");
  return {
    one: wi,
    all: Ji,
    settings: {
      omitOptionalTags: A.omitOptionalTags || !1,
      allowParseErrors: A.allowParseErrors || !1,
      allowDangerousCharacters: A.allowDangerousCharacters || !1,
      quoteSmart: A.quoteSmart || !1,
      preferUnquoted: A.preferUnquoted || !1,
      tightAttributes: A.tightAttributes || !1,
      upperDoctype: A.upperDoctype || !1,
      tightDoctype: A.tightDoctype || !1,
      bogusComments: A.bogusComments || !1,
      tightCommaSeparatedLists: A.tightCommaSeparatedLists || !1,
      tightSelfClosing: A.tightSelfClosing || !1,
      collapseEmptyAttributes: A.collapseEmptyAttributes || !1,
      allowDangerousHtml: A.allowDangerousHtml || !1,
      voids: A.voids || R7,
      characterReferences: A.characterReferences || Ki,
      closeSelfClosing: A.closeSelfClosing || !1,
      closeEmptyElements: A.closeEmptyElements || !1,
    },
    schema: A.space === "svg" ? yj : $t,
    quote: e,
    alternative: r,
  }.one(Array.isArray(t) ? { type: "root", children: t } : t, void 0, void 0);
}
function wi(t, j, A) {
  return Uo(t, j, A, this);
}
function Ji(t) {
  let j = [],
    A = (t && t.children) || Li,
    e = -1;
  for (; ++e < A.length; ) j[e] = this.one(A[e], e, t);
  return j.join("");
}
function ae(t) {
  let j = this,
    A = { ...j.data("settings"), ...t };
  j.compiler = e;
  function e(r) {
    return d6(r, A);
  }
}
function E6(t) {
  F7(t, [/\r?\n|\r/g, Hi]);
}
function Hi() {
  return { type: "break" };
}
function se() {
  return function (t) {
    E6(t);
  };
}
function ke(t) {
  let j = this;
  j.compiler = A;
  function A(e) {
    return G7(e, { ...j.data("settings"), ...t, extensions: j.data("toMarkdownExtensions") || [] });
  }
}
var Vi = /^(#{1,6})\s+(.+)$/m,
  Ui = /\*\*(.*?)\*\*/,
  Xi = /\*(.*?)\*/,
  Qi = /^\s*[-*+]\s+(.+)$/m,
  zi = /^\s*(\d+)\.\s+(.+)$/m,
  Wi = /^>\s+(.+)$/m,
  Yi = /\[([^\]]+)\]\((https?:\/\/[^)]+)\)/,
  Zi = /`([^`]+)`/,
  Aa = /```[\s\S]*?```/,
  ja = /^\s*\|(.+\|)+\s*$/m,
  ea = /~~(.*?)~~/,
  ta = /^\s*[-*+]\s+\[[ x]\]\s+\S/m,
  ra = /^(-{3,}|_{3,})\s*$/m,
  oa = /!\[([^\]]*)\]\((https?:\/\/[^)]+)\)/,
  na = /<(https?:\/\/[^>]+)>/;
function FB(t) {
  return !t || t.length === 0
    ? !1
    : [Vi, Ui, Xi, Qi, zi, Wi, Yi, Zi, Aa, ja, ea, ta, ra, oa, na].some((A) => A.test(t));
}
function ia(t) {
  return ze()
    .use(f7)
    .use(se)
    .use(Qe)
    .use(I7, {
      allowDangerousHtml: !0,
      handlers: {
        break: function (A, e) {
          let r = { type: "element", tagName: "br", properties: {}, children: [] };
          return (A.patch(e, r), [A.applyData(e, r)]);
        },
      },
    })
    .use(ae, { allowDangerousHtml: !0 })
    .processSync(t)
    .toString();
}
function fB(t, j) {
  let A = ia(t),
    e = document.createElement("div");
  return ((e.innerHTML = A), lt.fromSchema(j).parseSlice(e, { preserveWhitespace: !0 }));
}
function IB(t, j, A) {
  let e = {},
    r;
  for (r in A.nodes) {
    let s = A.nodes[r].spec.toMarkdown;
    e[r] = (...a) => s(...a, j);
  }
  let o = {},
    n;
  for (n in A.marks) {
    let s = A.marks[n].spec.toMarkdown;
    o[n] = (...a) => s(...a, j);
  }
  let i = pt(t, { schema: A, nodeHandlers: e, markHandlers: o });
  return ze()
    .use(Qe)
    .use(ke, {
      handlers: {
        text(...s) {
          let [a] = s;
          return a.data?.avoidEscaping ? a.value : u7.text(...s);
        },
      },
      bullet: "-",
      emphasis: "_",
    })
    .stringify(i);
}
function B6(t) {
  return t.type === "existing" || t.type === "privateDraft";
}
function PB({ stackElement: t, tree: j }) {
  if (!t) return null;
  let A = j.getNodeWithTrait(t.collectionId, $A);
  if (A) return A;
  let e = t.type === "arrayItem" ? t.collectionItemId : t.itemId,
    r = j.getNodeWithTrait(e, N);
  return r?.parentid ? j.getNodeWithTrait(r.parentid, $A) : null;
}
function sa({ treeStore: t, stackElement: j }) {
  if (!j) return null;
  if (j.type === "existing" || j.type === "privateDraft")
    return t.tree.getNodeWithTrait(j.itemId, N);
  if (j.type === "arrayItem") return t.tree.getNodeWithTrait(j.collectionItemId, N) ?? null;
  aA(j);
}
function MB({ treeStore: t, stackElement: j }) {
  return !j || !p6(j) ? null : sa({ treeStore: t, stackElement: j });
}
function DB(t) {
  return t.findLast(B6) ?? null;
}
function CB(t) {
  return t.reduce((j, A) => (B6(A) ? j + 1 : j), 0);
}
function NB(t, j = []) {
  let A = t.stores.selectionStore.ids.length === 1 ? t.stores.selectionStore.ids.at(0) : void 0,
    e = ka(t, j),
    r = e.at(0),
    o = t.tree.getNodeWithTrait(A, N),
    n = o && $e(t, o) ? o : null;
  if (n) {
    if (!r) return [Xo(t, n)];
    if (r.type === "existing")
      return r.itemId === n.id
        ? e
        : (d(e.length === 1, "Must dismiss overlay stack before changing selection"), [Xo(t, n)]);
    if (r.type === "privateDraft") {
      if (r.itemId === n.id) return e;
      throw new Error("Must save private draft item stack before selecting a collection item");
    }
    if (r.type === "arrayItem") return e;
    aA(r);
  }
  if (!r || r.type === "privateDraft") return e;
  if (r.type === "existing")
    return t.stores.chromeStore.mainView !== 2
      ? e
      : (d(e.length === 1, "Must dismiss overlay stack before de-selecting collection item"), []);
  if (r.type === "arrayItem") return e;
  aA(r);
}
function ka(t, j) {
  let A = [];
  for (let e = 0; e < j.length; e++) {
    let r = j[e];
    if (!r) continue;
    if (r.type === "existing") {
      let i = t.tree.getNodeWithTrait(r.itemId, N);
      i && ce(t, i)
        ? (r = le(t, i, { saveAction: r.saveAction, showErrors: !1 }))
        : i?.parentid &&
          r.collectionId !== i.parentid &&
          (r = le(t, i, { saveAction: r.saveAction, showErrors: !1 }));
    }
    if (r.type === "privateDraft") {
      let i = t.tree.getNodeWithTrait(r.itemId, N);
      i && $e(t, i) && !ce(t, i) && (r = le(t, i, { saveAction: r.saveAction, showErrors: !1 }));
    }
    let o = A.at(-1) ?? null;
    if (!la(t, r, o) || (e > 0 && p6(r) && !r.saveAction)) break;
    A.push(r);
  }
  return A;
}
function la(t, j, A) {
  if (j.type === "existing") {
    let e = t.tree.getNodeWithTrait(j.itemId, N);
    return e ? !!e.parentid && $e(t, e) : !1;
  }
  if (j.type === "privateDraft") {
    let e = t.tree.getNodeWithTrait(j.itemId, N);
    return e ? e.parentid === j.collectionId && ce(t, e) : !1;
  }
  if (j.type === "arrayItem") {
    if (
      !A ||
      !B6(A) ||
      A.itemId !== j.collectionItemId ||
      (p6(A) && A.collectionId !== j.collectionId)
    )
      return !1;
    let e = t.tree.getNodeWithTrait(j.collectionId, $A);
    if (!e) return !1;
    let r = t.tree.getNodeWithTrait(j.collectionItemId, N);
    if (!r || !$e(t, r) || (r.parentid && r.parentid !== j.collectionId)) return !1;
    let o = e.getVariable(j.arrayFieldId);
    if (!o || !ot(o)) return !1;
    let n = r.getControlProp(j.arrayFieldId);
    return !n || !jt(n) || !ZA(n.value)
      ? !1
      : n.value.some(({ type: i, id: s }) => i === "object" && s === j.arrayItemId);
  }
  aA(j);
}
function ce(t, j) {
  return gr(j, { currentUserId: t.stores.sessionStore.user.id });
}
function $e(t, j) {
  return _r(j, { currentUserId: t.stores.sessionStore.user.id });
}
function p6(t) {
  return t.type === "privateDraft";
}
function Xo(t, j) {
  return le(t, j, { showErrors: !1 });
}
function le(t, j, A) {
  if (j.parentid && ce(t, j)) {
    let r = {
      collectionId: j.parentid,
      itemId: j.id,
      showErrors: A.showErrors,
      type: "privateDraft",
    };
    return (
      A.saveAction && (r.saveAction = A.saveAction),
      A.attemptedClose && (r.attemptedClose = A.attemptedClose),
      r
    );
  }
  d(j.parentid, "Existing collection item must have a parent collection");
  let e = { collectionId: j.parentid, itemId: j.id, type: "existing" };
  return (A.saveAction && (e.saveAction = A.saveAction), e);
}
function LB(t) {
  let { newContentAsDraft: j } = t.stores.persistedUserDefaults;
  return !(!j || t.stores.publishStore.publishStatus === 1);
}
function ca(t, j, A) {
  let e = t.reactComponentForIdentifier(j.codeComponentIdentifier);
  if (!e) return;
  let r = e.properties[A.controlPropKey];
  return r ? (Sj(r, j.getRawControlProp(A.controlPropKey)) ?? void 0) : void 0;
}
function $a(t, j, A) {
  if (!st(j)) return;
  let e = t.reactComponentForIdentifier(j.layoutTemplateIdentifier);
  if (!e) return;
  let r = e.properties[A.controlPropKey];
  return r ? (Sj(r, j.getRawControlProp(A.controlPropKey)) ?? void 0) : void 0;
}
function da(t, j, A, e) {
  let n = t.getNodeParent(A).getPropertyControls(t, j)[e.controlPropKey];
  if (!n) return;
  let i = A.getControlProp(e.controlPropKey);
  return Sj(n, i) ?? void 0;
}
function Wo(t, j, A, e) {
  if (Y(A, !0)) return $a(j, A, e);
  if (N(A)) return da(t, j, A, e);
  if (b(A)) return ca(j, A, e);
}
function uj(t, j, A, e) {
  let r = Wo(t, j, A, e);
  if (!r) return;
  if (!e.ancestorControlKeyPath) return r;
  let o = Je(e.ancestorControlKeyPath);
  for (let n of o) {
    if (!r) return;
    if ((d(r.type === n.type), r.type === "object")) {
      if ((d(n.type === "object"), !yA(r.value))) return;
      r = r.value[n.key];
    } else if (r.type === "array") {
      if ((d(n.type === "array"), !ZA(r.value))) return;
      r = r.value.find((i) => i.id === n.id);
    } else {
      return;
    }
  }
  return r;
}
function Gj(t, j, A, e, r) {
  if (!e.ancestorControlKeyPath) {
    It(A)
      ? A.setControlProp(e.controlPropKey, r)
      : N(A)
        ? (d(He(r), "Invalid control prop"), A.setControlProp(e.controlPropKey, r))
        : aA(A, "Unsupported node");
    return;
  }
  d(Y(A, !0) || b(A) || N(A));
  function o(a) {
    if (a) return a.type === "array" || a.type === "object" ? { ...a } : (d(a.type === r.type), r);
  }
  let n = o(Wo(t, j, A, e));
  if (!n) return;
  let i = Je(e.ancestorControlKeyPath),
    s = n;
  for (let a of i) {
    if (!s) return;
    if ((d(s.type === a.type), s.type === "object")) {
      if ((d(a.type === "object", "Current control should be of type Object"), !yA(s.value)))
        return;
      let k = s.value[a.key],
        l = o(k);
      if (!l) return;
      ((s.value = { ...s.value, [a.key]: l }), (s = l));
    } else if (s.type === "array") {
      if ((d(a.type === "array", "Current control should be of type Array"), !ZA(s.value))) return;
      let k = s.value.find((c) => c.id === a.id),
        l = o(k);
      if (!l) return;
      ((s.value = s.value.map((c) => (c.id === l.id ? l : c))), (s = l));
    } else {
      return;
    }
  }
  Y(A, !0) || b(A)
    ? A.setControlProp(e.controlPropKey, n)
    : N(A)
      ? (d(He(n), "Invalid control prop"), A.setControlProp(e.controlPropKey, n))
      : aA(A, "Unsupported node");
}
function P(t, j, A, e) {
  let r = j ? j[t] : void 0,
    o = e(r, A);
  if (!(!j && !o)) {
    if (!o) {
      d(j);
      let n = Object.keys(j),
        i = n.length;
      if (i === 0 || (i === 1 && n[0] === t)) return;
      let s = { ...j };
      return (delete s[t], s);
    }
    return { ...j, [t]: o };
  }
}
function m6(t, j, A, e, r) {
  function o(n) {
    function i(s) {
      return s.name === e.type;
    }
    return {
      ...n,
      transforms: n.transforms.map((s) => (s.id !== e.transformId || !i(s) ? s : r(s))),
    };
  }
  if (TA(A) && fj(A.textContent)) {
    let n = o(A.textContent);
    A.set({ textContent: n });
    return;
  }
  if (he(A) && fj(A.formInputPlaceholder)) {
    let n = o(A.formInputPlaceholder);
    A.set({ formInputPlaceholder: n });
    return;
  }
  if (b(A)) {
    let n = uj(j, t.componentLoader, A, e);
    if (!n || (d(n.type === "string"), !fj(n.value))) return;
    let i = { ...n };
    ((i.value = o(n.value)), Gj(j, t.componentLoader, A, e, i));
  }
}
function Yo(t, j, A, e, r, o) {
  t.scheduler.process(() => {
    let n = t.stores.treeStore.getDataTreeOrPartialTree(),
      i = n.get(j.nodeId),
      s = j.type;
    switch (s) {
      case "string":
      case "richtext":
      case "link": {
        if (!N(i) && !b(i) && !Y(i)) return;
        let a = uj(n, t.componentLoader, i, j);
        if (!a) break;
        d(s === a.type);
        let k = { ...a },
          l = P(A.id, a.valueLocalized, "default", r);
        ((k.valueLocalized = l), Gj(n, t.componentLoader, i, j, k));
        break;
      }
      case "slug": {
        if (!N(i)) return;
        let a = uj(n, t.componentLoader, i, j);
        d(a?.type === "string", "expected slug control type to be a ControlType.String");
        let k = { ...a },
          l = P(A.id, a.valueLocalized, "default", r);
        ((k.valueLocalized = l), Gj(n, t.componentLoader, i, j, k));
        break;
      }
      case "image": {
        if (!N(i) && !b(i) && !Y(i)) return;
        let a = uj(n, t.componentLoader, i, j);
        if (!a) break;
        d(s === a.type);
        let k = { ...a };
        if (e === "imageSrc" || e === "all") {
          let l = P(A.id, a.valueLocalized, "imageSrc", r);
          k.valueLocalized = l;
        }
        if (e === "default" || e === "all") {
          let l = P(A.id, a.altLocalized, "default", r);
          k.altLocalized = l;
        }
        Gj(n, t.componentLoader, i, j, k);
        break;
      }
      case "enum": {
        if (!$A(i)) return;
        let a = i.variables.map((k) => {
          if (k.id !== j.variableId) return k;
          d(k.type === j.type);
          let l = k.cases.map((c) => {
            if (c.id !== j.enumCaseId) return c;
            let $ = P(A.id, c.nameLocalized, "default", r);
            return { ...c, nameLocalized: $ };
          });
          return { ...k, cases: l };
        });
        i.set({ variables: a });
        break;
      }
      case "RootNode":
      case "WebPageNode": {
        if (!sr(i)) return;
        let a = j.key === "title" ? "titleLocalized" : "descriptionLocalized",
          k = i[a],
          l = P(A.id, k, "default", r);
        i.set({ [a]: l });
        break;
      }
      case "RichTextNode": {
        if (!TA(i)) return;
        let a = P(A.id, i.htmlLocalized, "default", r);
        (i.set({ htmlLocalized: a }), Kt(i) && Oe(t) && cr(t, lr(t, [i.id])).catch(H));
        break;
      }
      case "FrameNode": {
        if (!OA(i)) return;
        if (e === "imageSrc" || e === "all") {
          let a = P(A.id, i.fillImageLocalized, "imageSrc", r);
          i.set({ fillImageLocalized: a });
        }
        if (e === "default" || e === "all") {
          let a = P(A.id, i.altAttributeLocalized, "default", r);
          i.set({ altAttributeLocalized: a });
        }
        break;
      }
      case "nodeLink": {
        if (!Jt(i)) break;
        let a = P(A.id, i.linkLocalized, "default", r);
        i.set({ linkLocalized: a });
        break;
      }
      case "prefix":
      case "suffix": {
        if (!i) break;
        m6(t, n, i, j, (a) => {
          let k = { ...a },
            l = P(A.id, a.valueLocalized, "default", r);
          return ((k.valueLocalized = l), k);
        });
        break;
      }
      case "convertFromBoolean": {
        if (!i) break;
        m6(t, n, i, j, (a) => {
          let k = { ...a };
          return (
            j.property === "truthy" &&
              (k.truthyLocalized = P(A.id, a.truthyLocalized, "default", r)),
            j.property === "falsy" && (k.falsyLocalized = P(A.id, a.falsyLocalized, "default", r)),
            j.property === "fallback" &&
              a.fallback &&
              (k.fallback = {
                ...a.fallback,
                valueLocalized: P(A.id, a.fallback.valueLocalized, "default", r),
              }),
            k
          );
        });
        break;
      }
      case "convertFromEnum": {
        if (!i) break;
        m6(t, n, i, j, (a) => {
          let k = { ...a };
          return j.isDefaultCase
            ? ((k.defaultLocalized = P(A.id, a.defaultLocalized, "default", r)), k)
            : ((k.cases = a.cases.map((l) => {
                if (l.id !== j.caseId) return l;
                let c = { ...l },
                  $ = P(A.id, l.toLocalized, "default", r);
                return ((c.toLocalized = $), c);
              })),
              k);
        });
        break;
      }
      case "FormPlainTextInputNode": {
        if (!he(i)) return;
        i.set({
          formInputPlaceholderLocalized: P(A.id, i.formInputPlaceholderLocalized, "default", r),
        });
        break;
      }
      case "FormSelectNode": {
        if (!ge(i) || !i.formSelectOptions) return;
        let { optionId: a } = j,
          k = i.formSelectOptions.map((l) => {
            if (l.id !== a) return l;
            let c = P(A.id, l.titleLocalized, "default", r);
            return { ...l, titleLocalized: c };
          });
        i.set({ formSelectOptions: k });
        break;
      }
      case "formSelectGenericOption": {
        if (!ge(i)) return;
        let { key: a } = j;
        switch (a) {
          case "allItemsLabel": {
            let k = P(A.id, i.allItemsLabelLocalized, "default", r);
            i.set({ allItemsLabelLocalized: k });
            break;
          }
          case "booleanTrueLabel": {
            let k = P(A.id, i.booleanTrueLabelLocalized, "default", r);
            i.set({ booleanTrueLabelLocalized: k });
            break;
          }
          case "booleanFalseLabel": {
            let k = P(A.id, i.booleanFalseLabelLocalized, "default", r);
            i.set({ booleanFalseLabelLocalized: k });
            break;
          }
          default:
            aA(a);
        }
        break;
      }
      case "RouteSegmentNode": {
        if (!Q6(i)) return;
        let a = i.segmentLocalized,
          k = P(A.id, a, "default", r);
        i.set({ segmentLocalized: k });
        break;
      }
      default:
    }
  }, o);
}
function Ea(t, j, A, e, r) {
  (d(
    wj(j),
    "RichTextDocument localized values are only supported for rich-text control prop sources"
  ),
    t.scheduler.process(() => {
      let o = t.stores.treeStore.getDataTreeOrPartialTree(),
        n = o.get(j.nodeId);
      if (!N(n) && !b(n) && !Y(n)) return;
      let i = uj(o, t.componentLoader, n, j);
      if (!i) return;
      d(i.type === "richtext");
      let s = structuredClone(i);
      ((s.valueLocalized = P(A.id, i.valueLocalized, "default", e)),
        Gj(o, t.componentLoader, n, j, s));
    }, r));
}
function Qo(
  t,
  j,
  A,
  e,
  r,
  {
    needsReview: o,
    generatedByAI: n,
    pluginId: i,
    pluginMode: s,
    ignoreTracking: a,
    trackingSource: k,
  }
) {
  let l = { value: t, hash: Ba(j, A), lastEdited: Date.now() };
  o && (l.needsReview = !0);
  let c = O6(r?.value, t);
  return (
    (n || (c && r?.generatedByAI)) && (l.generatedByAI = !0),
    i || Re(s)
      ? (d(i && Re(s), "Cannot set only pluginId or pluginMode"),
        (l.pluginId = i),
        (l.pluginMode = s))
      : c &&
        r?.pluginId &&
        ((l.pluginId = r.pluginId), r.pluginMode && (l.pluginMode = r.pluginMode)),
    a ||
      q(r ? "localized_value_update" : "localized_value_create", {
        code: e.code,
        valueLength: typeof t == "string" ? t.length : 0,
        generatedByAI: !!l.generatedByAI,
        source: k,
      }),
    l
  );
}
function Ba(t, j) {
  switch (j) {
    case "default":
      return t.hash;
    case "imageSrc":
      return (d(Jj(t)), t.imageHash);
    default:
      aA(j);
  }
}
function pa(t, j) {
  if (!j.draft) return;
  let A = t.stores.localizationStore.progressPerLocale[j.id];
  !eA(A) ||
    A === 1 ||
    t.scheduler.runBeforeNextFrame(() => {
      t.stores.localizationStore.progressPerLocale[j.id] === 1 &&
        (t.stores.modalStore.stack.some(({ type: r }) => r === "BatchTranslate") ||
          t.stores.modalStore.set({ type: "LocaleReady", activeLocale: j, source: "automatic" }));
    });
}
function ma(t, j) {
  if (!t.stores.chromeStore.isPreviewingLocaleOnCanvas) return;
  let A = t.stores.treeStore.getDataTreeOrPartialTree().get(j.nodeId);
  if (!$A(A) && !N(A)) return;
  let e = $A(A) ? A.id : A.parentid;
  e && t.stores.codeGenerationStore.updateComponent(e).catch(H);
}
function zo(t, j, A) {
  Oe(t) && (pa(t, A), ma(t, j));
}
function Zo(t, j, A, e, r) {
  let {
    needsReview: o,
    generatedByAI: n,
    pluginId: i,
    pluginMode: s,
    ignoreTracking: a,
    target: k = "default",
    trackingSource: l,
    preserveExistingTextType: c,
    eventType: $,
  } = r;
  if (
    (d(
      typeof j == "string" || j === null || wj(A),
      "RichTextDocument localized values are only supported for rich-text control prop sources"
    ),
    j !== null && typeof j != "string")
  ) {
    (Ea(
      t,
      A,
      e,
      (p) =>
        Qo(j, A, k, e, p, {
          needsReview: o,
          generatedByAI: n,
          pluginId: i,
          pluginMode: s,
          ignoreTracking: a,
          trackingSource: l,
        }),
      $
    ),
      zo(t, A, e));
    return;
  }
  (Yo(
    t,
    A,
    e,
    k,
    (p) => {
      let B = Qo(j, A, k, e, p, {
        needsReview: o,
        generatedByAI: n,
        pluginId: i,
        pluginMode: s,
        ignoreTracking: a,
        trackingSource: l,
      });
      if (
        (k === "imageSrc" &&
          ("imageFocalPoint" in r
            ? r.imageFocalPoint && (B.imageFocalPoint = r.imageFocalPoint)
            : p?.imageFocalPoint && (B.imageFocalPoint = p.imageFocalPoint)),
        c)
      )
        if (k === "default") {
          let m = A.localizedValues[e.id],
            R = m?.value ?? null;
          (d(R === j, "The text type can only be preserved when the source value is unchanged"),
            m?.type && (B.type = m.type));
        } else k === "imageSrc" || aA(k);
      else
        j7(A) &&
          typeof j == "string" &&
          (!wj(A) || typeof A.richTextValue == "string") &&
          (B.type = "rich-text");
      return B;
    },
    $
  ),
    zo(t, A, e));
}
function An(t, j) {
  let A = t.localizedValues[j.id];
  return A ? A.value : null;
}
function l0(t, j, A, e) {
  let r = "default",
    o = An(j, A),
    n = j.localizedValues[A.id];
  if (Jj(j)) {
    let i = j.imageLocalizedValues[A.id];
    i && ((r = "imageSrc"), (o = i.value), (n = i));
  }
  (Zo(t, o, j, A, {
    needsReview: !0,
    ignoreTracking: !0,
    target: r,
    trackingSource: e,
    preserveExistingTextType: !0,
  }),
    q("localized_value_request_review", {
      code: A.code,
      valueLength: typeof o == "string" ? o.length : 0,
      generatedByAI: !!n?.generatedByAI,
      source: e,
    }));
}
function c0(t, j, A, { trackingSource: e, ignoreTracking: r }) {
  let o = "default",
    n = An(j, A),
    i = j.localizedValues[A.id];
  if (Jj(j)) {
    let s = j.imageLocalizedValues[A.id];
    s && ((o = "imageSrc"), (n = s.value), (i = s));
  }
  (Zo(t, n, j, A, {
    ignoreTracking: !0,
    target: o,
    trackingSource: e,
    preserveExistingTextType: !0,
  }),
    r ||
      q("localized_value_approve", {
        code: A.code,
        valueLength: typeof n == "string" ? n.length : 0,
        generatedByAI: !!i?.generatedByAI,
        source: e,
      }));
}
function $0(t, j, A, { target: e, ignoreTracking: r, trackingSource: o, eventType: n }) {
  Yo(
    t,
    j,
    A,
    e,
    (i) => {
      let s = i?.value;
      r ||
        q("localized_value_clear", {
          code: A.code,
          valueLength: typeof s == "string" ? s.length : 0,
          generatedByAI: !!i?.generatedByAI,
          source: o,
        });
    },
    n
  );
}
function jn(t, j, A, e) {
  j.stores.canvasStore.invalidateTransformUntilRendered(() => {
    try {
      (j.stores.scopeStore.select(t, e), j.stores.codeEditorStore.closeEditor());
    } catch {
      (q("open_primary_component_fail", {}),
        L({
          type: "add",
          variant: "error",
          key: "invalid-deep-link",
          primaryText: "Primary component",
          secondaryText: "has been deleted.",
          icon: "error",
          duration: 1e4,
        }));
      return;
    }
    if (!A) return;
    let r = j.tree.getNode(A);
    r &&
      j.tree.getScopeNodeFor(r)?.id === t &&
      (j.stores.selectionStore.set(r.id),
      j.stores.canvasStore.zoomToCenter(sA(j.tree, r), { animated: !1, maxZoom: 1 }));
  });
}
function ua(t, j) {
  let A = t.stores.insertSidebarStore.getEditBehavior(j.moduleId),
    e = JA(t.stores.treeStore.tree, j),
    r = t.componentLoader.componentForIdentifier(j.value)?.annotations;
  return (e && A === "block") || S6(r, "framerDisableUnlink");
}
function en(t, j, A, e) {
  let r = _(t),
    o = qj(j, "canDesign"),
    { includeAccelerator: n = !0 } = e;
  if (Q(r) && (r.type === "canvasComponent" || r.type === "screen" || r.type === "vector"))
    return [
      {
        label: o ? "View" : "Edit",
        accelerator: n ? Vj.fakeSelectChildren : void 0,
        click: () => {
          if (r.type === "vector") {
            let c = j.stores.modulesStore
              .forType("vector")
              .getByStableName(r.localIdName)
              ?.annotations(null, "default")?.framerVector;
            if (!FA(c)) return;
            let { set: $ } = c;
            jn($.id, j, r.localIdName, e);
            return;
          }
          let [, l] = r.localId.split("/");
          l && jn(l, j, A, e);
        },
      },
    ];
  if (V(r) && r.kind === "externalModuleExport") {
    if (e.node?.isVectorInstance)
      return xr(j, t)
        ? YA()
        : [
            {
              label: "Edit",
              accelerator: n ? Vj.fakeSelectChildren : void 0,
              click: () => go(j, r.moduleId, e.node?.id),
              enabled: !o,
            },
          ];
    let l = j.stores.insertSidebarStore.getEditBehavior(r.moduleId),
      c = j.stores.treeStore.tree.getNode(r.moduleId),
      p = e.externalModuleType === "canvasComponent" && c && c.ownerId === "1h0OTH9KlHTd4pyc9CLt";
    if (ua(j, r)) return YA();
    if (!i6(e.externalModuleType)) return YA();
    let { node: B, withUnlinkModal: m, externalModuleType: R } = e;
    return B && l === "autoDetach"
      ? [
          {
            label: "Detach Instance",
            enabled: !o,
            click: async () => {
              (j.beginUndoGroup(),
                Or(
                  {
                    key: "auto-detach",
                    progress: {
                      variant: "progress",
                      primaryText: "Detaching component",
                      secondaryText: "instance\u2026",
                    },
                    error: {
                      variant: "error",
                      primaryText: "Failed to detach",
                      secondaryText: "component instance.",
                    },
                  },
                  async () => {
                    await Ga(B, j, r, R);
                  }
                ).catch(H),
                j.endUndoGroup());
            },
          },
        ]
      : p
        ? [
            {
              label: "Edit",
              accelerator: n ? Vj.fakeSelectChildren : void 0,
              click: () => {
                ho(j, { identifier: r, moduleType: R, instance: B, enterIsolation: !0 }).catch(H);
              },
              enabled: !o,
            },
          ]
        : [
            {
              label: "Unlink",
              click: () => {
                a6(j, {
                  identifier: r,
                  moduleType: R,
                  instance: B,
                  withUnlinkModal: m,
                  enterIsolation: !0,
                }).catch(H);
              },
              enabled: !o,
            },
          ];
  }
  let i = je(j.componentLoader, t);
  if (!i || i.type !== "component" || i.depth !== 0) return YA();
  let s = e.keepHistory
      ? void 0
      : () => {
          j.stores.scopeStore.goBack();
        },
    a = Kj(j.stores.treeStore, j.stores.modulesStore, j.stores.pluginStore, r);
  return [
    ...(a && Ur(j.stores.modulesStore, r) && !o && Q(t)
      ? [
          {
            label: Vr(a),
            click: () => {
              (s?.(), Ae(j, t, a));
            },
          },
        ]
      : []),
    {
      label: `${o ? "View" : "Edit"} Code`,
      click: () => {
        (s?.(), j.stores.codeEditorStore.editFile(i.file));
      },
      enabled: Q(t),
    },
  ];
}
async function Ga(t, j, A, e) {
  let r = new MA(),
    o = await pj(j, {
      identifier: A,
      moduleType: e,
      instance: t,
      renamedIds: r,
      isAutoDetach: !0,
      enterIsolation: !1,
    });
  if (!o) return null;
  let { node: n } = o;
  return W(n)
    ? j.scheduler.processWhenReadyAsync(() => {
        let i = Lj.detachSmartComponentOrWebPageInstance(j, t.draftOrCurrent(), n, r);
        return (i && j.stores.selectionStore.set(i.id), i);
      })
    : null;
}
function tn(t) {
  return t?.find((A) => A.enabled !== !1)?.click;
}
function Ra(t, j, A) {
  return en(t, j, null, A);
}
function M0(t, j, A) {
  return tn(Ra(t, j, A));
}
function Fa(
  t,
  j,
  { withUnlinkModal: A = !0, externalModuleType: e, activateContentPanel: r } = {}
) {
  let o = Mt(j.tree, t);
  return en(t.codeComponentIdentifier, j, o, {
    keepHistory: !0,
    node: t,
    withUnlinkModal: A,
    externalModuleType: e,
    activateContentPanel: r,
  });
}
function D0(t, j, A = {}) {
  return tn(Fa(t, j, A));
}
var QA = Be(nn(), 1);
function fa(t, j) {
  let A = new Map(),
    e = t.variables.filter((r) => r.type === "slug");
  for (let r of e) {
    let o = new Set();
    A.set(r.id, o);
    for (let n of t.getUnsortedChildren()) {
      if (n.id === j.id) continue;
      let i = n.getControlProp(r.id);
      i && v(i.value) && o.add(i.value);
    }
  }
  return A;
}
function q0(t, j) {
  let A = (0, QA.useRef)(null),
    e = (0, QA.useMemo)(() => {
      if (!t || !j) return new Map();
      let r = fa(t, j),
        o = A.current;
      if (!o) return r;
      let n = new Map();
      for (let [i, s] of r) {
        n.set(i, s);
        let a = o.get(i);
        a && Oj(a, s) && n.set(i, a);
      }
      return n;
    }, [t, j]);
  return (
    (0, QA.useEffect)(() => {
      A.current = e;
    }, [e]),
    e
  );
}
function U0(t, j, A, e) {
  for (let r of t.variables) {
    if (r.type === "divider" || !rt(r)) continue;
    let o = A.get(r.id);
    d(o, "existingSlugValueForVariable is required for slug fields");
    let n = e?.get(r.id) ?? j.getControlProp(r.id);
    if ((!n || !v(n.value) || !n.value.length) && r.associatedStringVariable) {
      let s = e?.get(r.associatedStringVariable) ?? j.getControlProp(r.associatedStringVariable);
      if (s && v(s.value)) {
        let a = z6(s.value);
        if (a.length && !o.has(a))
          return (
            j.setControlProp(r.id, { type: "string", value: a }),
            { changedControlProp: r.id }
          );
      }
    }
  }
  return !1;
}
export {
  wd as a,
  lo as b,
  Jn as c,
  r6 as d,
  mo as e,
  uo as f,
  _7 as g,
  sn as h,
  Ma as i,
  K7 as j,
  xa as k,
  O7 as l,
  T7 as m,
  qa as n,
  L7 as o,
  cj as p,
  Za as q,
  As as r,
  N7 as s,
  Y7 as t,
  Sn as u,
  yn as v,
  t6 as w,
  Jl as x,
  ko as y,
  q7 as z,
  o6 as A,
  Qn as B,
  Ro as C,
  Wn as D,
  Fo as E,
  je as F,
  i6 as G,
  a6 as H,
  ho as I,
  _d as J,
  k6 as K,
  ti as L,
  vd as M,
  Pd as N,
  Md as O,
  Dd as P,
  Cd as Q,
  d6 as R,
  se as S,
  FB as T,
  ia as U,
  fB as V,
  IB as W,
  jn as X,
  ua as Y,
  Ga as Z,
  Ra as _,
  M0 as $,
  Fa as aa,
  D0 as ba,
  PB as ca,
  sa as da,
  MB as ea,
  DB as fa,
  CB as ga,
  NB as ha,
  ka as ia,
  p6 as ja,
  LB as ka,
  fa as la,
  q0 as ma,
  U0 as na,
  Zo as oa,
  l0 as pa,
  c0 as qa,
  $0 as ra,
};
//# sourceMappingURL=chunk-4YPNFGEP.mjs.map
